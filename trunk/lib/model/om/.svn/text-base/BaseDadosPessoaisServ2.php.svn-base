<?php

/**
 * Base class that represents a row from the 'DADOS_PESSOAIS_SERV_2' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:22:46 2010
 *
 * @package    lib.model.om
 */
abstract class BaseDadosPessoaisServ2 extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        DadosPessoaisServ2Peer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the dt_versao_orc field.
	 * @var        int
	 */
	protected $dt_versao_orc;

	/**
	 * The value for the cd_orgao field.
	 * @var        string
	 */
	protected $cd_orgao;

	/**
	 * The value for the cd_unid_orc field.
	 * @var        string
	 */
	protected $cd_unid_orc;

	/**
	 * The value for the cd_cpf_serv_muni field.
	 * @var        string
	 */
	protected $cd_cpf_serv_muni;

	/**
	 * The value for the nu_identidade_ds field.
	 * @var        string
	 */
	protected $nu_identidade_ds;

	/**
	 * The value for the de_exped_ident_ds field.
	 * @var        string
	 */
	protected $de_exped_ident_ds;

	/**
	 * The value for the nu_tit_eleitor_ds field.
	 * @var        string
	 */
	protected $nu_tit_eleitor_ds;

	/**
	 * The value for the cd_est_civil field.
	 * @var        string
	 */
	protected $cd_est_civil;

	/**
	 * The value for the cd_instrucao field.
	 * @var        string
	 */
	protected $cd_instrucao;

	/**
	 * The value for the cd_sexo_ds field.
	 * @var        string
	 */
	protected $cd_sexo_ds;

	/**
	 * The value for the nm_serv_muni_ds field.
	 * @var        string
	 */
	protected $nm_serv_muni_ds;

	/**
	 * The value for the de_endereco_ds field.
	 * @var        string
	 */
	protected $de_endereco_ds;

	/**
	 * The value for the nu_fone_ds field.
	 * @var        string
	 */
	protected $nu_fone_ds;

	/**
	 * The value for the dt_nascim_ds field.
	 * @var        string
	 */
	protected $dt_nascim_ds;

	/**
	 * @var        EstadosCivis
	 */
	protected $aEstadosCivis;

	/**
	 * @var        GrausInstrucao
	 */
	protected $aGrausInstrucao;

	/**
	 * @var        ServidoresMunicipais
	 */
	protected $aServidoresMunicipais;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'DadosPessoaisServ2Peer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [dt_versao_orc] column value.
	 * 
	 * @return     int
	 */
	public function getDtVersaoOrc()
	{
		return $this->dt_versao_orc;
	}

	/**
	 * Get the [cd_orgao] column value.
	 * 
	 * @return     string
	 */
	public function getCdOrgao()
	{
		return $this->cd_orgao;
	}

	/**
	 * Get the [cd_unid_orc] column value.
	 * 
	 * @return     string
	 */
	public function getCdUnidOrc()
	{
		return $this->cd_unid_orc;
	}

	/**
	 * Get the [cd_cpf_serv_muni] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfServMuni()
	{
		return $this->cd_cpf_serv_muni;
	}

	/**
	 * Get the [nu_identidade_ds] column value.
	 * 
	 * @return     string
	 */
	public function getNuIdentidadeDs()
	{
		return $this->nu_identidade_ds;
	}

	/**
	 * Get the [de_exped_ident_ds] column value.
	 * 
	 * @return     string
	 */
	public function getDeExpedIdentDs()
	{
		return $this->de_exped_ident_ds;
	}

	/**
	 * Get the [nu_tit_eleitor_ds] column value.
	 * 
	 * @return     string
	 */
	public function getNuTitEleitorDs()
	{
		return $this->nu_tit_eleitor_ds;
	}

	/**
	 * Get the [cd_est_civil] column value.
	 * 
	 * @return     string
	 */
	public function getCdEstCivil()
	{
		return $this->cd_est_civil;
	}

	/**
	 * Get the [cd_instrucao] column value.
	 * 
	 * @return     string
	 */
	public function getCdInstrucao()
	{
		return $this->cd_instrucao;
	}

	/**
	 * Get the [cd_sexo_ds] column value.
	 * 
	 * @return     string
	 */
	public function getCdSexoDs()
	{
		return $this->cd_sexo_ds;
	}

	/**
	 * Get the [nm_serv_muni_ds] column value.
	 * 
	 * @return     string
	 */
	public function getNmServMuniDs()
	{
		return $this->nm_serv_muni_ds;
	}

	/**
	 * Get the [de_endereco_ds] column value.
	 * 
	 * @return     string
	 */
	public function getDeEnderecoDs()
	{
		return $this->de_endereco_ds;
	}

	/**
	 * Get the [nu_fone_ds] column value.
	 * 
	 * @return     string
	 */
	public function getNuFoneDs()
	{
		return $this->nu_fone_ds;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_nascim_ds] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtNascimDs($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_nascim_ds === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_nascim_ds);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_nascim_ds, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_MUNICIPIO;
		}

		if ($this->aServidoresMunicipais !== null && $this->aServidoresMunicipais->getCdMunicipio() !== $v) {
			$this->aServidoresMunicipais = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [dt_versao_orc] column.
	 * 
	 * @param      int $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setDtVersaoOrc($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_versao_orc !== $v) {
			$this->dt_versao_orc = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::DT_VERSAO_ORC;
		}

		if ($this->aServidoresMunicipais !== null && $this->aServidoresMunicipais->getDtVersaoOrc() !== $v) {
			$this->aServidoresMunicipais = null;
		}

		return $this;
	} // setDtVersaoOrc()

	/**
	 * Set the value of [cd_orgao] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdOrgao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_orgao !== $v) {
			$this->cd_orgao = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_ORGAO;
		}

		if ($this->aServidoresMunicipais !== null && $this->aServidoresMunicipais->getCdOrgao() !== $v) {
			$this->aServidoresMunicipais = null;
		}

		return $this;
	} // setCdOrgao()

	/**
	 * Set the value of [cd_unid_orc] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdUnidOrc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_unid_orc !== $v) {
			$this->cd_unid_orc = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_UNID_ORC;
		}

		if ($this->aServidoresMunicipais !== null && $this->aServidoresMunicipais->getCdUnidOrc() !== $v) {
			$this->aServidoresMunicipais = null;
		}

		return $this;
	} // setCdUnidOrc()

	/**
	 * Set the value of [cd_cpf_serv_muni] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdCpfServMuni($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_serv_muni !== $v) {
			$this->cd_cpf_serv_muni = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_CPF_SERV_MUNI;
		}

		return $this;
	} // setCdCpfServMuni()

	/**
	 * Set the value of [nu_identidade_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setNuIdentidadeDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_identidade_ds !== $v) {
			$this->nu_identidade_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::NU_IDENTIDADE_DS;
		}

		return $this;
	} // setNuIdentidadeDs()

	/**
	 * Set the value of [de_exped_ident_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setDeExpedIdentDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->de_exped_ident_ds !== $v) {
			$this->de_exped_ident_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::DE_EXPED_IDENT_DS;
		}

		return $this;
	} // setDeExpedIdentDs()

	/**
	 * Set the value of [nu_tit_eleitor_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setNuTitEleitorDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_tit_eleitor_ds !== $v) {
			$this->nu_tit_eleitor_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::NU_TIT_ELEITOR_DS;
		}

		return $this;
	} // setNuTitEleitorDs()

	/**
	 * Set the value of [cd_est_civil] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdEstCivil($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_est_civil !== $v) {
			$this->cd_est_civil = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_EST_CIVIL;
		}

		if ($this->aEstadosCivis !== null && $this->aEstadosCivis->getCdEstCivil() !== $v) {
			$this->aEstadosCivis = null;
		}

		return $this;
	} // setCdEstCivil()

	/**
	 * Set the value of [cd_instrucao] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdInstrucao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_instrucao !== $v) {
			$this->cd_instrucao = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_INSTRUCAO;
		}

		if ($this->aGrausInstrucao !== null && $this->aGrausInstrucao->getCdInstrucao() !== $v) {
			$this->aGrausInstrucao = null;
		}

		return $this;
	} // setCdInstrucao()

	/**
	 * Set the value of [cd_sexo_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setCdSexoDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_sexo_ds !== $v) {
			$this->cd_sexo_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::CD_SEXO_DS;
		}

		return $this;
	} // setCdSexoDs()

	/**
	 * Set the value of [nm_serv_muni_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setNmServMuniDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nm_serv_muni_ds !== $v) {
			$this->nm_serv_muni_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::NM_SERV_MUNI_DS;
		}

		return $this;
	} // setNmServMuniDs()

	/**
	 * Set the value of [de_endereco_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setDeEnderecoDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->de_endereco_ds !== $v) {
			$this->de_endereco_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::DE_ENDERECO_DS;
		}

		return $this;
	} // setDeEnderecoDs()

	/**
	 * Set the value of [nu_fone_ds] column.
	 * 
	 * @param      string $v new value
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setNuFoneDs($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_fone_ds !== $v) {
			$this->nu_fone_ds = $v;
			$this->modifiedColumns[] = DadosPessoaisServ2Peer::NU_FONE_DS;
		}

		return $this;
	} // setNuFoneDs()

	/**
	 * Sets the value of [dt_nascim_ds] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 */
	public function setDtNascimDs($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_nascim_ds !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_nascim_ds !== null && $tmpDt = new MssqlDateTime($this->dt_nascim_ds)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_nascim_ds = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = DadosPessoaisServ2Peer::DT_NASCIM_DS;
			}
		} // if either are not null

		return $this;
	} // setDtNascimDs()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->dt_versao_orc = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_orgao = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->cd_unid_orc = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->cd_cpf_serv_muni = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->nu_identidade_ds = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->de_exped_ident_ds = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->nu_tit_eleitor_ds = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->cd_est_civil = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->cd_instrucao = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->cd_sexo_ds = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->nm_serv_muni_ds = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->de_endereco_ds = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->nu_fone_ds = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->dt_nascim_ds = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 15; // 15 = DadosPessoaisServ2Peer::NUM_COLUMNS - DadosPessoaisServ2Peer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating DadosPessoaisServ2 object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aServidoresMunicipais !== null && $this->cd_municipio !== $this->aServidoresMunicipais->getCdMunicipio()) {
			$this->aServidoresMunicipais = null;
		}
		if ($this->aServidoresMunicipais !== null && $this->dt_versao_orc !== $this->aServidoresMunicipais->getDtVersaoOrc()) {
			$this->aServidoresMunicipais = null;
		}
		if ($this->aServidoresMunicipais !== null && $this->cd_orgao !== $this->aServidoresMunicipais->getCdOrgao()) {
			$this->aServidoresMunicipais = null;
		}
		if ($this->aServidoresMunicipais !== null && $this->cd_unid_orc !== $this->aServidoresMunicipais->getCdUnidOrc()) {
			$this->aServidoresMunicipais = null;
		}
		if ($this->aEstadosCivis !== null && $this->cd_est_civil !== $this->aEstadosCivis->getCdEstCivil()) {
			$this->aEstadosCivis = null;
		}
		if ($this->aGrausInstrucao !== null && $this->cd_instrucao !== $this->aGrausInstrucao->getCdInstrucao()) {
			$this->aGrausInstrucao = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DadosPessoaisServ2Peer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = DadosPessoaisServ2Peer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aEstadosCivis = null;
			$this->aGrausInstrucao = null;
			$this->aServidoresMunicipais = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DadosPessoaisServ2Peer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseDadosPessoaisServ2:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				DadosPessoaisServ2Peer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseDadosPessoaisServ2:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DadosPessoaisServ2Peer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseDadosPessoaisServ2:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseDadosPessoaisServ2:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				DadosPessoaisServ2Peer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEstadosCivis !== null) {
				if ($this->aEstadosCivis->isModified() || $this->aEstadosCivis->isNew()) {
					$affectedRows += $this->aEstadosCivis->save($con);
				}
				$this->setEstadosCivis($this->aEstadosCivis);
			}

			if ($this->aGrausInstrucao !== null) {
				if ($this->aGrausInstrucao->isModified() || $this->aGrausInstrucao->isNew()) {
					$affectedRows += $this->aGrausInstrucao->save($con);
				}
				$this->setGrausInstrucao($this->aGrausInstrucao);
			}

			if ($this->aServidoresMunicipais !== null) {
				if ($this->aServidoresMunicipais->isModified() || $this->aServidoresMunicipais->isNew()) {
					$affectedRows += $this->aServidoresMunicipais->save($con);
				}
				$this->setServidoresMunicipais($this->aServidoresMunicipais);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = DadosPessoaisServ2Peer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += DadosPessoaisServ2Peer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aEstadosCivis !== null) {
				if (!$this->aEstadosCivis->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aEstadosCivis->getValidationFailures());
				}
			}

			if ($this->aGrausInstrucao !== null) {
				if (!$this->aGrausInstrucao->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aGrausInstrucao->getValidationFailures());
				}
			}

			if ($this->aServidoresMunicipais !== null) {
				if (!$this->aServidoresMunicipais->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aServidoresMunicipais->getValidationFailures());
				}
			}


			if (($retval = DadosPessoaisServ2Peer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = DadosPessoaisServ2Peer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getDtVersaoOrc();
				break;
			case 2:
				return $this->getCdOrgao();
				break;
			case 3:
				return $this->getCdUnidOrc();
				break;
			case 4:
				return $this->getCdCpfServMuni();
				break;
			case 5:
				return $this->getNuIdentidadeDs();
				break;
			case 6:
				return $this->getDeExpedIdentDs();
				break;
			case 7:
				return $this->getNuTitEleitorDs();
				break;
			case 8:
				return $this->getCdEstCivil();
				break;
			case 9:
				return $this->getCdInstrucao();
				break;
			case 10:
				return $this->getCdSexoDs();
				break;
			case 11:
				return $this->getNmServMuniDs();
				break;
			case 12:
				return $this->getDeEnderecoDs();
				break;
			case 13:
				return $this->getNuFoneDs();
				break;
			case 14:
				return $this->getDtNascimDs();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = DadosPessoaisServ2Peer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getDtVersaoOrc(),
			$keys[2] => $this->getCdOrgao(),
			$keys[3] => $this->getCdUnidOrc(),
			$keys[4] => $this->getCdCpfServMuni(),
			$keys[5] => $this->getNuIdentidadeDs(),
			$keys[6] => $this->getDeExpedIdentDs(),
			$keys[7] => $this->getNuTitEleitorDs(),
			$keys[8] => $this->getCdEstCivil(),
			$keys[9] => $this->getCdInstrucao(),
			$keys[10] => $this->getCdSexoDs(),
			$keys[11] => $this->getNmServMuniDs(),
			$keys[12] => $this->getDeEnderecoDs(),
			$keys[13] => $this->getNuFoneDs(),
			$keys[14] => $this->getDtNascimDs(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = DadosPessoaisServ2Peer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setDtVersaoOrc($value);
				break;
			case 2:
				$this->setCdOrgao($value);
				break;
			case 3:
				$this->setCdUnidOrc($value);
				break;
			case 4:
				$this->setCdCpfServMuni($value);
				break;
			case 5:
				$this->setNuIdentidadeDs($value);
				break;
			case 6:
				$this->setDeExpedIdentDs($value);
				break;
			case 7:
				$this->setNuTitEleitorDs($value);
				break;
			case 8:
				$this->setCdEstCivil($value);
				break;
			case 9:
				$this->setCdInstrucao($value);
				break;
			case 10:
				$this->setCdSexoDs($value);
				break;
			case 11:
				$this->setNmServMuniDs($value);
				break;
			case 12:
				$this->setDeEnderecoDs($value);
				break;
			case 13:
				$this->setNuFoneDs($value);
				break;
			case 14:
				$this->setDtNascimDs($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = DadosPessoaisServ2Peer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDtVersaoOrc($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdOrgao($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCdUnidOrc($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCdCpfServMuni($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setNuIdentidadeDs($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setDeExpedIdentDs($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setNuTitEleitorDs($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCdEstCivil($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCdInstrucao($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setCdSexoDs($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setNmServMuniDs($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setDeEnderecoDs($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setNuFoneDs($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setDtNascimDs($arr[$keys[14]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(DadosPessoaisServ2Peer::DATABASE_NAME);

		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_MUNICIPIO)) $criteria->add(DadosPessoaisServ2Peer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::DT_VERSAO_ORC)) $criteria->add(DadosPessoaisServ2Peer::DT_VERSAO_ORC, $this->dt_versao_orc);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_ORGAO)) $criteria->add(DadosPessoaisServ2Peer::CD_ORGAO, $this->cd_orgao);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_UNID_ORC)) $criteria->add(DadosPessoaisServ2Peer::CD_UNID_ORC, $this->cd_unid_orc);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_CPF_SERV_MUNI)) $criteria->add(DadosPessoaisServ2Peer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::NU_IDENTIDADE_DS)) $criteria->add(DadosPessoaisServ2Peer::NU_IDENTIDADE_DS, $this->nu_identidade_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::DE_EXPED_IDENT_DS)) $criteria->add(DadosPessoaisServ2Peer::DE_EXPED_IDENT_DS, $this->de_exped_ident_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::NU_TIT_ELEITOR_DS)) $criteria->add(DadosPessoaisServ2Peer::NU_TIT_ELEITOR_DS, $this->nu_tit_eleitor_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_EST_CIVIL)) $criteria->add(DadosPessoaisServ2Peer::CD_EST_CIVIL, $this->cd_est_civil);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_INSTRUCAO)) $criteria->add(DadosPessoaisServ2Peer::CD_INSTRUCAO, $this->cd_instrucao);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::CD_SEXO_DS)) $criteria->add(DadosPessoaisServ2Peer::CD_SEXO_DS, $this->cd_sexo_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::NM_SERV_MUNI_DS)) $criteria->add(DadosPessoaisServ2Peer::NM_SERV_MUNI_DS, $this->nm_serv_muni_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::DE_ENDERECO_DS)) $criteria->add(DadosPessoaisServ2Peer::DE_ENDERECO_DS, $this->de_endereco_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::NU_FONE_DS)) $criteria->add(DadosPessoaisServ2Peer::NU_FONE_DS, $this->nu_fone_ds);
		if ($this->isColumnModified(DadosPessoaisServ2Peer::DT_NASCIM_DS)) $criteria->add(DadosPessoaisServ2Peer::DT_NASCIM_DS, $this->dt_nascim_ds);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(DadosPessoaisServ2Peer::DATABASE_NAME);

		$criteria->add(DadosPessoaisServ2Peer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(DadosPessoaisServ2Peer::DT_VERSAO_ORC, $this->dt_versao_orc);
		$criteria->add(DadosPessoaisServ2Peer::CD_ORGAO, $this->cd_orgao);
		$criteria->add(DadosPessoaisServ2Peer::CD_UNID_ORC, $this->cd_unid_orc);
		$criteria->add(DadosPessoaisServ2Peer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getDtVersaoOrc();

		$pks[2] = $this->getCdOrgao();

		$pks[3] = $this->getCdUnidOrc();

		$pks[4] = $this->getCdCpfServMuni();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setDtVersaoOrc($keys[1]);

		$this->setCdOrgao($keys[2]);

		$this->setCdUnidOrc($keys[3]);

		$this->setCdCpfServMuni($keys[4]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of DadosPessoaisServ2 (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setDtVersaoOrc($this->dt_versao_orc);

		$copyObj->setCdOrgao($this->cd_orgao);

		$copyObj->setCdUnidOrc($this->cd_unid_orc);

		$copyObj->setCdCpfServMuni($this->cd_cpf_serv_muni);

		$copyObj->setNuIdentidadeDs($this->nu_identidade_ds);

		$copyObj->setDeExpedIdentDs($this->de_exped_ident_ds);

		$copyObj->setNuTitEleitorDs($this->nu_tit_eleitor_ds);

		$copyObj->setCdEstCivil($this->cd_est_civil);

		$copyObj->setCdInstrucao($this->cd_instrucao);

		$copyObj->setCdSexoDs($this->cd_sexo_ds);

		$copyObj->setNmServMuniDs($this->nm_serv_muni_ds);

		$copyObj->setDeEnderecoDs($this->de_endereco_ds);

		$copyObj->setNuFoneDs($this->nu_fone_ds);

		$copyObj->setDtNascimDs($this->dt_nascim_ds);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     DadosPessoaisServ2 Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     DadosPessoaisServ2Peer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new DadosPessoaisServ2Peer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a EstadosCivis object.
	 *
	 * @param      EstadosCivis $v
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setEstadosCivis(EstadosCivis $v = null)
	{
		if ($v === null) {
			$this->setCdEstCivil(NULL);
		} else {
			$this->setCdEstCivil($v->getCdEstCivil());
		}

		$this->aEstadosCivis = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the EstadosCivis object, it will not be re-added.
		if ($v !== null) {
			$v->addDadosPessoaisServ2($this);
		}

		return $this;
	}


	/**
	 * Get the associated EstadosCivis object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     EstadosCivis The associated EstadosCivis object.
	 * @throws     PropelException
	 */
	public function getEstadosCivis(PropelPDO $con = null)
	{
		if ($this->aEstadosCivis === null && (($this->cd_est_civil !== "" && $this->cd_est_civil !== null))) {
			$this->aEstadosCivis = EstadosCivisPeer::retrieveByPk($this->cd_est_civil);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aEstadosCivis->addDadosPessoaisServ2s($this);
			 */
		}
		return $this->aEstadosCivis;
	}

	/**
	 * Declares an association between this object and a GrausInstrucao object.
	 *
	 * @param      GrausInstrucao $v
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setGrausInstrucao(GrausInstrucao $v = null)
	{
		if ($v === null) {
			$this->setCdInstrucao(NULL);
		} else {
			$this->setCdInstrucao($v->getCdInstrucao());
		}

		$this->aGrausInstrucao = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the GrausInstrucao object, it will not be re-added.
		if ($v !== null) {
			$v->addDadosPessoaisServ2($this);
		}

		return $this;
	}


	/**
	 * Get the associated GrausInstrucao object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     GrausInstrucao The associated GrausInstrucao object.
	 * @throws     PropelException
	 */
	public function getGrausInstrucao(PropelPDO $con = null)
	{
		if ($this->aGrausInstrucao === null && (($this->cd_instrucao !== "" && $this->cd_instrucao !== null))) {
			$this->aGrausInstrucao = GrausInstrucaoPeer::retrieveByPk($this->cd_instrucao);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aGrausInstrucao->addDadosPessoaisServ2s($this);
			 */
		}
		return $this->aGrausInstrucao;
	}

	/**
	 * Declares an association between this object and a ServidoresMunicipais object.
	 *
	 * @param      ServidoresMunicipais $v
	 * @return     DadosPessoaisServ2 The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setServidoresMunicipais(ServidoresMunicipais $v = null)
	{
		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		$this->aServidoresMunicipais = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the ServidoresMunicipais object, it will not be re-added.
		if ($v !== null) {
			$v->addDadosPessoaisServ2($this);
		}

		return $this;
	}


	/**
	 * Get the associated ServidoresMunicipais object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     ServidoresMunicipais The associated ServidoresMunicipais object.
	 * @throws     PropelException
	 */
	public function getServidoresMunicipais(PropelPDO $con = null)
	{
		if ($this->aServidoresMunicipais === null && (($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->cd_orgao !== "" && $this->cd_orgao !== null) && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && $this->dt_versao_orc !== null)) {
			$c = new Criteria(ServidoresMunicipaisPeer::DATABASE_NAME);
			$c->add(ServidoresMunicipaisPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(ServidoresMunicipaisPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(ServidoresMunicipaisPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(ServidoresMunicipaisPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$this->aServidoresMunicipais = ServidoresMunicipaisPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aServidoresMunicipais->addDadosPessoaisServ2s($this);
			 */
		}
		return $this->aServidoresMunicipais;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aEstadosCivis = null;
			$this->aGrausInstrucao = null;
			$this->aServidoresMunicipais = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseDadosPessoaisServ2:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseDadosPessoaisServ2::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseDadosPessoaisServ2
