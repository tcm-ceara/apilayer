<?php

/**
 * Base class that represents a row from the 'REINGRESSOS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:23:11 2010
 *
 * @package    lib.model.om
 */
abstract class BaseReingressos extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ReingressosPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the dt_versao_orc field.
	 * @var        int
	 */
	protected $dt_versao_orc;

	/**
	 * The value for the cd_orgao field.
	 * @var        string
	 */
	protected $cd_orgao;

	/**
	 * The value for the cd_unid_orc field.
	 * @var        string
	 */
	protected $cd_unid_orc;

	/**
	 * The value for the cd_cpf_serv_muni field.
	 * @var        string
	 */
	protected $cd_cpf_serv_muni;

	/**
	 * The value for the cd_ingresso field.
	 * @var        string
	 */
	protected $cd_ingresso;

	/**
	 * The value for the cd_vinculo field.
	 * @var        string
	 */
	protected $cd_vinculo;

	/**
	 * The value for the nu_expediente_no field.
	 * @var        string
	 */
	protected $nu_expediente_no;

	/**
	 * The value for the nu_expediente_de field.
	 * @var        string
	 */
	protected $nu_expediente_de;

	/**
	 * The value for the nu_expediente_re field.
	 * @var        string
	 */
	protected $nu_expediente_re;

	/**
	 * The value for the cd_reingresso field.
	 * @var        string
	 */
	protected $cd_reingresso;

	/**
	 * The value for the cd_expediente field.
	 * @var        string
	 */
	protected $cd_expediente;

	/**
	 * The value for the dt_expediente_re field.
	 * @var        string
	 */
	protected $dt_expediente_re;

	/**
	 * The value for the dt_public_re field.
	 * @var        string
	 */
	protected $dt_public_re;

	/**
	 * The value for the cd_amparo_legal field.
	 * @var        string
	 */
	protected $cd_amparo_legal;

	/**
	 * The value for the nu_amparo_legal_re field.
	 * @var        string
	 */
	protected $nu_amparo_legal_re;

	/**
	 * The value for the dt_amparo_legal_re field.
	 * @var        string
	 */
	protected $dt_amparo_legal_re;

	/**
	 * The value for the dt_public_amparo_re field.
	 * @var        string
	 */
	protected $dt_public_amparo_re;

	/**
	 * The value for the dt_ref_re field.
	 * @var        int
	 */
	protected $dt_ref_re;

	/**
	 * @var        TiposReingressos
	 */
	protected $aTiposReingressos;

	/**
	 * @var        TiposExpedientes
	 */
	protected $aTiposExpedientes;

	/**
	 * @var        TiposAmparosLegais
	 */
	protected $aTiposAmparosLegais;

	/**
	 * @var        Desligamentos
	 */
	protected $aDesligamentos;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'ReingressosPeer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [dt_versao_orc] column value.
	 * 
	 * @return     int
	 */
	public function getDtVersaoOrc()
	{
		return $this->dt_versao_orc;
	}

	/**
	 * Get the [cd_orgao] column value.
	 * 
	 * @return     string
	 */
	public function getCdOrgao()
	{
		return $this->cd_orgao;
	}

	/**
	 * Get the [cd_unid_orc] column value.
	 * 
	 * @return     string
	 */
	public function getCdUnidOrc()
	{
		return $this->cd_unid_orc;
	}

	/**
	 * Get the [cd_cpf_serv_muni] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfServMuni()
	{
		return $this->cd_cpf_serv_muni;
	}

	/**
	 * Get the [cd_ingresso] column value.
	 * 
	 * @return     string
	 */
	public function getCdIngresso()
	{
		return $this->cd_ingresso;
	}

	/**
	 * Get the [cd_vinculo] column value.
	 * 
	 * @return     string
	 */
	public function getCdVinculo()
	{
		return $this->cd_vinculo;
	}

	/**
	 * Get the [nu_expediente_no] column value.
	 * 
	 * @return     string
	 */
	public function getNuExpedienteNo()
	{
		return $this->nu_expediente_no;
	}

	/**
	 * Get the [nu_expediente_de] column value.
	 * 
	 * @return     string
	 */
	public function getNuExpedienteDe()
	{
		return $this->nu_expediente_de;
	}

	/**
	 * Get the [nu_expediente_re] column value.
	 * 
	 * @return     string
	 */
	public function getNuExpedienteRe()
	{
		return $this->nu_expediente_re;
	}

	/**
	 * Get the [cd_reingresso] column value.
	 * 
	 * @return     string
	 */
	public function getCdReingresso()
	{
		return $this->cd_reingresso;
	}

	/**
	 * Get the [cd_expediente] column value.
	 * 
	 * @return     string
	 */
	public function getCdExpediente()
	{
		return $this->cd_expediente;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_expediente_re] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtExpedienteRe($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_expediente_re === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_expediente_re);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_expediente_re, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [dt_public_re] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtPublicRe($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_public_re === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_public_re);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_public_re, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [cd_amparo_legal] column value.
	 * 
	 * @return     string
	 */
	public function getCdAmparoLegal()
	{
		return $this->cd_amparo_legal;
	}

	/**
	 * Get the [nu_amparo_legal_re] column value.
	 * 
	 * @return     string
	 */
	public function getNuAmparoLegalRe()
	{
		return $this->nu_amparo_legal_re;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_amparo_legal_re] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtAmparoLegalRe($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_amparo_legal_re === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_amparo_legal_re);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_amparo_legal_re, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [dt_public_amparo_re] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtPublicAmparoRe($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_public_amparo_re === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_public_amparo_re);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_public_amparo_re, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [dt_ref_re] column value.
	 * 
	 * @return     int
	 */
	public function getDtRefRe()
	{
		return $this->dt_ref_re;
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_MUNICIPIO;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getCdMunicipio() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [dt_versao_orc] column.
	 * 
	 * @param      int $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtVersaoOrc($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_versao_orc !== $v) {
			$this->dt_versao_orc = $v;
			$this->modifiedColumns[] = ReingressosPeer::DT_VERSAO_ORC;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getDtVersaoOrc() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setDtVersaoOrc()

	/**
	 * Set the value of [cd_orgao] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdOrgao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_orgao !== $v) {
			$this->cd_orgao = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_ORGAO;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getCdOrgao() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setCdOrgao()

	/**
	 * Set the value of [cd_unid_orc] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdUnidOrc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_unid_orc !== $v) {
			$this->cd_unid_orc = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_UNID_ORC;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getCdUnidOrc() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setCdUnidOrc()

	/**
	 * Set the value of [cd_cpf_serv_muni] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdCpfServMuni($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_serv_muni !== $v) {
			$this->cd_cpf_serv_muni = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_CPF_SERV_MUNI;
		}

		return $this;
	} // setCdCpfServMuni()

	/**
	 * Set the value of [cd_ingresso] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdIngresso($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_ingresso !== $v) {
			$this->cd_ingresso = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_INGRESSO;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getCdIngresso() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setCdIngresso()

	/**
	 * Set the value of [cd_vinculo] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdVinculo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_vinculo !== $v) {
			$this->cd_vinculo = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_VINCULO;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getCdVinculo() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setCdVinculo()

	/**
	 * Set the value of [nu_expediente_no] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setNuExpedienteNo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_expediente_no !== $v) {
			$this->nu_expediente_no = $v;
			$this->modifiedColumns[] = ReingressosPeer::NU_EXPEDIENTE_NO;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getNuExpedienteNo() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setNuExpedienteNo()

	/**
	 * Set the value of [nu_expediente_de] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setNuExpedienteDe($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_expediente_de !== $v) {
			$this->nu_expediente_de = $v;
			$this->modifiedColumns[] = ReingressosPeer::NU_EXPEDIENTE_DE;
		}

		if ($this->aDesligamentos !== null && $this->aDesligamentos->getNuExpedienteDe() !== $v) {
			$this->aDesligamentos = null;
		}

		return $this;
	} // setNuExpedienteDe()

	/**
	 * Set the value of [nu_expediente_re] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setNuExpedienteRe($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_expediente_re !== $v) {
			$this->nu_expediente_re = $v;
			$this->modifiedColumns[] = ReingressosPeer::NU_EXPEDIENTE_RE;
		}

		return $this;
	} // setNuExpedienteRe()

	/**
	 * Set the value of [cd_reingresso] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdReingresso($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_reingresso !== $v) {
			$this->cd_reingresso = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_REINGRESSO;
		}

		if ($this->aTiposReingressos !== null && $this->aTiposReingressos->getCdReingresso() !== $v) {
			$this->aTiposReingressos = null;
		}

		return $this;
	} // setCdReingresso()

	/**
	 * Set the value of [cd_expediente] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdExpediente($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_expediente !== $v) {
			$this->cd_expediente = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_EXPEDIENTE;
		}

		if ($this->aTiposExpedientes !== null && $this->aTiposExpedientes->getCdExpediente() !== $v) {
			$this->aTiposExpedientes = null;
		}

		return $this;
	} // setCdExpediente()

	/**
	 * Sets the value of [dt_expediente_re] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtExpedienteRe($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_expediente_re !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_expediente_re !== null && $tmpDt = new MssqlDateTime($this->dt_expediente_re)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_expediente_re = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ReingressosPeer::DT_EXPEDIENTE_RE;
			}
		} // if either are not null

		return $this;
	} // setDtExpedienteRe()

	/**
	 * Sets the value of [dt_public_re] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtPublicRe($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_public_re !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_public_re !== null && $tmpDt = new MssqlDateTime($this->dt_public_re)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_public_re = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ReingressosPeer::DT_PUBLIC_RE;
			}
		} // if either are not null

		return $this;
	} // setDtPublicRe()

	/**
	 * Set the value of [cd_amparo_legal] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setCdAmparoLegal($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_amparo_legal !== $v) {
			$this->cd_amparo_legal = $v;
			$this->modifiedColumns[] = ReingressosPeer::CD_AMPARO_LEGAL;
		}

		if ($this->aTiposAmparosLegais !== null && $this->aTiposAmparosLegais->getCdAmparoLegal() !== $v) {
			$this->aTiposAmparosLegais = null;
		}

		return $this;
	} // setCdAmparoLegal()

	/**
	 * Set the value of [nu_amparo_legal_re] column.
	 * 
	 * @param      string $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setNuAmparoLegalRe($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_amparo_legal_re !== $v) {
			$this->nu_amparo_legal_re = $v;
			$this->modifiedColumns[] = ReingressosPeer::NU_AMPARO_LEGAL_RE;
		}

		return $this;
	} // setNuAmparoLegalRe()

	/**
	 * Sets the value of [dt_amparo_legal_re] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtAmparoLegalRe($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_amparo_legal_re !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_amparo_legal_re !== null && $tmpDt = new MssqlDateTime($this->dt_amparo_legal_re)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_amparo_legal_re = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ReingressosPeer::DT_AMPARO_LEGAL_RE;
			}
		} // if either are not null

		return $this;
	} // setDtAmparoLegalRe()

	/**
	 * Sets the value of [dt_public_amparo_re] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtPublicAmparoRe($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_public_amparo_re !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_public_amparo_re !== null && $tmpDt = new MssqlDateTime($this->dt_public_amparo_re)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_public_amparo_re = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ReingressosPeer::DT_PUBLIC_AMPARO_RE;
			}
		} // if either are not null

		return $this;
	} // setDtPublicAmparoRe()

	/**
	 * Set the value of [dt_ref_re] column.
	 * 
	 * @param      int $v new value
	 * @return     Reingressos The current object (for fluent API support)
	 */
	public function setDtRefRe($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_ref_re !== $v) {
			$this->dt_ref_re = $v;
			$this->modifiedColumns[] = ReingressosPeer::DT_REF_RE;
		}

		return $this;
	} // setDtRefRe()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->dt_versao_orc = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_orgao = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->cd_unid_orc = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->cd_cpf_serv_muni = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->cd_ingresso = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->cd_vinculo = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->nu_expediente_no = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->nu_expediente_de = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->nu_expediente_re = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->cd_reingresso = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->cd_expediente = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->dt_expediente_re = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->dt_public_re = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->cd_amparo_legal = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->nu_amparo_legal_re = ($row[$startcol + 15] !== null) ? (string) $row[$startcol + 15] : null;
			$this->dt_amparo_legal_re = ($row[$startcol + 16] !== null) ? (string) $row[$startcol + 16] : null;
			$this->dt_public_amparo_re = ($row[$startcol + 17] !== null) ? (string) $row[$startcol + 17] : null;
			$this->dt_ref_re = ($row[$startcol + 18] !== null) ? (int) $row[$startcol + 18] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 19; // 19 = ReingressosPeer::NUM_COLUMNS - ReingressosPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Reingressos object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aDesligamentos !== null && $this->cd_municipio !== $this->aDesligamentos->getCdMunicipio()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->dt_versao_orc !== $this->aDesligamentos->getDtVersaoOrc()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->cd_orgao !== $this->aDesligamentos->getCdOrgao()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->cd_unid_orc !== $this->aDesligamentos->getCdUnidOrc()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->cd_ingresso !== $this->aDesligamentos->getCdIngresso()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->cd_vinculo !== $this->aDesligamentos->getCdVinculo()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->nu_expediente_no !== $this->aDesligamentos->getNuExpedienteNo()) {
			$this->aDesligamentos = null;
		}
		if ($this->aDesligamentos !== null && $this->nu_expediente_de !== $this->aDesligamentos->getNuExpedienteDe()) {
			$this->aDesligamentos = null;
		}
		if ($this->aTiposReingressos !== null && $this->cd_reingresso !== $this->aTiposReingressos->getCdReingresso()) {
			$this->aTiposReingressos = null;
		}
		if ($this->aTiposExpedientes !== null && $this->cd_expediente !== $this->aTiposExpedientes->getCdExpediente()) {
			$this->aTiposExpedientes = null;
		}
		if ($this->aTiposAmparosLegais !== null && $this->cd_amparo_legal !== $this->aTiposAmparosLegais->getCdAmparoLegal()) {
			$this->aTiposAmparosLegais = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ReingressosPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ReingressosPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aTiposReingressos = null;
			$this->aTiposExpedientes = null;
			$this->aTiposAmparosLegais = null;
			$this->aDesligamentos = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ReingressosPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseReingressos:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				ReingressosPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseReingressos:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ReingressosPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseReingressos:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseReingressos:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				ReingressosPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTiposReingressos !== null) {
				if ($this->aTiposReingressos->isModified() || $this->aTiposReingressos->isNew()) {
					$affectedRows += $this->aTiposReingressos->save($con);
				}
				$this->setTiposReingressos($this->aTiposReingressos);
			}

			if ($this->aTiposExpedientes !== null) {
				if ($this->aTiposExpedientes->isModified() || $this->aTiposExpedientes->isNew()) {
					$affectedRows += $this->aTiposExpedientes->save($con);
				}
				$this->setTiposExpedientes($this->aTiposExpedientes);
			}

			if ($this->aTiposAmparosLegais !== null) {
				if ($this->aTiposAmparosLegais->isModified() || $this->aTiposAmparosLegais->isNew()) {
					$affectedRows += $this->aTiposAmparosLegais->save($con);
				}
				$this->setTiposAmparosLegais($this->aTiposAmparosLegais);
			}

			if ($this->aDesligamentos !== null) {
				if ($this->aDesligamentos->isModified() || $this->aDesligamentos->isNew()) {
					$affectedRows += $this->aDesligamentos->save($con);
				}
				$this->setDesligamentos($this->aDesligamentos);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ReingressosPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += ReingressosPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTiposReingressos !== null) {
				if (!$this->aTiposReingressos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTiposReingressos->getValidationFailures());
				}
			}

			if ($this->aTiposExpedientes !== null) {
				if (!$this->aTiposExpedientes->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTiposExpedientes->getValidationFailures());
				}
			}

			if ($this->aTiposAmparosLegais !== null) {
				if (!$this->aTiposAmparosLegais->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTiposAmparosLegais->getValidationFailures());
				}
			}

			if ($this->aDesligamentos !== null) {
				if (!$this->aDesligamentos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDesligamentos->getValidationFailures());
				}
			}


			if (($retval = ReingressosPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ReingressosPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getDtVersaoOrc();
				break;
			case 2:
				return $this->getCdOrgao();
				break;
			case 3:
				return $this->getCdUnidOrc();
				break;
			case 4:
				return $this->getCdCpfServMuni();
				break;
			case 5:
				return $this->getCdIngresso();
				break;
			case 6:
				return $this->getCdVinculo();
				break;
			case 7:
				return $this->getNuExpedienteNo();
				break;
			case 8:
				return $this->getNuExpedienteDe();
				break;
			case 9:
				return $this->getNuExpedienteRe();
				break;
			case 10:
				return $this->getCdReingresso();
				break;
			case 11:
				return $this->getCdExpediente();
				break;
			case 12:
				return $this->getDtExpedienteRe();
				break;
			case 13:
				return $this->getDtPublicRe();
				break;
			case 14:
				return $this->getCdAmparoLegal();
				break;
			case 15:
				return $this->getNuAmparoLegalRe();
				break;
			case 16:
				return $this->getDtAmparoLegalRe();
				break;
			case 17:
				return $this->getDtPublicAmparoRe();
				break;
			case 18:
				return $this->getDtRefRe();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ReingressosPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getDtVersaoOrc(),
			$keys[2] => $this->getCdOrgao(),
			$keys[3] => $this->getCdUnidOrc(),
			$keys[4] => $this->getCdCpfServMuni(),
			$keys[5] => $this->getCdIngresso(),
			$keys[6] => $this->getCdVinculo(),
			$keys[7] => $this->getNuExpedienteNo(),
			$keys[8] => $this->getNuExpedienteDe(),
			$keys[9] => $this->getNuExpedienteRe(),
			$keys[10] => $this->getCdReingresso(),
			$keys[11] => $this->getCdExpediente(),
			$keys[12] => $this->getDtExpedienteRe(),
			$keys[13] => $this->getDtPublicRe(),
			$keys[14] => $this->getCdAmparoLegal(),
			$keys[15] => $this->getNuAmparoLegalRe(),
			$keys[16] => $this->getDtAmparoLegalRe(),
			$keys[17] => $this->getDtPublicAmparoRe(),
			$keys[18] => $this->getDtRefRe(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ReingressosPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setDtVersaoOrc($value);
				break;
			case 2:
				$this->setCdOrgao($value);
				break;
			case 3:
				$this->setCdUnidOrc($value);
				break;
			case 4:
				$this->setCdCpfServMuni($value);
				break;
			case 5:
				$this->setCdIngresso($value);
				break;
			case 6:
				$this->setCdVinculo($value);
				break;
			case 7:
				$this->setNuExpedienteNo($value);
				break;
			case 8:
				$this->setNuExpedienteDe($value);
				break;
			case 9:
				$this->setNuExpedienteRe($value);
				break;
			case 10:
				$this->setCdReingresso($value);
				break;
			case 11:
				$this->setCdExpediente($value);
				break;
			case 12:
				$this->setDtExpedienteRe($value);
				break;
			case 13:
				$this->setDtPublicRe($value);
				break;
			case 14:
				$this->setCdAmparoLegal($value);
				break;
			case 15:
				$this->setNuAmparoLegalRe($value);
				break;
			case 16:
				$this->setDtAmparoLegalRe($value);
				break;
			case 17:
				$this->setDtPublicAmparoRe($value);
				break;
			case 18:
				$this->setDtRefRe($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ReingressosPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDtVersaoOrc($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdOrgao($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCdUnidOrc($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCdCpfServMuni($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCdIngresso($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCdVinculo($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setNuExpedienteNo($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setNuExpedienteDe($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setNuExpedienteRe($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setCdReingresso($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setCdExpediente($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setDtExpedienteRe($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setDtPublicRe($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setCdAmparoLegal($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setNuAmparoLegalRe($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setDtAmparoLegalRe($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setDtPublicAmparoRe($arr[$keys[17]]);
		if (array_key_exists($keys[18], $arr)) $this->setDtRefRe($arr[$keys[18]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ReingressosPeer::DATABASE_NAME);

		if ($this->isColumnModified(ReingressosPeer::CD_MUNICIPIO)) $criteria->add(ReingressosPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(ReingressosPeer::DT_VERSAO_ORC)) $criteria->add(ReingressosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		if ($this->isColumnModified(ReingressosPeer::CD_ORGAO)) $criteria->add(ReingressosPeer::CD_ORGAO, $this->cd_orgao);
		if ($this->isColumnModified(ReingressosPeer::CD_UNID_ORC)) $criteria->add(ReingressosPeer::CD_UNID_ORC, $this->cd_unid_orc);
		if ($this->isColumnModified(ReingressosPeer::CD_CPF_SERV_MUNI)) $criteria->add(ReingressosPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		if ($this->isColumnModified(ReingressosPeer::CD_INGRESSO)) $criteria->add(ReingressosPeer::CD_INGRESSO, $this->cd_ingresso);
		if ($this->isColumnModified(ReingressosPeer::CD_VINCULO)) $criteria->add(ReingressosPeer::CD_VINCULO, $this->cd_vinculo);
		if ($this->isColumnModified(ReingressosPeer::NU_EXPEDIENTE_NO)) $criteria->add(ReingressosPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		if ($this->isColumnModified(ReingressosPeer::NU_EXPEDIENTE_DE)) $criteria->add(ReingressosPeer::NU_EXPEDIENTE_DE, $this->nu_expediente_de);
		if ($this->isColumnModified(ReingressosPeer::NU_EXPEDIENTE_RE)) $criteria->add(ReingressosPeer::NU_EXPEDIENTE_RE, $this->nu_expediente_re);
		if ($this->isColumnModified(ReingressosPeer::CD_REINGRESSO)) $criteria->add(ReingressosPeer::CD_REINGRESSO, $this->cd_reingresso);
		if ($this->isColumnModified(ReingressosPeer::CD_EXPEDIENTE)) $criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);
		if ($this->isColumnModified(ReingressosPeer::DT_EXPEDIENTE_RE)) $criteria->add(ReingressosPeer::DT_EXPEDIENTE_RE, $this->dt_expediente_re);
		if ($this->isColumnModified(ReingressosPeer::DT_PUBLIC_RE)) $criteria->add(ReingressosPeer::DT_PUBLIC_RE, $this->dt_public_re);
		if ($this->isColumnModified(ReingressosPeer::CD_AMPARO_LEGAL)) $criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);
		if ($this->isColumnModified(ReingressosPeer::NU_AMPARO_LEGAL_RE)) $criteria->add(ReingressosPeer::NU_AMPARO_LEGAL_RE, $this->nu_amparo_legal_re);
		if ($this->isColumnModified(ReingressosPeer::DT_AMPARO_LEGAL_RE)) $criteria->add(ReingressosPeer::DT_AMPARO_LEGAL_RE, $this->dt_amparo_legal_re);
		if ($this->isColumnModified(ReingressosPeer::DT_PUBLIC_AMPARO_RE)) $criteria->add(ReingressosPeer::DT_PUBLIC_AMPARO_RE, $this->dt_public_amparo_re);
		if ($this->isColumnModified(ReingressosPeer::DT_REF_RE)) $criteria->add(ReingressosPeer::DT_REF_RE, $this->dt_ref_re);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ReingressosPeer::DATABASE_NAME);

		$criteria->add(ReingressosPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(ReingressosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		$criteria->add(ReingressosPeer::CD_ORGAO, $this->cd_orgao);
		$criteria->add(ReingressosPeer::CD_UNID_ORC, $this->cd_unid_orc);
		$criteria->add(ReingressosPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		$criteria->add(ReingressosPeer::CD_INGRESSO, $this->cd_ingresso);
		$criteria->add(ReingressosPeer::CD_VINCULO, $this->cd_vinculo);
		$criteria->add(ReingressosPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		$criteria->add(ReingressosPeer::NU_EXPEDIENTE_DE, $this->nu_expediente_de);
		$criteria->add(ReingressosPeer::NU_EXPEDIENTE_RE, $this->nu_expediente_re);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getDtVersaoOrc();

		$pks[2] = $this->getCdOrgao();

		$pks[3] = $this->getCdUnidOrc();

		$pks[4] = $this->getCdCpfServMuni();

		$pks[5] = $this->getCdIngresso();

		$pks[6] = $this->getCdVinculo();

		$pks[7] = $this->getNuExpedienteNo();

		$pks[8] = $this->getNuExpedienteDe();

		$pks[9] = $this->getNuExpedienteRe();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setDtVersaoOrc($keys[1]);

		$this->setCdOrgao($keys[2]);

		$this->setCdUnidOrc($keys[3]);

		$this->setCdCpfServMuni($keys[4]);

		$this->setCdIngresso($keys[5]);

		$this->setCdVinculo($keys[6]);

		$this->setNuExpedienteNo($keys[7]);

		$this->setNuExpedienteDe($keys[8]);

		$this->setNuExpedienteRe($keys[9]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Reingressos (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setDtVersaoOrc($this->dt_versao_orc);

		$copyObj->setCdOrgao($this->cd_orgao);

		$copyObj->setCdUnidOrc($this->cd_unid_orc);

		$copyObj->setCdCpfServMuni($this->cd_cpf_serv_muni);

		$copyObj->setCdIngresso($this->cd_ingresso);

		$copyObj->setCdVinculo($this->cd_vinculo);

		$copyObj->setNuExpedienteNo($this->nu_expediente_no);

		$copyObj->setNuExpedienteDe($this->nu_expediente_de);

		$copyObj->setNuExpedienteRe($this->nu_expediente_re);

		$copyObj->setCdReingresso($this->cd_reingresso);

		$copyObj->setCdExpediente($this->cd_expediente);

		$copyObj->setDtExpedienteRe($this->dt_expediente_re);

		$copyObj->setDtPublicRe($this->dt_public_re);

		$copyObj->setCdAmparoLegal($this->cd_amparo_legal);

		$copyObj->setNuAmparoLegalRe($this->nu_amparo_legal_re);

		$copyObj->setDtAmparoLegalRe($this->dt_amparo_legal_re);

		$copyObj->setDtPublicAmparoRe($this->dt_public_amparo_re);

		$copyObj->setDtRefRe($this->dt_ref_re);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Reingressos Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ReingressosPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ReingressosPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a TiposReingressos object.
	 *
	 * @param      TiposReingressos $v
	 * @return     Reingressos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTiposReingressos(TiposReingressos $v = null)
	{
		if ($v === null) {
			$this->setCdReingresso(NULL);
		} else {
			$this->setCdReingresso($v->getCdReingresso());
		}

		$this->aTiposReingressos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TiposReingressos object, it will not be re-added.
		if ($v !== null) {
			$v->addReingressos($this);
		}

		return $this;
	}


	/**
	 * Get the associated TiposReingressos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TiposReingressos The associated TiposReingressos object.
	 * @throws     PropelException
	 */
	public function getTiposReingressos(PropelPDO $con = null)
	{
		if ($this->aTiposReingressos === null && (($this->cd_reingresso !== "" && $this->cd_reingresso !== null))) {
			$this->aTiposReingressos = TiposReingressosPeer::retrieveByPk($this->cd_reingresso);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTiposReingressos->addReingressoss($this);
			 */
		}
		return $this->aTiposReingressos;
	}

	/**
	 * Declares an association between this object and a TiposExpedientes object.
	 *
	 * @param      TiposExpedientes $v
	 * @return     Reingressos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTiposExpedientes(TiposExpedientes $v = null)
	{
		if ($v === null) {
			$this->setCdExpediente(NULL);
		} else {
			$this->setCdExpediente($v->getCdExpediente());
		}

		$this->aTiposExpedientes = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TiposExpedientes object, it will not be re-added.
		if ($v !== null) {
			$v->addReingressos($this);
		}

		return $this;
	}


	/**
	 * Get the associated TiposExpedientes object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TiposExpedientes The associated TiposExpedientes object.
	 * @throws     PropelException
	 */
	public function getTiposExpedientes(PropelPDO $con = null)
	{
		if ($this->aTiposExpedientes === null && (($this->cd_expediente !== "" && $this->cd_expediente !== null))) {
			$this->aTiposExpedientes = TiposExpedientesPeer::retrieveByPk($this->cd_expediente);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTiposExpedientes->addReingressoss($this);
			 */
		}
		return $this->aTiposExpedientes;
	}

	/**
	 * Declares an association between this object and a TiposAmparosLegais object.
	 *
	 * @param      TiposAmparosLegais $v
	 * @return     Reingressos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTiposAmparosLegais(TiposAmparosLegais $v = null)
	{
		if ($v === null) {
			$this->setCdAmparoLegal(NULL);
		} else {
			$this->setCdAmparoLegal($v->getCdAmparoLegal());
		}

		$this->aTiposAmparosLegais = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TiposAmparosLegais object, it will not be re-added.
		if ($v !== null) {
			$v->addReingressos($this);
		}

		return $this;
	}


	/**
	 * Get the associated TiposAmparosLegais object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TiposAmparosLegais The associated TiposAmparosLegais object.
	 * @throws     PropelException
	 */
	public function getTiposAmparosLegais(PropelPDO $con = null)
	{
		if ($this->aTiposAmparosLegais === null && (($this->cd_amparo_legal !== "" && $this->cd_amparo_legal !== null))) {
			$this->aTiposAmparosLegais = TiposAmparosLegaisPeer::retrieveByPk($this->cd_amparo_legal);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTiposAmparosLegais->addReingressoss($this);
			 */
		}
		return $this->aTiposAmparosLegais;
	}

	/**
	 * Declares an association between this object and a Desligamentos object.
	 *
	 * @param      Desligamentos $v
	 * @return     Reingressos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDesligamentos(Desligamentos $v = null)
	{
		if ($v === null) {
			$this->setCdIngresso(NULL);
		} else {
			$this->setCdIngresso($v->getCdIngresso());
		}

		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setCdVinculo(NULL);
		} else {
			$this->setCdVinculo($v->getCdVinculo());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		if ($v === null) {
			$this->setNuExpedienteDe(NULL);
		} else {
			$this->setNuExpedienteDe($v->getNuExpedienteDe());
		}

		if ($v === null) {
			$this->setNuExpedienteNo(NULL);
		} else {
			$this->setNuExpedienteNo($v->getNuExpedienteNo());
		}

		$this->aDesligamentos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Desligamentos object, it will not be re-added.
		if ($v !== null) {
			$v->addReingressos($this);
		}

		return $this;
	}


	/**
	 * Get the associated Desligamentos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Desligamentos The associated Desligamentos object.
	 * @throws     PropelException
	 */
	public function getDesligamentos(PropelPDO $con = null)
	{
		if ($this->aDesligamentos === null && (($this->cd_ingresso !== "" && $this->cd_ingresso !== null) && ($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->cd_orgao !== "" && $this->cd_orgao !== null) && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && ($this->cd_vinculo !== "" && $this->cd_vinculo !== null) && $this->dt_versao_orc !== null && ($this->nu_expediente_de !== "" && $this->nu_expediente_de !== null) && ($this->nu_expediente_no !== "" && $this->nu_expediente_no !== null))) {
			$c = new Criteria(DesligamentosPeer::DATABASE_NAME);
			$c->add(DesligamentosPeer::CD_INGRESSO, $this->cd_ingresso);
			$c->add(DesligamentosPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(DesligamentosPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(DesligamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(DesligamentosPeer::CD_VINCULO, $this->cd_vinculo);
			$c->add(DesligamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$c->add(DesligamentosPeer::NU_EXPEDIENTE_DE, $this->nu_expediente_de);
			$c->add(DesligamentosPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
			$this->aDesligamentos = DesligamentosPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDesligamentos->addReingressoss($this);
			 */
		}
		return $this->aDesligamentos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aTiposReingressos = null;
			$this->aTiposExpedientes = null;
			$this->aTiposAmparosLegais = null;
			$this->aDesligamentos = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseReingressos:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseReingressos::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseReingressos
