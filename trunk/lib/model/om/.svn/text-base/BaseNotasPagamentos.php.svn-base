<?php

/**
 * Base class that represents a row from the 'NOTAS_PAGAMENTOS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:23:03 2010
 *
 * @package    lib.model.om
 */
abstract class BaseNotasPagamentos extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        NotasPagamentosPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the dt_versao_orc field.
	 * @var        int
	 */
	protected $dt_versao_orc;

	/**
	 * The value for the cd_orgao field.
	 * @var        string
	 */
	protected $cd_orgao;

	/**
	 * The value for the cd_unid_orc field.
	 * @var        string
	 */
	protected $cd_unid_orc;

	/**
	 * The value for the dt_emissao_ne field.
	 * @var        string
	 */
	protected $dt_emissao_ne;

	/**
	 * The value for the nu_nota_empenho field.
	 * @var        string
	 */
	protected $nu_nota_empenho;

	/**
	 * The value for the nu_sub_empenho_np field.
	 * @var        string
	 */
	protected $nu_sub_empenho_np;

	/**
	 * The value for the nu_nota_np field.
	 * @var        string
	 */
	protected $nu_nota_np;

	/**
	 * The value for the dt_ref_np field.
	 * @var        int
	 */
	protected $dt_ref_np;

	/**
	 * The value for the nu_doc_caixa_np field.
	 * @var        string
	 */
	protected $nu_doc_caixa_np;

	/**
	 * The value for the dt_nota_np field.
	 * @var        string
	 */
	protected $dt_nota_np;

	/**
	 * The value for the vl_nota_np field.
	 * @var        string
	 */
	protected $vl_nota_np;

	/**
	 * The value for the vl_emp_a_pagar_np field.
	 * @var        string
	 */
	protected $vl_emp_a_pagar_np;

	/**
	 * The value for the st_estornado_np field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $st_estornado_np;

	/**
	 * The value for the cd_cpf_resp_pag_np field.
	 * @var        string
	 */
	protected $cd_cpf_resp_pag_np;

	/**
	 * The value for the nm_resp_pag_np field.
	 * @var        string
	 */
	protected $nm_resp_pag_np;

	/**
	 * @var        DadosPessoais
	 */
	protected $aDadosPessoais;

	/**
	 * @var        NotasEmpenhos
	 */
	protected $aNotasEmpenhos;

	/**
	 * @var        array ChequesNotasPagamentos[] Collection to store aggregation of ChequesNotasPagamentos objects.
	 */
	protected $collChequesNotasPagamentoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collChequesNotasPagamentoss.
	 */
	private $lastChequesNotasPagamentosCriteria = null;

	/**
	 * @var        array DeducoesNotasPag[] Collection to store aggregation of DeducoesNotasPag objects.
	 */
	protected $collDeducoesNotasPags;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDeducoesNotasPags.
	 */
	private $lastDeducoesNotasPagCriteria = null;

	/**
	 * @var        array DeducoesNotasPagas[] Collection to store aggregation of DeducoesNotasPagas objects.
	 */
	protected $collDeducoesNotasPagass;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDeducoesNotasPagass.
	 */
	private $lastDeducoesNotasPagasCriteria = null;

	/**
	 * @var        array EstornosPagamentos[] Collection to store aggregation of EstornosPagamentos objects.
	 */
	protected $collEstornosPagamentoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collEstornosPagamentoss.
	 */
	private $lastEstornosPagamentosCriteria = null;

	/**
	 * @var        array PagamentosFolhas[] Collection to store aggregation of PagamentosFolhas objects.
	 */
	protected $collPagamentosFolhass;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPagamentosFolhass.
	 */
	private $lastPagamentosFolhasCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'NotasPagamentosPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->st_estornado_np = 0;
	}

	/**
	 * Initializes internal state of BaseNotasPagamentos object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [dt_versao_orc] column value.
	 * 
	 * @return     int
	 */
	public function getDtVersaoOrc()
	{
		return $this->dt_versao_orc;
	}

	/**
	 * Get the [cd_orgao] column value.
	 * 
	 * @return     string
	 */
	public function getCdOrgao()
	{
		return $this->cd_orgao;
	}

	/**
	 * Get the [cd_unid_orc] column value.
	 * 
	 * @return     string
	 */
	public function getCdUnidOrc()
	{
		return $this->cd_unid_orc;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_emissao_ne] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtEmissaoNe($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_emissao_ne === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_emissao_ne);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_emissao_ne, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [nu_nota_empenho] column value.
	 * 
	 * @return     string
	 */
	public function getNuNotaEmpenho()
	{
		return $this->nu_nota_empenho;
	}

	/**
	 * Get the [nu_sub_empenho_np] column value.
	 * 
	 * @return     string
	 */
	public function getNuSubEmpenhoNp()
	{
		return $this->nu_sub_empenho_np;
	}

	/**
	 * Get the [nu_nota_np] column value.
	 * 
	 * @return     string
	 */
	public function getNuNotaNp()
	{
		return $this->nu_nota_np;
	}

	/**
	 * Get the [dt_ref_np] column value.
	 * 
	 * @return     int
	 */
	public function getDtRefNp()
	{
		return $this->dt_ref_np;
	}

	/**
	 * Get the [nu_doc_caixa_np] column value.
	 * 
	 * @return     string
	 */
	public function getNuDocCaixaNp()
	{
		return $this->nu_doc_caixa_np;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_nota_np] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtNotaNp($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_nota_np === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_nota_np);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_nota_np, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [vl_nota_np] column value.
	 * 
	 * @return     string
	 */
	public function getVlNotaNp()
	{
		return $this->vl_nota_np;
	}

	/**
	 * Get the [vl_emp_a_pagar_np] column value.
	 * 
	 * @return     string
	 */
	public function getVlEmpAPagarNp()
	{
		return $this->vl_emp_a_pagar_np;
	}

	/**
	 * Get the [st_estornado_np] column value.
	 * 
	 * @return     int
	 */
	public function getStEstornadoNp()
	{
		return $this->st_estornado_np;
	}

	/**
	 * Get the [cd_cpf_resp_pag_np] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfRespPagNp()
	{
		return $this->cd_cpf_resp_pag_np;
	}

	/**
	 * Get the [nm_resp_pag_np] column value.
	 * 
	 * @return     string
	 */
	public function getNmRespPagNp()
	{
		return $this->nm_resp_pag_np;
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::CD_MUNICIPIO;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [dt_versao_orc] column.
	 * 
	 * @param      int $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setDtVersaoOrc($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_versao_orc !== $v) {
			$this->dt_versao_orc = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::DT_VERSAO_ORC;
		}

		if ($this->aNotasEmpenhos !== null && $this->aNotasEmpenhos->getDtVersaoOrc() !== $v) {
			$this->aNotasEmpenhos = null;
		}

		return $this;
	} // setDtVersaoOrc()

	/**
	 * Set the value of [cd_orgao] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setCdOrgao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_orgao !== $v) {
			$this->cd_orgao = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::CD_ORGAO;
		}

		if ($this->aNotasEmpenhos !== null && $this->aNotasEmpenhos->getCdOrgao() !== $v) {
			$this->aNotasEmpenhos = null;
		}

		return $this;
	} // setCdOrgao()

	/**
	 * Set the value of [cd_unid_orc] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setCdUnidOrc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_unid_orc !== $v) {
			$this->cd_unid_orc = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::CD_UNID_ORC;
		}

		if ($this->aNotasEmpenhos !== null && $this->aNotasEmpenhos->getCdUnidOrc() !== $v) {
			$this->aNotasEmpenhos = null;
		}

		return $this;
	} // setCdUnidOrc()

	/**
	 * Sets the value of [dt_emissao_ne] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setDtEmissaoNe($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_emissao_ne !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_emissao_ne !== null && $tmpDt = new MssqlDateTime($this->dt_emissao_ne)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_emissao_ne = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = NotasPagamentosPeer::DT_EMISSAO_NE;
			}
		} // if either are not null

		if ($this->aNotasEmpenhos !== null && $this->aNotasEmpenhos->getDtEmissaoNe() !== $v) {
			$this->aNotasEmpenhos = null;
		}

		return $this;
	} // setDtEmissaoNe()

	/**
	 * Set the value of [nu_nota_empenho] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setNuNotaEmpenho($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_nota_empenho !== $v) {
			$this->nu_nota_empenho = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::NU_NOTA_EMPENHO;
		}

		if ($this->aNotasEmpenhos !== null && $this->aNotasEmpenhos->getNuNotaEmpenho() !== $v) {
			$this->aNotasEmpenhos = null;
		}

		return $this;
	} // setNuNotaEmpenho()

	/**
	 * Set the value of [nu_sub_empenho_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setNuSubEmpenhoNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_sub_empenho_np !== $v) {
			$this->nu_sub_empenho_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::NU_SUB_EMPENHO_NP;
		}

		return $this;
	} // setNuSubEmpenhoNp()

	/**
	 * Set the value of [nu_nota_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setNuNotaNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_nota_np !== $v) {
			$this->nu_nota_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::NU_NOTA_NP;
		}

		return $this;
	} // setNuNotaNp()

	/**
	 * Set the value of [dt_ref_np] column.
	 * 
	 * @param      int $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setDtRefNp($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_ref_np !== $v) {
			$this->dt_ref_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::DT_REF_NP;
		}

		return $this;
	} // setDtRefNp()

	/**
	 * Set the value of [nu_doc_caixa_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setNuDocCaixaNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_doc_caixa_np !== $v) {
			$this->nu_doc_caixa_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::NU_DOC_CAIXA_NP;
		}

		return $this;
	} // setNuDocCaixaNp()

	/**
	 * Sets the value of [dt_nota_np] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setDtNotaNp($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_nota_np !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_nota_np !== null && $tmpDt = new MssqlDateTime($this->dt_nota_np)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_nota_np = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = NotasPagamentosPeer::DT_NOTA_NP;
			}
		} // if either are not null

		return $this;
	} // setDtNotaNp()

	/**
	 * Set the value of [vl_nota_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setVlNotaNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->vl_nota_np !== $v) {
			$this->vl_nota_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::VL_NOTA_NP;
		}

		return $this;
	} // setVlNotaNp()

	/**
	 * Set the value of [vl_emp_a_pagar_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setVlEmpAPagarNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->vl_emp_a_pagar_np !== $v) {
			$this->vl_emp_a_pagar_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::VL_EMP_A_PAGAR_NP;
		}

		return $this;
	} // setVlEmpAPagarNp()

	/**
	 * Set the value of [st_estornado_np] column.
	 * 
	 * @param      int $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setStEstornadoNp($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->st_estornado_np !== $v || $this->isNew()) {
			$this->st_estornado_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::ST_ESTORNADO_NP;
		}

		return $this;
	} // setStEstornadoNp()

	/**
	 * Set the value of [cd_cpf_resp_pag_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setCdCpfRespPagNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_resp_pag_np !== $v) {
			$this->cd_cpf_resp_pag_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::CD_CPF_RESP_PAG_NP;
		}

		if ($this->aDadosPessoais !== null && $this->aDadosPessoais->getCdCpfServMuni() !== $v) {
			$this->aDadosPessoais = null;
		}

		return $this;
	} // setCdCpfRespPagNp()

	/**
	 * Set the value of [nm_resp_pag_np] column.
	 * 
	 * @param      string $v new value
	 * @return     NotasPagamentos The current object (for fluent API support)
	 */
	public function setNmRespPagNp($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nm_resp_pag_np !== $v) {
			$this->nm_resp_pag_np = $v;
			$this->modifiedColumns[] = NotasPagamentosPeer::NM_RESP_PAG_NP;
		}

		return $this;
	} // setNmRespPagNp()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->st_estornado_np !== 0) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->dt_versao_orc = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_orgao = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->cd_unid_orc = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->dt_emissao_ne = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->nu_nota_empenho = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->nu_sub_empenho_np = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->nu_nota_np = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->dt_ref_np = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->nu_doc_caixa_np = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->dt_nota_np = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->vl_nota_np = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->vl_emp_a_pagar_np = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->st_estornado_np = ($row[$startcol + 13] !== null) ? (int) $row[$startcol + 13] : null;
			$this->cd_cpf_resp_pag_np = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->nm_resp_pag_np = ($row[$startcol + 15] !== null) ? (string) $row[$startcol + 15] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 16; // 16 = NotasPagamentosPeer::NUM_COLUMNS - NotasPagamentosPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating NotasPagamentos object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aNotasEmpenhos !== null && $this->dt_versao_orc !== $this->aNotasEmpenhos->getDtVersaoOrc()) {
			$this->aNotasEmpenhos = null;
		}
		if ($this->aNotasEmpenhos !== null && $this->cd_orgao !== $this->aNotasEmpenhos->getCdOrgao()) {
			$this->aNotasEmpenhos = null;
		}
		if ($this->aNotasEmpenhos !== null && $this->cd_unid_orc !== $this->aNotasEmpenhos->getCdUnidOrc()) {
			$this->aNotasEmpenhos = null;
		}
		if ($this->aNotasEmpenhos !== null && $this->dt_emissao_ne !== $this->aNotasEmpenhos->getDtEmissaoNe()) {
			$this->aNotasEmpenhos = null;
		}
		if ($this->aNotasEmpenhos !== null && $this->nu_nota_empenho !== $this->aNotasEmpenhos->getNuNotaEmpenho()) {
			$this->aNotasEmpenhos = null;
		}
		if ($this->aDadosPessoais !== null && $this->cd_cpf_resp_pag_np !== $this->aDadosPessoais->getCdCpfServMuni()) {
			$this->aDadosPessoais = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(NotasPagamentosPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = NotasPagamentosPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aDadosPessoais = null;
			$this->aNotasEmpenhos = null;
			$this->collChequesNotasPagamentoss = null;
			$this->lastChequesNotasPagamentosCriteria = null;

			$this->collDeducoesNotasPags = null;
			$this->lastDeducoesNotasPagCriteria = null;

			$this->collDeducoesNotasPagass = null;
			$this->lastDeducoesNotasPagasCriteria = null;

			$this->collEstornosPagamentoss = null;
			$this->lastEstornosPagamentosCriteria = null;

			$this->collPagamentosFolhass = null;
			$this->lastPagamentosFolhasCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(NotasPagamentosPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseNotasPagamentos:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				NotasPagamentosPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseNotasPagamentos:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(NotasPagamentosPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseNotasPagamentos:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseNotasPagamentos:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				NotasPagamentosPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDadosPessoais !== null) {
				if ($this->aDadosPessoais->isModified() || $this->aDadosPessoais->isNew()) {
					$affectedRows += $this->aDadosPessoais->save($con);
				}
				$this->setDadosPessoais($this->aDadosPessoais);
			}

			if ($this->aNotasEmpenhos !== null) {
				if ($this->aNotasEmpenhos->isModified() || $this->aNotasEmpenhos->isNew()) {
					$affectedRows += $this->aNotasEmpenhos->save($con);
				}
				$this->setNotasEmpenhos($this->aNotasEmpenhos);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = NotasPagamentosPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += NotasPagamentosPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collChequesNotasPagamentoss !== null) {
				foreach ($this->collChequesNotasPagamentoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDeducoesNotasPags !== null) {
				foreach ($this->collDeducoesNotasPags as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDeducoesNotasPagass !== null) {
				foreach ($this->collDeducoesNotasPagass as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collEstornosPagamentoss !== null) {
				foreach ($this->collEstornosPagamentoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPagamentosFolhass !== null) {
				foreach ($this->collPagamentosFolhass as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aDadosPessoais !== null) {
				if (!$this->aDadosPessoais->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDadosPessoais->getValidationFailures());
				}
			}

			if ($this->aNotasEmpenhos !== null) {
				if (!$this->aNotasEmpenhos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aNotasEmpenhos->getValidationFailures());
				}
			}


			if (($retval = NotasPagamentosPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collChequesNotasPagamentoss !== null) {
					foreach ($this->collChequesNotasPagamentoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDeducoesNotasPags !== null) {
					foreach ($this->collDeducoesNotasPags as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDeducoesNotasPagass !== null) {
					foreach ($this->collDeducoesNotasPagass as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collEstornosPagamentoss !== null) {
					foreach ($this->collEstornosPagamentoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPagamentosFolhass !== null) {
					foreach ($this->collPagamentosFolhass as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = NotasPagamentosPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getDtVersaoOrc();
				break;
			case 2:
				return $this->getCdOrgao();
				break;
			case 3:
				return $this->getCdUnidOrc();
				break;
			case 4:
				return $this->getDtEmissaoNe();
				break;
			case 5:
				return $this->getNuNotaEmpenho();
				break;
			case 6:
				return $this->getNuSubEmpenhoNp();
				break;
			case 7:
				return $this->getNuNotaNp();
				break;
			case 8:
				return $this->getDtRefNp();
				break;
			case 9:
				return $this->getNuDocCaixaNp();
				break;
			case 10:
				return $this->getDtNotaNp();
				break;
			case 11:
				return $this->getVlNotaNp();
				break;
			case 12:
				return $this->getVlEmpAPagarNp();
				break;
			case 13:
				return $this->getStEstornadoNp();
				break;
			case 14:
				return $this->getCdCpfRespPagNp();
				break;
			case 15:
				return $this->getNmRespPagNp();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = NotasPagamentosPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getDtVersaoOrc(),
			$keys[2] => $this->getCdOrgao(),
			$keys[3] => $this->getCdUnidOrc(),
			$keys[4] => $this->getDtEmissaoNe(),
			$keys[5] => $this->getNuNotaEmpenho(),
			$keys[6] => $this->getNuSubEmpenhoNp(),
			$keys[7] => $this->getNuNotaNp(),
			$keys[8] => $this->getDtRefNp(),
			$keys[9] => $this->getNuDocCaixaNp(),
			$keys[10] => $this->getDtNotaNp(),
			$keys[11] => $this->getVlNotaNp(),
			$keys[12] => $this->getVlEmpAPagarNp(),
			$keys[13] => $this->getStEstornadoNp(),
			$keys[14] => $this->getCdCpfRespPagNp(),
			$keys[15] => $this->getNmRespPagNp(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = NotasPagamentosPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setDtVersaoOrc($value);
				break;
			case 2:
				$this->setCdOrgao($value);
				break;
			case 3:
				$this->setCdUnidOrc($value);
				break;
			case 4:
				$this->setDtEmissaoNe($value);
				break;
			case 5:
				$this->setNuNotaEmpenho($value);
				break;
			case 6:
				$this->setNuSubEmpenhoNp($value);
				break;
			case 7:
				$this->setNuNotaNp($value);
				break;
			case 8:
				$this->setDtRefNp($value);
				break;
			case 9:
				$this->setNuDocCaixaNp($value);
				break;
			case 10:
				$this->setDtNotaNp($value);
				break;
			case 11:
				$this->setVlNotaNp($value);
				break;
			case 12:
				$this->setVlEmpAPagarNp($value);
				break;
			case 13:
				$this->setStEstornadoNp($value);
				break;
			case 14:
				$this->setCdCpfRespPagNp($value);
				break;
			case 15:
				$this->setNmRespPagNp($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = NotasPagamentosPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDtVersaoOrc($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdOrgao($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCdUnidOrc($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setDtEmissaoNe($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setNuNotaEmpenho($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setNuSubEmpenhoNp($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setNuNotaNp($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setDtRefNp($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setNuDocCaixaNp($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setDtNotaNp($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setVlNotaNp($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setVlEmpAPagarNp($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setStEstornadoNp($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setCdCpfRespPagNp($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setNmRespPagNp($arr[$keys[15]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);

		if ($this->isColumnModified(NotasPagamentosPeer::CD_MUNICIPIO)) $criteria->add(NotasPagamentosPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(NotasPagamentosPeer::DT_VERSAO_ORC)) $criteria->add(NotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		if ($this->isColumnModified(NotasPagamentosPeer::CD_ORGAO)) $criteria->add(NotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);
		if ($this->isColumnModified(NotasPagamentosPeer::CD_UNID_ORC)) $criteria->add(NotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);
		if ($this->isColumnModified(NotasPagamentosPeer::DT_EMISSAO_NE)) $criteria->add(NotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);
		if ($this->isColumnModified(NotasPagamentosPeer::NU_NOTA_EMPENHO)) $criteria->add(NotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);
		if ($this->isColumnModified(NotasPagamentosPeer::NU_SUB_EMPENHO_NP)) $criteria->add(NotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);
		if ($this->isColumnModified(NotasPagamentosPeer::NU_NOTA_NP)) $criteria->add(NotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);
		if ($this->isColumnModified(NotasPagamentosPeer::DT_REF_NP)) $criteria->add(NotasPagamentosPeer::DT_REF_NP, $this->dt_ref_np);
		if ($this->isColumnModified(NotasPagamentosPeer::NU_DOC_CAIXA_NP)) $criteria->add(NotasPagamentosPeer::NU_DOC_CAIXA_NP, $this->nu_doc_caixa_np);
		if ($this->isColumnModified(NotasPagamentosPeer::DT_NOTA_NP)) $criteria->add(NotasPagamentosPeer::DT_NOTA_NP, $this->dt_nota_np);
		if ($this->isColumnModified(NotasPagamentosPeer::VL_NOTA_NP)) $criteria->add(NotasPagamentosPeer::VL_NOTA_NP, $this->vl_nota_np);
		if ($this->isColumnModified(NotasPagamentosPeer::VL_EMP_A_PAGAR_NP)) $criteria->add(NotasPagamentosPeer::VL_EMP_A_PAGAR_NP, $this->vl_emp_a_pagar_np);
		if ($this->isColumnModified(NotasPagamentosPeer::ST_ESTORNADO_NP)) $criteria->add(NotasPagamentosPeer::ST_ESTORNADO_NP, $this->st_estornado_np);
		if ($this->isColumnModified(NotasPagamentosPeer::CD_CPF_RESP_PAG_NP)) $criteria->add(NotasPagamentosPeer::CD_CPF_RESP_PAG_NP, $this->cd_cpf_resp_pag_np);
		if ($this->isColumnModified(NotasPagamentosPeer::NM_RESP_PAG_NP)) $criteria->add(NotasPagamentosPeer::NM_RESP_PAG_NP, $this->nm_resp_pag_np);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);

		$criteria->add(NotasPagamentosPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(NotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		$criteria->add(NotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);
		$criteria->add(NotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);
		$criteria->add(NotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);
		$criteria->add(NotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);
		$criteria->add(NotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);
		$criteria->add(NotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getDtVersaoOrc();

		$pks[2] = $this->getCdOrgao();

		$pks[3] = $this->getCdUnidOrc();

		$pks[4] = $this->getDtEmissaoNe();

		$pks[5] = $this->getNuNotaEmpenho();

		$pks[6] = $this->getNuSubEmpenhoNp();

		$pks[7] = $this->getNuNotaNp();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setDtVersaoOrc($keys[1]);

		$this->setCdOrgao($keys[2]);

		$this->setCdUnidOrc($keys[3]);

		$this->setDtEmissaoNe($keys[4]);

		$this->setNuNotaEmpenho($keys[5]);

		$this->setNuSubEmpenhoNp($keys[6]);

		$this->setNuNotaNp($keys[7]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of NotasPagamentos (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setDtVersaoOrc($this->dt_versao_orc);

		$copyObj->setCdOrgao($this->cd_orgao);

		$copyObj->setCdUnidOrc($this->cd_unid_orc);

		$copyObj->setDtEmissaoNe($this->dt_emissao_ne);

		$copyObj->setNuNotaEmpenho($this->nu_nota_empenho);

		$copyObj->setNuSubEmpenhoNp($this->nu_sub_empenho_np);

		$copyObj->setNuNotaNp($this->nu_nota_np);

		$copyObj->setDtRefNp($this->dt_ref_np);

		$copyObj->setNuDocCaixaNp($this->nu_doc_caixa_np);

		$copyObj->setDtNotaNp($this->dt_nota_np);

		$copyObj->setVlNotaNp($this->vl_nota_np);

		$copyObj->setVlEmpAPagarNp($this->vl_emp_a_pagar_np);

		$copyObj->setStEstornadoNp($this->st_estornado_np);

		$copyObj->setCdCpfRespPagNp($this->cd_cpf_resp_pag_np);

		$copyObj->setNmRespPagNp($this->nm_resp_pag_np);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getChequesNotasPagamentoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addChequesNotasPagamentos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDeducoesNotasPags() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDeducoesNotasPag($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDeducoesNotasPagass() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDeducoesNotasPagas($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getEstornosPagamentoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addEstornosPagamentos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPagamentosFolhass() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPagamentosFolhas($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     NotasPagamentos Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     NotasPagamentosPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new NotasPagamentosPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a DadosPessoais object.
	 *
	 * @param      DadosPessoais $v
	 * @return     NotasPagamentos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDadosPessoais(DadosPessoais $v = null)
	{
		if ($v === null) {
			$this->setCdCpfRespPagNp(NULL);
		} else {
			$this->setCdCpfRespPagNp($v->getCdCpfServMuni());
		}

		$this->aDadosPessoais = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the DadosPessoais object, it will not be re-added.
		if ($v !== null) {
			$v->addNotasPagamentos($this);
		}

		return $this;
	}


	/**
	 * Get the associated DadosPessoais object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     DadosPessoais The associated DadosPessoais object.
	 * @throws     PropelException
	 */
	public function getDadosPessoais(PropelPDO $con = null)
	{
		if ($this->aDadosPessoais === null && (($this->cd_cpf_resp_pag_np !== "" && $this->cd_cpf_resp_pag_np !== null))) {
			$this->aDadosPessoais = DadosPessoaisPeer::retrieveByPk($this->cd_cpf_resp_pag_np);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDadosPessoais->addNotasPagamentoss($this);
			 */
		}
		return $this->aDadosPessoais;
	}

	/**
	 * Declares an association between this object and a NotasEmpenhos object.
	 *
	 * @param      NotasEmpenhos $v
	 * @return     NotasPagamentos The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setNotasEmpenhos(NotasEmpenhos $v = null)
	{
		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setDtEmissaoNe(NULL);
		} else {
			$this->setDtEmissaoNe($v->getDtEmissaoNe());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		if ($v === null) {
			$this->setNuNotaEmpenho(NULL);
		} else {
			$this->setNuNotaEmpenho($v->getNuNotaEmpenho());
		}

		$this->aNotasEmpenhos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the NotasEmpenhos object, it will not be re-added.
		if ($v !== null) {
			$v->addNotasPagamentos($this);
		}

		return $this;
	}


	/**
	 * Get the associated NotasEmpenhos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     NotasEmpenhos The associated NotasEmpenhos object.
	 * @throws     PropelException
	 */
	public function getNotasEmpenhos(PropelPDO $con = null)
	{
		if ($this->aNotasEmpenhos === null && (($this->cd_orgao !== "" && $this->cd_orgao !== null) && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && ($this->dt_emissao_ne !== "" && $this->dt_emissao_ne !== null) && $this->dt_versao_orc !== null && ($this->nu_nota_empenho !== "" && $this->nu_nota_empenho !== null))) {
			$c = new Criteria(NotasEmpenhosPeer::DATABASE_NAME);
			$c->add(NotasEmpenhosPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(NotasEmpenhosPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(NotasEmpenhosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);
			$c->add(NotasEmpenhosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$c->add(NotasEmpenhosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);
			$this->aNotasEmpenhos = NotasEmpenhosPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aNotasEmpenhos->addNotasPagamentoss($this);
			 */
		}
		return $this->aNotasEmpenhos;
	}

	/**
	 * Clears out the collChequesNotasPagamentoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addChequesNotasPagamentoss()
	 */
	public function clearChequesNotasPagamentoss()
	{
		$this->collChequesNotasPagamentoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collChequesNotasPagamentoss collection (array).
	 *
	 * By default this just sets the collChequesNotasPagamentoss collection to an empty array (like clearcollChequesNotasPagamentoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initChequesNotasPagamentoss()
	{
		$this->collChequesNotasPagamentoss = array();
	}

	/**
	 * Gets an array of ChequesNotasPagamentos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos has previously been saved, it will retrieve
	 * related ChequesNotasPagamentoss from storage. If this NotasPagamentos is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ChequesNotasPagamentos[]
	 * @throws     PropelException
	 */
	public function getChequesNotasPagamentoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collChequesNotasPagamentoss === null) {
			if ($this->isNew()) {
			   $this->collChequesNotasPagamentoss = array();
			} else {

				$criteria->add(ChequesNotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(ChequesNotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(ChequesNotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(ChequesNotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(ChequesNotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				ChequesNotasPagamentosPeer::addSelectColumns($criteria);
				$this->collChequesNotasPagamentoss = ChequesNotasPagamentosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ChequesNotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(ChequesNotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(ChequesNotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(ChequesNotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(ChequesNotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				ChequesNotasPagamentosPeer::addSelectColumns($criteria);
				if (!isset($this->lastChequesNotasPagamentosCriteria) || !$this->lastChequesNotasPagamentosCriteria->equals($criteria)) {
					$this->collChequesNotasPagamentoss = ChequesNotasPagamentosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastChequesNotasPagamentosCriteria = $criteria;
		return $this->collChequesNotasPagamentoss;
	}

	/**
	 * Returns the number of related ChequesNotasPagamentos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ChequesNotasPagamentos objects.
	 * @throws     PropelException
	 */
	public function countChequesNotasPagamentoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collChequesNotasPagamentoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ChequesNotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(ChequesNotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(ChequesNotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(ChequesNotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(ChequesNotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$count = ChequesNotasPagamentosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ChequesNotasPagamentosPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(ChequesNotasPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(ChequesNotasPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(ChequesNotasPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(ChequesNotasPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(ChequesNotasPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				if (!isset($this->lastChequesNotasPagamentosCriteria) || !$this->lastChequesNotasPagamentosCriteria->equals($criteria)) {
					$count = ChequesNotasPagamentosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collChequesNotasPagamentoss);
				}
			} else {
				$count = count($this->collChequesNotasPagamentoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ChequesNotasPagamentos object to this object
	 * through the ChequesNotasPagamentos foreign key attribute.
	 *
	 * @param      ChequesNotasPagamentos $l ChequesNotasPagamentos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addChequesNotasPagamentos(ChequesNotasPagamentos $l)
	{
		if ($this->collChequesNotasPagamentoss === null) {
			$this->initChequesNotasPagamentoss();
		}
		if (!in_array($l, $this->collChequesNotasPagamentoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collChequesNotasPagamentoss, $l);
			$l->setNotasPagamentos($this);
		}
	}

	/**
	 * Clears out the collDeducoesNotasPags collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDeducoesNotasPags()
	 */
	public function clearDeducoesNotasPags()
	{
		$this->collDeducoesNotasPags = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDeducoesNotasPags collection (array).
	 *
	 * By default this just sets the collDeducoesNotasPags collection to an empty array (like clearcollDeducoesNotasPags());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDeducoesNotasPags()
	{
		$this->collDeducoesNotasPags = array();
	}

	/**
	 * Gets an array of DeducoesNotasPag objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos has previously been saved, it will retrieve
	 * related DeducoesNotasPags from storage. If this NotasPagamentos is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array DeducoesNotasPag[]
	 * @throws     PropelException
	 */
	public function getDeducoesNotasPags($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDeducoesNotasPags === null) {
			if ($this->isNew()) {
			   $this->collDeducoesNotasPags = array();
			} else {

				$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				DeducoesNotasPagPeer::addSelectColumns($criteria);
				$this->collDeducoesNotasPags = DeducoesNotasPagPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				DeducoesNotasPagPeer::addSelectColumns($criteria);
				if (!isset($this->lastDeducoesNotasPagCriteria) || !$this->lastDeducoesNotasPagCriteria->equals($criteria)) {
					$this->collDeducoesNotasPags = DeducoesNotasPagPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDeducoesNotasPagCriteria = $criteria;
		return $this->collDeducoesNotasPags;
	}

	/**
	 * Returns the number of related DeducoesNotasPag objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related DeducoesNotasPag objects.
	 * @throws     PropelException
	 */
	public function countDeducoesNotasPags(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDeducoesNotasPags === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$count = DeducoesNotasPagPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				if (!isset($this->lastDeducoesNotasPagCriteria) || !$this->lastDeducoesNotasPagCriteria->equals($criteria)) {
					$count = DeducoesNotasPagPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDeducoesNotasPags);
				}
			} else {
				$count = count($this->collDeducoesNotasPags);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a DeducoesNotasPag object to this object
	 * through the DeducoesNotasPag foreign key attribute.
	 *
	 * @param      DeducoesNotasPag $l DeducoesNotasPag
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDeducoesNotasPag(DeducoesNotasPag $l)
	{
		if ($this->collDeducoesNotasPags === null) {
			$this->initDeducoesNotasPags();
		}
		if (!in_array($l, $this->collDeducoesNotasPags, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDeducoesNotasPags, $l);
			$l->setNotasPagamentos($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos is new, it will return
	 * an empty collection; or if this NotasPagamentos has previously
	 * been saved, it will retrieve related DeducoesNotasPags from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in NotasPagamentos.
	 */
	public function getDeducoesNotasPagsJoinTiposDeducoes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDeducoesNotasPags === null) {
			if ($this->isNew()) {
				$this->collDeducoesNotasPags = array();
			} else {

				$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$this->collDeducoesNotasPags = DeducoesNotasPagPeer::doSelectJoinTiposDeducoes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DeducoesNotasPagPeer::CD_ORGAO, $this->cd_orgao);

			$criteria->add(DeducoesNotasPagPeer::CD_UNID_ORC, $this->cd_unid_orc);

			$criteria->add(DeducoesNotasPagPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

			$criteria->add(DeducoesNotasPagPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

			$criteria->add(DeducoesNotasPagPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

			$criteria->add(DeducoesNotasPagPeer::NU_NOTA_NP, $this->nu_nota_np);

			$criteria->add(DeducoesNotasPagPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

			if (!isset($this->lastDeducoesNotasPagCriteria) || !$this->lastDeducoesNotasPagCriteria->equals($criteria)) {
				$this->collDeducoesNotasPags = DeducoesNotasPagPeer::doSelectJoinTiposDeducoes($criteria, $con, $join_behavior);
			}
		}
		$this->lastDeducoesNotasPagCriteria = $criteria;

		return $this->collDeducoesNotasPags;
	}

	/**
	 * Clears out the collDeducoesNotasPagass collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDeducoesNotasPagass()
	 */
	public function clearDeducoesNotasPagass()
	{
		$this->collDeducoesNotasPagass = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDeducoesNotasPagass collection (array).
	 *
	 * By default this just sets the collDeducoesNotasPagass collection to an empty array (like clearcollDeducoesNotasPagass());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDeducoesNotasPagass()
	{
		$this->collDeducoesNotasPagass = array();
	}

	/**
	 * Gets an array of DeducoesNotasPagas objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos has previously been saved, it will retrieve
	 * related DeducoesNotasPagass from storage. If this NotasPagamentos is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array DeducoesNotasPagas[]
	 * @throws     PropelException
	 */
	public function getDeducoesNotasPagass($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDeducoesNotasPagass === null) {
			if ($this->isNew()) {
			   $this->collDeducoesNotasPagass = array();
			} else {

				$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				DeducoesNotasPagasPeer::addSelectColumns($criteria);
				$this->collDeducoesNotasPagass = DeducoesNotasPagasPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				DeducoesNotasPagasPeer::addSelectColumns($criteria);
				if (!isset($this->lastDeducoesNotasPagasCriteria) || !$this->lastDeducoesNotasPagasCriteria->equals($criteria)) {
					$this->collDeducoesNotasPagass = DeducoesNotasPagasPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDeducoesNotasPagasCriteria = $criteria;
		return $this->collDeducoesNotasPagass;
	}

	/**
	 * Returns the number of related DeducoesNotasPagas objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related DeducoesNotasPagas objects.
	 * @throws     PropelException
	 */
	public function countDeducoesNotasPagass(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDeducoesNotasPagass === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$count = DeducoesNotasPagasPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				if (!isset($this->lastDeducoesNotasPagasCriteria) || !$this->lastDeducoesNotasPagasCriteria->equals($criteria)) {
					$count = DeducoesNotasPagasPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDeducoesNotasPagass);
				}
			} else {
				$count = count($this->collDeducoesNotasPagass);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a DeducoesNotasPagas object to this object
	 * through the DeducoesNotasPagas foreign key attribute.
	 *
	 * @param      DeducoesNotasPagas $l DeducoesNotasPagas
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDeducoesNotasPagas(DeducoesNotasPagas $l)
	{
		if ($this->collDeducoesNotasPagass === null) {
			$this->initDeducoesNotasPagass();
		}
		if (!in_array($l, $this->collDeducoesNotasPagass, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDeducoesNotasPagass, $l);
			$l->setNotasPagamentos($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos is new, it will return
	 * an empty collection; or if this NotasPagamentos has previously
	 * been saved, it will retrieve related DeducoesNotasPagass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in NotasPagamentos.
	 */
	public function getDeducoesNotasPagassJoinContasExtraOrc($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDeducoesNotasPagass === null) {
			if ($this->isNew()) {
				$this->collDeducoesNotasPagass = array();
			} else {

				$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$this->collDeducoesNotasPagass = DeducoesNotasPagasPeer::doSelectJoinContasExtraOrc($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DeducoesNotasPagasPeer::CD_ORGAO, $this->cd_orgao);

			$criteria->add(DeducoesNotasPagasPeer::CD_UNID_ORC, $this->cd_unid_orc);

			$criteria->add(DeducoesNotasPagasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

			$criteria->add(DeducoesNotasPagasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

			$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

			$criteria->add(DeducoesNotasPagasPeer::NU_NOTA_NP, $this->nu_nota_np);

			$criteria->add(DeducoesNotasPagasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

			if (!isset($this->lastDeducoesNotasPagasCriteria) || !$this->lastDeducoesNotasPagasCriteria->equals($criteria)) {
				$this->collDeducoesNotasPagass = DeducoesNotasPagasPeer::doSelectJoinContasExtraOrc($criteria, $con, $join_behavior);
			}
		}
		$this->lastDeducoesNotasPagasCriteria = $criteria;

		return $this->collDeducoesNotasPagass;
	}

	/**
	 * Clears out the collEstornosPagamentoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addEstornosPagamentoss()
	 */
	public function clearEstornosPagamentoss()
	{
		$this->collEstornosPagamentoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collEstornosPagamentoss collection (array).
	 *
	 * By default this just sets the collEstornosPagamentoss collection to an empty array (like clearcollEstornosPagamentoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initEstornosPagamentoss()
	{
		$this->collEstornosPagamentoss = array();
	}

	/**
	 * Gets an array of EstornosPagamentos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos has previously been saved, it will retrieve
	 * related EstornosPagamentoss from storage. If this NotasPagamentos is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array EstornosPagamentos[]
	 * @throws     PropelException
	 */
	public function getEstornosPagamentoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collEstornosPagamentoss === null) {
			if ($this->isNew()) {
			   $this->collEstornosPagamentoss = array();
			} else {

				$criteria->add(EstornosPagamentosPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(EstornosPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(EstornosPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(EstornosPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(EstornosPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(EstornosPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(EstornosPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				EstornosPagamentosPeer::addSelectColumns($criteria);
				$this->collEstornosPagamentoss = EstornosPagamentosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(EstornosPagamentosPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(EstornosPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(EstornosPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(EstornosPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(EstornosPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(EstornosPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(EstornosPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				EstornosPagamentosPeer::addSelectColumns($criteria);
				if (!isset($this->lastEstornosPagamentosCriteria) || !$this->lastEstornosPagamentosCriteria->equals($criteria)) {
					$this->collEstornosPagamentoss = EstornosPagamentosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastEstornosPagamentosCriteria = $criteria;
		return $this->collEstornosPagamentoss;
	}

	/**
	 * Returns the number of related EstornosPagamentos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related EstornosPagamentos objects.
	 * @throws     PropelException
	 */
	public function countEstornosPagamentoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collEstornosPagamentoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(EstornosPagamentosPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(EstornosPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(EstornosPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(EstornosPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(EstornosPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(EstornosPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(EstornosPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$count = EstornosPagamentosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(EstornosPagamentosPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(EstornosPagamentosPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(EstornosPagamentosPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(EstornosPagamentosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(EstornosPagamentosPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(EstornosPagamentosPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(EstornosPagamentosPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				if (!isset($this->lastEstornosPagamentosCriteria) || !$this->lastEstornosPagamentosCriteria->equals($criteria)) {
					$count = EstornosPagamentosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collEstornosPagamentoss);
				}
			} else {
				$count = count($this->collEstornosPagamentoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a EstornosPagamentos object to this object
	 * through the EstornosPagamentos foreign key attribute.
	 *
	 * @param      EstornosPagamentos $l EstornosPagamentos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addEstornosPagamentos(EstornosPagamentos $l)
	{
		if ($this->collEstornosPagamentoss === null) {
			$this->initEstornosPagamentoss();
		}
		if (!in_array($l, $this->collEstornosPagamentoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collEstornosPagamentoss, $l);
			$l->setNotasPagamentos($this);
		}
	}

	/**
	 * Clears out the collPagamentosFolhass collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPagamentosFolhass()
	 */
	public function clearPagamentosFolhass()
	{
		$this->collPagamentosFolhass = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPagamentosFolhass collection (array).
	 *
	 * By default this just sets the collPagamentosFolhass collection to an empty array (like clearcollPagamentosFolhass());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPagamentosFolhass()
	{
		$this->collPagamentosFolhass = array();
	}

	/**
	 * Gets an array of PagamentosFolhas objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this NotasPagamentos has previously been saved, it will retrieve
	 * related PagamentosFolhass from storage. If this NotasPagamentos is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array PagamentosFolhas[]
	 * @throws     PropelException
	 */
	public function getPagamentosFolhass($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPagamentosFolhass === null) {
			if ($this->isNew()) {
			   $this->collPagamentosFolhass = array();
			} else {

				$criteria->add(PagamentosFolhasPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(PagamentosFolhasPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(PagamentosFolhasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(PagamentosFolhasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(PagamentosFolhasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(PagamentosFolhasPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(PagamentosFolhasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				PagamentosFolhasPeer::addSelectColumns($criteria);
				$this->collPagamentosFolhass = PagamentosFolhasPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PagamentosFolhasPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(PagamentosFolhasPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(PagamentosFolhasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(PagamentosFolhasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(PagamentosFolhasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(PagamentosFolhasPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(PagamentosFolhasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				PagamentosFolhasPeer::addSelectColumns($criteria);
				if (!isset($this->lastPagamentosFolhasCriteria) || !$this->lastPagamentosFolhasCriteria->equals($criteria)) {
					$this->collPagamentosFolhass = PagamentosFolhasPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPagamentosFolhasCriteria = $criteria;
		return $this->collPagamentosFolhass;
	}

	/**
	 * Returns the number of related PagamentosFolhas objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PagamentosFolhas objects.
	 * @throws     PropelException
	 */
	public function countPagamentosFolhass(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(NotasPagamentosPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPagamentosFolhass === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PagamentosFolhasPeer::CD_ORGAO, $this->cd_orgao);

				$criteria->add(PagamentosFolhasPeer::CD_UNID_ORC, $this->cd_unid_orc);

				$criteria->add(PagamentosFolhasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);

				$criteria->add(PagamentosFolhasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);

				$criteria->add(PagamentosFolhasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);

				$criteria->add(PagamentosFolhasPeer::NU_NOTA_NP, $this->nu_nota_np);

				$criteria->add(PagamentosFolhasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				$count = PagamentosFolhasPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PagamentosFolhasPeer::CD_ORGAO, $this->cd_orgao);


				$criteria->add(PagamentosFolhasPeer::CD_UNID_ORC, $this->cd_unid_orc);


				$criteria->add(PagamentosFolhasPeer::DT_EMISSAO_NE, $this->dt_emissao_ne);


				$criteria->add(PagamentosFolhasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);


				$criteria->add(PagamentosFolhasPeer::NU_NOTA_EMPENHO, $this->nu_nota_empenho);


				$criteria->add(PagamentosFolhasPeer::NU_NOTA_NP, $this->nu_nota_np);


				$criteria->add(PagamentosFolhasPeer::NU_SUB_EMPENHO_NP, $this->nu_sub_empenho_np);

				if (!isset($this->lastPagamentosFolhasCriteria) || !$this->lastPagamentosFolhasCriteria->equals($criteria)) {
					$count = PagamentosFolhasPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPagamentosFolhass);
				}
			} else {
				$count = count($this->collPagamentosFolhass);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a PagamentosFolhas object to this object
	 * through the PagamentosFolhas foreign key attribute.
	 *
	 * @param      PagamentosFolhas $l PagamentosFolhas
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPagamentosFolhas(PagamentosFolhas $l)
	{
		if ($this->collPagamentosFolhass === null) {
			$this->initPagamentosFolhass();
		}
		if (!in_array($l, $this->collPagamentosFolhass, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPagamentosFolhass, $l);
			$l->setNotasPagamentos($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collChequesNotasPagamentoss) {
				foreach ((array) $this->collChequesNotasPagamentoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDeducoesNotasPags) {
				foreach ((array) $this->collDeducoesNotasPags as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDeducoesNotasPagass) {
				foreach ((array) $this->collDeducoesNotasPagass as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collEstornosPagamentoss) {
				foreach ((array) $this->collEstornosPagamentoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPagamentosFolhass) {
				foreach ((array) $this->collPagamentosFolhass as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collChequesNotasPagamentoss = null;
		$this->collDeducoesNotasPags = null;
		$this->collDeducoesNotasPagass = null;
		$this->collEstornosPagamentoss = null;
		$this->collPagamentosFolhass = null;
			$this->aDadosPessoais = null;
			$this->aNotasEmpenhos = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseNotasPagamentos:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseNotasPagamentos::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseNotasPagamentos
