<?php

/**
 * Base class that represents a row from the 'TIPOS_EXPEDIENTES' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:23:17 2010
 *
 * @package    lib.model.om
 */
abstract class BaseTiposExpedientes extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        TiposExpedientesPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_expediente field.
	 * @var        string
	 */
	protected $cd_expediente;

	/**
	 * The value for the de_expediente field.
	 * @var        string
	 */
	protected $de_expediente;

	/**
	 * @var        array AgentesPublicos[] Collection to store aggregation of AgentesPublicos objects.
	 */
	protected $collAgentesPublicoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAgentesPublicoss.
	 */
	private $lastAgentesPublicosCriteria = null;

	/**
	 * @var        array ConcessoesItens[] Collection to store aggregation of ConcessoesItens objects.
	 */
	protected $collConcessoesItenss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collConcessoesItenss.
	 */
	private $lastConcessoesItensCriteria = null;

	/**
	 * @var        array Desligamentos[] Collection to store aggregation of Desligamentos objects.
	 */
	protected $collDesligamentoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDesligamentoss.
	 */
	private $lastDesligamentosCriteria = null;

	/**
	 * @var        array Reingressos[] Collection to store aggregation of Reingressos objects.
	 */
	protected $collReingressoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collReingressoss.
	 */
	private $lastReingressosCriteria = null;

	/**
	 * @var        array SuspensoesConcItens[] Collection to store aggregation of SuspensoesConcItens objects.
	 */
	protected $collSuspensoesConcItenss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSuspensoesConcItenss.
	 */
	private $lastSuspensoesConcItensCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'TiposExpedientesPeer';

	/**
	 * Get the [cd_expediente] column value.
	 * 
	 * @return     string
	 */
	public function getCdExpediente()
	{
		return $this->cd_expediente;
	}

	/**
	 * Get the [de_expediente] column value.
	 * 
	 * @return     string
	 */
	public function getDeExpediente()
	{
		return $this->de_expediente;
	}

	/**
	 * Set the value of [cd_expediente] column.
	 * 
	 * @param      string $v new value
	 * @return     TiposExpedientes The current object (for fluent API support)
	 */
	public function setCdExpediente($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_expediente !== $v) {
			$this->cd_expediente = $v;
			$this->modifiedColumns[] = TiposExpedientesPeer::CD_EXPEDIENTE;
		}

		return $this;
	} // setCdExpediente()

	/**
	 * Set the value of [de_expediente] column.
	 * 
	 * @param      string $v new value
	 * @return     TiposExpedientes The current object (for fluent API support)
	 */
	public function setDeExpediente($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->de_expediente !== $v) {
			$this->de_expediente = $v;
			$this->modifiedColumns[] = TiposExpedientesPeer::DE_EXPEDIENTE;
		}

		return $this;
	} // setDeExpediente()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_expediente = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->de_expediente = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 2; // 2 = TiposExpedientesPeer::NUM_COLUMNS - TiposExpedientesPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating TiposExpedientes object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposExpedientesPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = TiposExpedientesPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collAgentesPublicoss = null;
			$this->lastAgentesPublicosCriteria = null;

			$this->collConcessoesItenss = null;
			$this->lastConcessoesItensCriteria = null;

			$this->collDesligamentoss = null;
			$this->lastDesligamentosCriteria = null;

			$this->collReingressoss = null;
			$this->lastReingressosCriteria = null;

			$this->collSuspensoesConcItenss = null;
			$this->lastSuspensoesConcItensCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposExpedientesPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTiposExpedientes:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				TiposExpedientesPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTiposExpedientes:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposExpedientesPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTiposExpedientes:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTiposExpedientes:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				TiposExpedientesPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = TiposExpedientesPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += TiposExpedientesPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAgentesPublicoss !== null) {
				foreach ($this->collAgentesPublicoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collConcessoesItenss !== null) {
				foreach ($this->collConcessoesItenss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDesligamentoss !== null) {
				foreach ($this->collDesligamentoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collReingressoss !== null) {
				foreach ($this->collReingressoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSuspensoesConcItenss !== null) {
				foreach ($this->collSuspensoesConcItenss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = TiposExpedientesPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAgentesPublicoss !== null) {
					foreach ($this->collAgentesPublicoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collConcessoesItenss !== null) {
					foreach ($this->collConcessoesItenss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDesligamentoss !== null) {
					foreach ($this->collDesligamentoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collReingressoss !== null) {
					foreach ($this->collReingressoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSuspensoesConcItenss !== null) {
					foreach ($this->collSuspensoesConcItenss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TiposExpedientesPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdExpediente();
				break;
			case 1:
				return $this->getDeExpediente();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = TiposExpedientesPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdExpediente(),
			$keys[1] => $this->getDeExpediente(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TiposExpedientesPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdExpediente($value);
				break;
			case 1:
				$this->setDeExpediente($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = TiposExpedientesPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdExpediente($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDeExpediente($arr[$keys[1]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);

		if ($this->isColumnModified(TiposExpedientesPeer::CD_EXPEDIENTE)) $criteria->add(TiposExpedientesPeer::CD_EXPEDIENTE, $this->cd_expediente);
		if ($this->isColumnModified(TiposExpedientesPeer::DE_EXPEDIENTE)) $criteria->add(TiposExpedientesPeer::DE_EXPEDIENTE, $this->de_expediente);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);

		$criteria->add(TiposExpedientesPeer::CD_EXPEDIENTE, $this->cd_expediente);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getCdExpediente();
	}

	/**
	 * Generic method to set the primary key (cd_expediente column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setCdExpediente($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of TiposExpedientes (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdExpediente($this->cd_expediente);

		$copyObj->setDeExpediente($this->de_expediente);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getAgentesPublicoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAgentesPublicos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getConcessoesItenss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addConcessoesItens($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDesligamentoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDesligamentos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getReingressoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addReingressos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSuspensoesConcItenss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSuspensoesConcItens($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     TiposExpedientes Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     TiposExpedientesPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new TiposExpedientesPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collAgentesPublicoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAgentesPublicoss()
	 */
	public function clearAgentesPublicoss()
	{
		$this->collAgentesPublicoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAgentesPublicoss collection (array).
	 *
	 * By default this just sets the collAgentesPublicoss collection to an empty array (like clearcollAgentesPublicoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAgentesPublicoss()
	{
		$this->collAgentesPublicoss = array();
	}

	/**
	 * Gets an array of AgentesPublicos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes has previously been saved, it will retrieve
	 * related AgentesPublicoss from storage. If this TiposExpedientes is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array AgentesPublicos[]
	 * @throws     PropelException
	 */
	public function getAgentesPublicoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
			   $this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				AgentesPublicosPeer::addSelectColumns($criteria);
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				AgentesPublicosPeer::addSelectColumns($criteria);
				if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
					$this->collAgentesPublicoss = AgentesPublicosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;
		return $this->collAgentesPublicoss;
	}

	/**
	 * Returns the number of related AgentesPublicos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related AgentesPublicos objects.
	 * @throws     PropelException
	 */
	public function countAgentesPublicoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$count = AgentesPublicosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
					$count = AgentesPublicosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAgentesPublicoss);
				}
			} else {
				$count = count($this->collAgentesPublicoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a AgentesPublicos object to this object
	 * through the AgentesPublicos foreign key attribute.
	 *
	 * @param      AgentesPublicos $l AgentesPublicos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAgentesPublicos(AgentesPublicos $l)
	{
		if ($this->collAgentesPublicoss === null) {
			$this->initAgentesPublicoss();
		}
		if (!in_array($l, $this->collAgentesPublicoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAgentesPublicoss, $l);
			$l->setTiposExpedientes($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinTiposIngressos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposIngressos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposIngressos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinVinculos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinVinculos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinVinculos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinTiposAmparosLegais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposAmparosLegais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposAmparosLegais($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinSitFuncionais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinSitFuncionais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinSitFuncionais($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinRegimesJuridicos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinRegimesJuridicos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinRegimesJuridicos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinCboMunicipais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinCboMunicipais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinCboMunicipais($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinTiposCargos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposCargos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposCargos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getAgentesPublicossJoinServidores($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinServidores($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinServidores($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}

	/**
	 * Clears out the collConcessoesItenss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addConcessoesItenss()
	 */
	public function clearConcessoesItenss()
	{
		$this->collConcessoesItenss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collConcessoesItenss collection (array).
	 *
	 * By default this just sets the collConcessoesItenss collection to an empty array (like clearcollConcessoesItenss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initConcessoesItenss()
	{
		$this->collConcessoesItenss = array();
	}

	/**
	 * Gets an array of ConcessoesItens objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes has previously been saved, it will retrieve
	 * related ConcessoesItenss from storage. If this TiposExpedientes is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ConcessoesItens[]
	 * @throws     PropelException
	 */
	public function getConcessoesItenss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collConcessoesItenss === null) {
			if ($this->isNew()) {
			   $this->collConcessoesItenss = array();
			} else {

				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				ConcessoesItensPeer::addSelectColumns($criteria);
				$this->collConcessoesItenss = ConcessoesItensPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				ConcessoesItensPeer::addSelectColumns($criteria);
				if (!isset($this->lastConcessoesItensCriteria) || !$this->lastConcessoesItensCriteria->equals($criteria)) {
					$this->collConcessoesItenss = ConcessoesItensPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastConcessoesItensCriteria = $criteria;
		return $this->collConcessoesItenss;
	}

	/**
	 * Returns the number of related ConcessoesItens objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ConcessoesItens objects.
	 * @throws     PropelException
	 */
	public function countConcessoesItenss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collConcessoesItenss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$count = ConcessoesItensPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				if (!isset($this->lastConcessoesItensCriteria) || !$this->lastConcessoesItensCriteria->equals($criteria)) {
					$count = ConcessoesItensPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collConcessoesItenss);
				}
			} else {
				$count = count($this->collConcessoesItenss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ConcessoesItens object to this object
	 * through the ConcessoesItens foreign key attribute.
	 *
	 * @param      ConcessoesItens $l ConcessoesItens
	 * @return     void
	 * @throws     PropelException
	 */
	public function addConcessoesItens(ConcessoesItens $l)
	{
		if ($this->collConcessoesItenss === null) {
			$this->initConcessoesItenss();
		}
		if (!in_array($l, $this->collConcessoesItenss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collConcessoesItenss, $l);
			$l->setTiposExpedientes($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related ConcessoesItenss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getConcessoesItenssJoinAgentesPublicos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collConcessoesItenss === null) {
			if ($this->isNew()) {
				$this->collConcessoesItenss = array();
			} else {

				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collConcessoesItenss = ConcessoesItensPeer::doSelectJoinAgentesPublicos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastConcessoesItensCriteria) || !$this->lastConcessoesItensCriteria->equals($criteria)) {
				$this->collConcessoesItenss = ConcessoesItensPeer::doSelectJoinAgentesPublicos($criteria, $con, $join_behavior);
			}
		}
		$this->lastConcessoesItensCriteria = $criteria;

		return $this->collConcessoesItenss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related ConcessoesItenss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getConcessoesItenssJoinItensRemuneratorios($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collConcessoesItenss === null) {
			if ($this->isNew()) {
				$this->collConcessoesItenss = array();
			} else {

				$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collConcessoesItenss = ConcessoesItensPeer::doSelectJoinItensRemuneratorios($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ConcessoesItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastConcessoesItensCriteria) || !$this->lastConcessoesItensCriteria->equals($criteria)) {
				$this->collConcessoesItenss = ConcessoesItensPeer::doSelectJoinItensRemuneratorios($criteria, $con, $join_behavior);
			}
		}
		$this->lastConcessoesItensCriteria = $criteria;

		return $this->collConcessoesItenss;
	}

	/**
	 * Clears out the collDesligamentoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDesligamentoss()
	 */
	public function clearDesligamentoss()
	{
		$this->collDesligamentoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDesligamentoss collection (array).
	 *
	 * By default this just sets the collDesligamentoss collection to an empty array (like clearcollDesligamentoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDesligamentoss()
	{
		$this->collDesligamentoss = array();
	}

	/**
	 * Gets an array of Desligamentos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes has previously been saved, it will retrieve
	 * related Desligamentoss from storage. If this TiposExpedientes is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Desligamentos[]
	 * @throws     PropelException
	 */
	public function getDesligamentoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDesligamentoss === null) {
			if ($this->isNew()) {
			   $this->collDesligamentoss = array();
			} else {

				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				DesligamentosPeer::addSelectColumns($criteria);
				$this->collDesligamentoss = DesligamentosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				DesligamentosPeer::addSelectColumns($criteria);
				if (!isset($this->lastDesligamentosCriteria) || !$this->lastDesligamentosCriteria->equals($criteria)) {
					$this->collDesligamentoss = DesligamentosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDesligamentosCriteria = $criteria;
		return $this->collDesligamentoss;
	}

	/**
	 * Returns the number of related Desligamentos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Desligamentos objects.
	 * @throws     PropelException
	 */
	public function countDesligamentoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDesligamentoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$count = DesligamentosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				if (!isset($this->lastDesligamentosCriteria) || !$this->lastDesligamentosCriteria->equals($criteria)) {
					$count = DesligamentosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDesligamentoss);
				}
			} else {
				$count = count($this->collDesligamentoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Desligamentos object to this object
	 * through the Desligamentos foreign key attribute.
	 *
	 * @param      Desligamentos $l Desligamentos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDesligamentos(Desligamentos $l)
	{
		if ($this->collDesligamentoss === null) {
			$this->initDesligamentoss();
		}
		if (!in_array($l, $this->collDesligamentoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDesligamentoss, $l);
			$l->setTiposExpedientes($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related Desligamentoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getDesligamentossJoinTiposDesligamentos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDesligamentoss === null) {
			if ($this->isNew()) {
				$this->collDesligamentoss = array();
			} else {

				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collDesligamentoss = DesligamentosPeer::doSelectJoinTiposDesligamentos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastDesligamentosCriteria) || !$this->lastDesligamentosCriteria->equals($criteria)) {
				$this->collDesligamentoss = DesligamentosPeer::doSelectJoinTiposDesligamentos($criteria, $con, $join_behavior);
			}
		}
		$this->lastDesligamentosCriteria = $criteria;

		return $this->collDesligamentoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related Desligamentoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getDesligamentossJoinAgentesPublicos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDesligamentoss === null) {
			if ($this->isNew()) {
				$this->collDesligamentoss = array();
			} else {

				$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collDesligamentoss = DesligamentosPeer::doSelectJoinAgentesPublicos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DesligamentosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastDesligamentosCriteria) || !$this->lastDesligamentosCriteria->equals($criteria)) {
				$this->collDesligamentoss = DesligamentosPeer::doSelectJoinAgentesPublicos($criteria, $con, $join_behavior);
			}
		}
		$this->lastDesligamentosCriteria = $criteria;

		return $this->collDesligamentoss;
	}

	/**
	 * Clears out the collReingressoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addReingressoss()
	 */
	public function clearReingressoss()
	{
		$this->collReingressoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collReingressoss collection (array).
	 *
	 * By default this just sets the collReingressoss collection to an empty array (like clearcollReingressoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initReingressoss()
	{
		$this->collReingressoss = array();
	}

	/**
	 * Gets an array of Reingressos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes has previously been saved, it will retrieve
	 * related Reingressoss from storage. If this TiposExpedientes is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Reingressos[]
	 * @throws     PropelException
	 */
	public function getReingressoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
			   $this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				ReingressosPeer::addSelectColumns($criteria);
				$this->collReingressoss = ReingressosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				ReingressosPeer::addSelectColumns($criteria);
				if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
					$this->collReingressoss = ReingressosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastReingressosCriteria = $criteria;
		return $this->collReingressoss;
	}

	/**
	 * Returns the number of related Reingressos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Reingressos objects.
	 * @throws     PropelException
	 */
	public function countReingressoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$count = ReingressosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
					$count = ReingressosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collReingressoss);
				}
			} else {
				$count = count($this->collReingressoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Reingressos object to this object
	 * through the Reingressos foreign key attribute.
	 *
	 * @param      Reingressos $l Reingressos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addReingressos(Reingressos $l)
	{
		if ($this->collReingressoss === null) {
			$this->initReingressoss();
		}
		if (!in_array($l, $this->collReingressoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collReingressoss, $l);
			$l->setTiposExpedientes($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getReingressossJoinTiposReingressos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposReingressos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposReingressos($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getReingressossJoinTiposAmparosLegais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposAmparosLegais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposAmparosLegais($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getReingressossJoinDesligamentos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collReingressoss = ReingressosPeer::doSelectJoinDesligamentos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinDesligamentos($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}

	/**
	 * Clears out the collSuspensoesConcItenss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSuspensoesConcItenss()
	 */
	public function clearSuspensoesConcItenss()
	{
		$this->collSuspensoesConcItenss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSuspensoesConcItenss collection (array).
	 *
	 * By default this just sets the collSuspensoesConcItenss collection to an empty array (like clearcollSuspensoesConcItenss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSuspensoesConcItenss()
	{
		$this->collSuspensoesConcItenss = array();
	}

	/**
	 * Gets an array of SuspensoesConcItens objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes has previously been saved, it will retrieve
	 * related SuspensoesConcItenss from storage. If this TiposExpedientes is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SuspensoesConcItens[]
	 * @throws     PropelException
	 */
	public function getSuspensoesConcItenss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSuspensoesConcItenss === null) {
			if ($this->isNew()) {
			   $this->collSuspensoesConcItenss = array();
			} else {

				$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				SuspensoesConcItensPeer::addSelectColumns($criteria);
				$this->collSuspensoesConcItenss = SuspensoesConcItensPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				SuspensoesConcItensPeer::addSelectColumns($criteria);
				if (!isset($this->lastSuspensoesConcItensCriteria) || !$this->lastSuspensoesConcItensCriteria->equals($criteria)) {
					$this->collSuspensoesConcItenss = SuspensoesConcItensPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSuspensoesConcItensCriteria = $criteria;
		return $this->collSuspensoesConcItenss;
	}

	/**
	 * Returns the number of related SuspensoesConcItens objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SuspensoesConcItens objects.
	 * @throws     PropelException
	 */
	public function countSuspensoesConcItenss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSuspensoesConcItenss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$count = SuspensoesConcItensPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				if (!isset($this->lastSuspensoesConcItensCriteria) || !$this->lastSuspensoesConcItensCriteria->equals($criteria)) {
					$count = SuspensoesConcItensPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSuspensoesConcItenss);
				}
			} else {
				$count = count($this->collSuspensoesConcItenss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SuspensoesConcItens object to this object
	 * through the SuspensoesConcItens foreign key attribute.
	 *
	 * @param      SuspensoesConcItens $l SuspensoesConcItens
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSuspensoesConcItens(SuspensoesConcItens $l)
	{
		if ($this->collSuspensoesConcItenss === null) {
			$this->initSuspensoesConcItenss();
		}
		if (!in_array($l, $this->collSuspensoesConcItenss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSuspensoesConcItenss, $l);
			$l->setTiposExpedientes($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposExpedientes is new, it will return
	 * an empty collection; or if this TiposExpedientes has previously
	 * been saved, it will retrieve related SuspensoesConcItenss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposExpedientes.
	 */
	public function getSuspensoesConcItenssJoinConcessoesItens($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposExpedientesPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSuspensoesConcItenss === null) {
			if ($this->isNew()) {
				$this->collSuspensoesConcItenss = array();
			} else {

				$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

				$this->collSuspensoesConcItenss = SuspensoesConcItensPeer::doSelectJoinConcessoesItens($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SuspensoesConcItensPeer::CD_EXPEDIENTE, $this->cd_expediente);

			if (!isset($this->lastSuspensoesConcItensCriteria) || !$this->lastSuspensoesConcItensCriteria->equals($criteria)) {
				$this->collSuspensoesConcItenss = SuspensoesConcItensPeer::doSelectJoinConcessoesItens($criteria, $con, $join_behavior);
			}
		}
		$this->lastSuspensoesConcItensCriteria = $criteria;

		return $this->collSuspensoesConcItenss;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collAgentesPublicoss) {
				foreach ((array) $this->collAgentesPublicoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collConcessoesItenss) {
				foreach ((array) $this->collConcessoesItenss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDesligamentoss) {
				foreach ((array) $this->collDesligamentoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collReingressoss) {
				foreach ((array) $this->collReingressoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSuspensoesConcItenss) {
				foreach ((array) $this->collSuspensoesConcItenss as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collAgentesPublicoss = null;
		$this->collConcessoesItenss = null;
		$this->collDesligamentoss = null;
		$this->collReingressoss = null;
		$this->collSuspensoesConcItenss = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseTiposExpedientes:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseTiposExpedientes::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseTiposExpedientes
