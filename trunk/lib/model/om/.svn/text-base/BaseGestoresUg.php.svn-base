<?php

/**
 * Base class that represents a row from the 'GESTORES_UG' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:22:55 2010
 *
 * @package    lib.model.om
 */
abstract class BaseGestoresUg extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        GestoresUgPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the dt_versao_orc field.
	 * @var        int
	 */
	protected $dt_versao_orc;

	/**
	 * The value for the cd_unid_gest_ug field.
	 * @var        int
	 */
	protected $cd_unid_gest_ug;

	/**
	 * The value for the cd_orgao field.
	 * @var        string
	 */
	protected $cd_orgao;

	/**
	 * The value for the cd_unid_orc field.
	 * @var        string
	 */
	protected $cd_unid_orc;

	/**
	 * The value for the cd_cpf_serv_muni field.
	 * @var        string
	 */
	protected $cd_cpf_serv_muni;

	/**
	 * The value for the cd_ingresso field.
	 * @var        string
	 */
	protected $cd_ingresso;

	/**
	 * The value for the cd_vinculo field.
	 * @var        string
	 */
	protected $cd_vinculo;

	/**
	 * The value for the nu_expediente_no field.
	 * @var        string
	 */
	protected $nu_expediente_no;

	/**
	 * The value for the dt_ini_gestao_gug field.
	 * @var        string
	 */
	protected $dt_ini_gestao_gug;

	/**
	 * The value for the dt_ref_gug field.
	 * @var        int
	 */
	protected $dt_ref_gug;

	/**
	 * The value for the nm_gestor_gug field.
	 * @var        string
	 */
	protected $nm_gestor_gug;

	/**
	 * The value for the dt_fim_gestao_gug field.
	 * @var        string
	 */
	protected $dt_fim_gestao_gug;

	/**
	 * The value for the cd_tipo_cargo field.
	 * @var        string
	 */
	protected $cd_tipo_cargo;

	/**
	 * The value for the st_ordenador_gug field.
	 * @var        boolean
	 */
	protected $st_ordenador_gug;

	/**
	 * @var        TiposCargos
	 */
	protected $aTiposCargos;

	/**
	 * @var        UnidGestoras
	 */
	protected $aUnidGestoras;

	/**
	 * @var        AgentesPublicos
	 */
	protected $aAgentesPublicos;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'GestoresUgPeer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [dt_versao_orc] column value.
	 * 
	 * @return     int
	 */
	public function getDtVersaoOrc()
	{
		return $this->dt_versao_orc;
	}

	/**
	 * Get the [cd_unid_gest_ug] column value.
	 * 
	 * @return     int
	 */
	public function getCdUnidGestUg()
	{
		return $this->cd_unid_gest_ug;
	}

	/**
	 * Get the [cd_orgao] column value.
	 * 
	 * @return     string
	 */
	public function getCdOrgao()
	{
		return $this->cd_orgao;
	}

	/**
	 * Get the [cd_unid_orc] column value.
	 * 
	 * @return     string
	 */
	public function getCdUnidOrc()
	{
		return $this->cd_unid_orc;
	}

	/**
	 * Get the [cd_cpf_serv_muni] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfServMuni()
	{
		return $this->cd_cpf_serv_muni;
	}

	/**
	 * Get the [cd_ingresso] column value.
	 * 
	 * @return     string
	 */
	public function getCdIngresso()
	{
		return $this->cd_ingresso;
	}

	/**
	 * Get the [cd_vinculo] column value.
	 * 
	 * @return     string
	 */
	public function getCdVinculo()
	{
		return $this->cd_vinculo;
	}

	/**
	 * Get the [nu_expediente_no] column value.
	 * 
	 * @return     string
	 */
	public function getNuExpedienteNo()
	{
		return $this->nu_expediente_no;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_ini_gestao_gug] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtIniGestaoGug($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_ini_gestao_gug === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_ini_gestao_gug);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_ini_gestao_gug, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [dt_ref_gug] column value.
	 * 
	 * @return     int
	 */
	public function getDtRefGug()
	{
		return $this->dt_ref_gug;
	}

	/**
	 * Get the [nm_gestor_gug] column value.
	 * 
	 * @return     string
	 */
	public function getNmGestorGug()
	{
		return $this->nm_gestor_gug;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_fim_gestao_gug] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtFimGestaoGug($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_fim_gestao_gug === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_fim_gestao_gug);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_fim_gestao_gug, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [cd_tipo_cargo] column value.
	 * 
	 * @return     string
	 */
	public function getCdTipoCargo()
	{
		return $this->cd_tipo_cargo;
	}

	/**
	 * Get the [st_ordenador_gug] column value.
	 * 
	 * @return     boolean
	 */
	public function getStOrdenadorGug()
	{
		return $this->st_ordenador_gug;
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_MUNICIPIO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdMunicipio() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [dt_versao_orc] column.
	 * 
	 * @param      int $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setDtVersaoOrc($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_versao_orc !== $v) {
			$this->dt_versao_orc = $v;
			$this->modifiedColumns[] = GestoresUgPeer::DT_VERSAO_ORC;
		}

		if ($this->aUnidGestoras !== null && $this->aUnidGestoras->getDtVersaoOrc() !== $v) {
			$this->aUnidGestoras = null;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getDtVersaoOrc() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setDtVersaoOrc()

	/**
	 * Set the value of [cd_unid_gest_ug] column.
	 * 
	 * @param      int $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdUnidGestUg($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->cd_unid_gest_ug !== $v) {
			$this->cd_unid_gest_ug = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_UNID_GEST_UG;
		}

		if ($this->aUnidGestoras !== null && $this->aUnidGestoras->getCdUnidGestUg() !== $v) {
			$this->aUnidGestoras = null;
		}

		return $this;
	} // setCdUnidGestUg()

	/**
	 * Set the value of [cd_orgao] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdOrgao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_orgao !== $v) {
			$this->cd_orgao = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_ORGAO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdOrgao() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdOrgao()

	/**
	 * Set the value of [cd_unid_orc] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdUnidOrc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_unid_orc !== $v) {
			$this->cd_unid_orc = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_UNID_ORC;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdUnidOrc() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdUnidOrc()

	/**
	 * Set the value of [cd_cpf_serv_muni] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdCpfServMuni($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_serv_muni !== $v) {
			$this->cd_cpf_serv_muni = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_CPF_SERV_MUNI;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdCpfServMuni() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdCpfServMuni()

	/**
	 * Set the value of [cd_ingresso] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdIngresso($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_ingresso !== $v) {
			$this->cd_ingresso = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_INGRESSO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdIngresso() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdIngresso()

	/**
	 * Set the value of [cd_vinculo] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdVinculo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_vinculo !== $v) {
			$this->cd_vinculo = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_VINCULO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdVinculo() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdVinculo()

	/**
	 * Set the value of [nu_expediente_no] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setNuExpedienteNo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_expediente_no !== $v) {
			$this->nu_expediente_no = $v;
			$this->modifiedColumns[] = GestoresUgPeer::NU_EXPEDIENTE_NO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getNuExpedienteNo() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setNuExpedienteNo()

	/**
	 * Sets the value of [dt_ini_gestao_gug] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setDtIniGestaoGug($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_ini_gestao_gug !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_ini_gestao_gug !== null && $tmpDt = new MssqlDateTime($this->dt_ini_gestao_gug)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_ini_gestao_gug = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = GestoresUgPeer::DT_INI_GESTAO_GUG;
			}
		} // if either are not null

		return $this;
	} // setDtIniGestaoGug()

	/**
	 * Set the value of [dt_ref_gug] column.
	 * 
	 * @param      int $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setDtRefGug($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_ref_gug !== $v) {
			$this->dt_ref_gug = $v;
			$this->modifiedColumns[] = GestoresUgPeer::DT_REF_GUG;
		}

		return $this;
	} // setDtRefGug()

	/**
	 * Set the value of [nm_gestor_gug] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setNmGestorGug($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nm_gestor_gug !== $v) {
			$this->nm_gestor_gug = $v;
			$this->modifiedColumns[] = GestoresUgPeer::NM_GESTOR_GUG;
		}

		return $this;
	} // setNmGestorGug()

	/**
	 * Sets the value of [dt_fim_gestao_gug] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setDtFimGestaoGug($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_fim_gestao_gug !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_fim_gestao_gug !== null && $tmpDt = new MssqlDateTime($this->dt_fim_gestao_gug)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_fim_gestao_gug = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = GestoresUgPeer::DT_FIM_GESTAO_GUG;
			}
		} // if either are not null

		return $this;
	} // setDtFimGestaoGug()

	/**
	 * Set the value of [cd_tipo_cargo] column.
	 * 
	 * @param      string $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setCdTipoCargo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_tipo_cargo !== $v) {
			$this->cd_tipo_cargo = $v;
			$this->modifiedColumns[] = GestoresUgPeer::CD_TIPO_CARGO;
		}

		if ($this->aTiposCargos !== null && $this->aTiposCargos->getCdTipoCargo() !== $v) {
			$this->aTiposCargos = null;
		}

		return $this;
	} // setCdTipoCargo()

	/**
	 * Set the value of [st_ordenador_gug] column.
	 * 
	 * @param      boolean $v new value
	 * @return     GestoresUg The current object (for fluent API support)
	 */
	public function setStOrdenadorGug($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->st_ordenador_gug !== $v) {
			$this->st_ordenador_gug = $v;
			$this->modifiedColumns[] = GestoresUgPeer::ST_ORDENADOR_GUG;
		}

		return $this;
	} // setStOrdenadorGug()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->dt_versao_orc = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_unid_gest_ug = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->cd_orgao = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->cd_unid_orc = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->cd_cpf_serv_muni = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->cd_ingresso = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->cd_vinculo = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->nu_expediente_no = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->dt_ini_gestao_gug = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->dt_ref_gug = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->nm_gestor_gug = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->dt_fim_gestao_gug = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->cd_tipo_cargo = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->st_ordenador_gug = ($row[$startcol + 14] !== null) ? (boolean) $row[$startcol + 14] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 15; // 15 = GestoresUgPeer::NUM_COLUMNS - GestoresUgPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating GestoresUg object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aAgentesPublicos !== null && $this->cd_municipio !== $this->aAgentesPublicos->getCdMunicipio()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidGestoras !== null && $this->dt_versao_orc !== $this->aUnidGestoras->getDtVersaoOrc()) {
			$this->aUnidGestoras = null;
		}
		if ($this->aAgentesPublicos !== null && $this->dt_versao_orc !== $this->aAgentesPublicos->getDtVersaoOrc()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidGestoras !== null && $this->cd_unid_gest_ug !== $this->aUnidGestoras->getCdUnidGestUg()) {
			$this->aUnidGestoras = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_orgao !== $this->aAgentesPublicos->getCdOrgao()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_unid_orc !== $this->aAgentesPublicos->getCdUnidOrc()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_cpf_serv_muni !== $this->aAgentesPublicos->getCdCpfServMuni()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_ingresso !== $this->aAgentesPublicos->getCdIngresso()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_vinculo !== $this->aAgentesPublicos->getCdVinculo()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->nu_expediente_no !== $this->aAgentesPublicos->getNuExpedienteNo()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aTiposCargos !== null && $this->cd_tipo_cargo !== $this->aTiposCargos->getCdTipoCargo()) {
			$this->aTiposCargos = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GestoresUgPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = GestoresUgPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aTiposCargos = null;
			$this->aUnidGestoras = null;
			$this->aAgentesPublicos = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GestoresUgPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseGestoresUg:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				GestoresUgPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseGestoresUg:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(GestoresUgPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseGestoresUg:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseGestoresUg:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				GestoresUgPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTiposCargos !== null) {
				if ($this->aTiposCargos->isModified() || $this->aTiposCargos->isNew()) {
					$affectedRows += $this->aTiposCargos->save($con);
				}
				$this->setTiposCargos($this->aTiposCargos);
			}

			if ($this->aUnidGestoras !== null) {
				if ($this->aUnidGestoras->isModified() || $this->aUnidGestoras->isNew()) {
					$affectedRows += $this->aUnidGestoras->save($con);
				}
				$this->setUnidGestoras($this->aUnidGestoras);
			}

			if ($this->aAgentesPublicos !== null) {
				if ($this->aAgentesPublicos->isModified() || $this->aAgentesPublicos->isNew()) {
					$affectedRows += $this->aAgentesPublicos->save($con);
				}
				$this->setAgentesPublicos($this->aAgentesPublicos);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = GestoresUgPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += GestoresUgPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTiposCargos !== null) {
				if (!$this->aTiposCargos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTiposCargos->getValidationFailures());
				}
			}

			if ($this->aUnidGestoras !== null) {
				if (!$this->aUnidGestoras->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUnidGestoras->getValidationFailures());
				}
			}

			if ($this->aAgentesPublicos !== null) {
				if (!$this->aAgentesPublicos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAgentesPublicos->getValidationFailures());
				}
			}


			if (($retval = GestoresUgPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = GestoresUgPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getDtVersaoOrc();
				break;
			case 2:
				return $this->getCdUnidGestUg();
				break;
			case 3:
				return $this->getCdOrgao();
				break;
			case 4:
				return $this->getCdUnidOrc();
				break;
			case 5:
				return $this->getCdCpfServMuni();
				break;
			case 6:
				return $this->getCdIngresso();
				break;
			case 7:
				return $this->getCdVinculo();
				break;
			case 8:
				return $this->getNuExpedienteNo();
				break;
			case 9:
				return $this->getDtIniGestaoGug();
				break;
			case 10:
				return $this->getDtRefGug();
				break;
			case 11:
				return $this->getNmGestorGug();
				break;
			case 12:
				return $this->getDtFimGestaoGug();
				break;
			case 13:
				return $this->getCdTipoCargo();
				break;
			case 14:
				return $this->getStOrdenadorGug();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = GestoresUgPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getDtVersaoOrc(),
			$keys[2] => $this->getCdUnidGestUg(),
			$keys[3] => $this->getCdOrgao(),
			$keys[4] => $this->getCdUnidOrc(),
			$keys[5] => $this->getCdCpfServMuni(),
			$keys[6] => $this->getCdIngresso(),
			$keys[7] => $this->getCdVinculo(),
			$keys[8] => $this->getNuExpedienteNo(),
			$keys[9] => $this->getDtIniGestaoGug(),
			$keys[10] => $this->getDtRefGug(),
			$keys[11] => $this->getNmGestorGug(),
			$keys[12] => $this->getDtFimGestaoGug(),
			$keys[13] => $this->getCdTipoCargo(),
			$keys[14] => $this->getStOrdenadorGug(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = GestoresUgPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setDtVersaoOrc($value);
				break;
			case 2:
				$this->setCdUnidGestUg($value);
				break;
			case 3:
				$this->setCdOrgao($value);
				break;
			case 4:
				$this->setCdUnidOrc($value);
				break;
			case 5:
				$this->setCdCpfServMuni($value);
				break;
			case 6:
				$this->setCdIngresso($value);
				break;
			case 7:
				$this->setCdVinculo($value);
				break;
			case 8:
				$this->setNuExpedienteNo($value);
				break;
			case 9:
				$this->setDtIniGestaoGug($value);
				break;
			case 10:
				$this->setDtRefGug($value);
				break;
			case 11:
				$this->setNmGestorGug($value);
				break;
			case 12:
				$this->setDtFimGestaoGug($value);
				break;
			case 13:
				$this->setCdTipoCargo($value);
				break;
			case 14:
				$this->setStOrdenadorGug($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = GestoresUgPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDtVersaoOrc($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdUnidGestUg($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCdOrgao($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCdUnidOrc($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCdCpfServMuni($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCdIngresso($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCdVinculo($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setNuExpedienteNo($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setDtIniGestaoGug($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setDtRefGug($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setNmGestorGug($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setDtFimGestaoGug($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setCdTipoCargo($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setStOrdenadorGug($arr[$keys[14]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(GestoresUgPeer::DATABASE_NAME);

		if ($this->isColumnModified(GestoresUgPeer::CD_MUNICIPIO)) $criteria->add(GestoresUgPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(GestoresUgPeer::DT_VERSAO_ORC)) $criteria->add(GestoresUgPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		if ($this->isColumnModified(GestoresUgPeer::CD_UNID_GEST_UG)) $criteria->add(GestoresUgPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
		if ($this->isColumnModified(GestoresUgPeer::CD_ORGAO)) $criteria->add(GestoresUgPeer::CD_ORGAO, $this->cd_orgao);
		if ($this->isColumnModified(GestoresUgPeer::CD_UNID_ORC)) $criteria->add(GestoresUgPeer::CD_UNID_ORC, $this->cd_unid_orc);
		if ($this->isColumnModified(GestoresUgPeer::CD_CPF_SERV_MUNI)) $criteria->add(GestoresUgPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		if ($this->isColumnModified(GestoresUgPeer::CD_INGRESSO)) $criteria->add(GestoresUgPeer::CD_INGRESSO, $this->cd_ingresso);
		if ($this->isColumnModified(GestoresUgPeer::CD_VINCULO)) $criteria->add(GestoresUgPeer::CD_VINCULO, $this->cd_vinculo);
		if ($this->isColumnModified(GestoresUgPeer::NU_EXPEDIENTE_NO)) $criteria->add(GestoresUgPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		if ($this->isColumnModified(GestoresUgPeer::DT_INI_GESTAO_GUG)) $criteria->add(GestoresUgPeer::DT_INI_GESTAO_GUG, $this->dt_ini_gestao_gug);
		if ($this->isColumnModified(GestoresUgPeer::DT_REF_GUG)) $criteria->add(GestoresUgPeer::DT_REF_GUG, $this->dt_ref_gug);
		if ($this->isColumnModified(GestoresUgPeer::NM_GESTOR_GUG)) $criteria->add(GestoresUgPeer::NM_GESTOR_GUG, $this->nm_gestor_gug);
		if ($this->isColumnModified(GestoresUgPeer::DT_FIM_GESTAO_GUG)) $criteria->add(GestoresUgPeer::DT_FIM_GESTAO_GUG, $this->dt_fim_gestao_gug);
		if ($this->isColumnModified(GestoresUgPeer::CD_TIPO_CARGO)) $criteria->add(GestoresUgPeer::CD_TIPO_CARGO, $this->cd_tipo_cargo);
		if ($this->isColumnModified(GestoresUgPeer::ST_ORDENADOR_GUG)) $criteria->add(GestoresUgPeer::ST_ORDENADOR_GUG, $this->st_ordenador_gug);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(GestoresUgPeer::DATABASE_NAME);

		$criteria->add(GestoresUgPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(GestoresUgPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		$criteria->add(GestoresUgPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
		$criteria->add(GestoresUgPeer::CD_ORGAO, $this->cd_orgao);
		$criteria->add(GestoresUgPeer::CD_UNID_ORC, $this->cd_unid_orc);
		$criteria->add(GestoresUgPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		$criteria->add(GestoresUgPeer::CD_INGRESSO, $this->cd_ingresso);
		$criteria->add(GestoresUgPeer::CD_VINCULO, $this->cd_vinculo);
		$criteria->add(GestoresUgPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		$criteria->add(GestoresUgPeer::DT_INI_GESTAO_GUG, $this->dt_ini_gestao_gug);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getDtVersaoOrc();

		$pks[2] = $this->getCdUnidGestUg();

		$pks[3] = $this->getCdOrgao();

		$pks[4] = $this->getCdUnidOrc();

		$pks[5] = $this->getCdCpfServMuni();

		$pks[6] = $this->getCdIngresso();

		$pks[7] = $this->getCdVinculo();

		$pks[8] = $this->getNuExpedienteNo();

		$pks[9] = $this->getDtIniGestaoGug();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setDtVersaoOrc($keys[1]);

		$this->setCdUnidGestUg($keys[2]);

		$this->setCdOrgao($keys[3]);

		$this->setCdUnidOrc($keys[4]);

		$this->setCdCpfServMuni($keys[5]);

		$this->setCdIngresso($keys[6]);

		$this->setCdVinculo($keys[7]);

		$this->setNuExpedienteNo($keys[8]);

		$this->setDtIniGestaoGug($keys[9]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of GestoresUg (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setDtVersaoOrc($this->dt_versao_orc);

		$copyObj->setCdUnidGestUg($this->cd_unid_gest_ug);

		$copyObj->setCdOrgao($this->cd_orgao);

		$copyObj->setCdUnidOrc($this->cd_unid_orc);

		$copyObj->setCdCpfServMuni($this->cd_cpf_serv_muni);

		$copyObj->setCdIngresso($this->cd_ingresso);

		$copyObj->setCdVinculo($this->cd_vinculo);

		$copyObj->setNuExpedienteNo($this->nu_expediente_no);

		$copyObj->setDtIniGestaoGug($this->dt_ini_gestao_gug);

		$copyObj->setDtRefGug($this->dt_ref_gug);

		$copyObj->setNmGestorGug($this->nm_gestor_gug);

		$copyObj->setDtFimGestaoGug($this->dt_fim_gestao_gug);

		$copyObj->setCdTipoCargo($this->cd_tipo_cargo);

		$copyObj->setStOrdenadorGug($this->st_ordenador_gug);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     GestoresUg Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     GestoresUgPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new GestoresUgPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a TiposCargos object.
	 *
	 * @param      TiposCargos $v
	 * @return     GestoresUg The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTiposCargos(TiposCargos $v = null)
	{
		if ($v === null) {
			$this->setCdTipoCargo(NULL);
		} else {
			$this->setCdTipoCargo($v->getCdTipoCargo());
		}

		$this->aTiposCargos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TiposCargos object, it will not be re-added.
		if ($v !== null) {
			$v->addGestoresUg($this);
		}

		return $this;
	}


	/**
	 * Get the associated TiposCargos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TiposCargos The associated TiposCargos object.
	 * @throws     PropelException
	 */
	public function getTiposCargos(PropelPDO $con = null)
	{
		if ($this->aTiposCargos === null && (($this->cd_tipo_cargo !== "" && $this->cd_tipo_cargo !== null))) {
			$this->aTiposCargos = TiposCargosPeer::retrieveByPk($this->cd_tipo_cargo);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTiposCargos->addGestoresUgs($this);
			 */
		}
		return $this->aTiposCargos;
	}

	/**
	 * Declares an association between this object and a UnidGestoras object.
	 *
	 * @param      UnidGestoras $v
	 * @return     GestoresUg The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUnidGestoras(UnidGestoras $v = null)
	{
		if ($v === null) {
			$this->setCdUnidGestUg(NULL);
		} else {
			$this->setCdUnidGestUg($v->getCdUnidGestUg());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		$this->aUnidGestoras = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UnidGestoras object, it will not be re-added.
		if ($v !== null) {
			$v->addGestoresUg($this);
		}

		return $this;
	}


	/**
	 * Get the associated UnidGestoras object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UnidGestoras The associated UnidGestoras object.
	 * @throws     PropelException
	 */
	public function getUnidGestoras(PropelPDO $con = null)
	{
		if ($this->aUnidGestoras === null && ($this->cd_unid_gest_ug !== null && $this->dt_versao_orc !== null)) {
			$c = new Criteria(UnidGestorasPeer::DATABASE_NAME);
			$c->add(UnidGestorasPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
			$c->add(UnidGestorasPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$this->aUnidGestoras = UnidGestorasPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUnidGestoras->addGestoresUgs($this);
			 */
		}
		return $this->aUnidGestoras;
	}

	/**
	 * Declares an association between this object and a AgentesPublicos object.
	 *
	 * @param      AgentesPublicos $v
	 * @return     GestoresUg The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAgentesPublicos(AgentesPublicos $v = null)
	{
		if ($v === null) {
			$this->setCdCpfServMuni(NULL);
		} else {
			$this->setCdCpfServMuni($v->getCdCpfServMuni());
		}

		if ($v === null) {
			$this->setCdIngresso(NULL);
		} else {
			$this->setCdIngresso($v->getCdIngresso());
		}

		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setCdVinculo(NULL);
		} else {
			$this->setCdVinculo($v->getCdVinculo());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		if ($v === null) {
			$this->setNuExpedienteNo(NULL);
		} else {
			$this->setNuExpedienteNo($v->getNuExpedienteNo());
		}

		$this->aAgentesPublicos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the AgentesPublicos object, it will not be re-added.
		if ($v !== null) {
			$v->addGestoresUg($this);
		}

		return $this;
	}


	/**
	 * Get the associated AgentesPublicos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     AgentesPublicos The associated AgentesPublicos object.
	 * @throws     PropelException
	 */
	public function getAgentesPublicos(PropelPDO $con = null)
	{
		if ($this->aAgentesPublicos === null && (($this->cd_cpf_serv_muni !== "" && $this->cd_cpf_serv_muni !== null) && ($this->cd_ingresso !== "" && $this->cd_ingresso !== null) && ($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->cd_orgao !== "" && $this->cd_orgao !== null) && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && ($this->cd_vinculo !== "" && $this->cd_vinculo !== null) && $this->dt_versao_orc !== null && ($this->nu_expediente_no !== "" && $this->nu_expediente_no !== null))) {
			$c = new Criteria(AgentesPublicosPeer::DATABASE_NAME);
			$c->add(AgentesPublicosPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
			$c->add(AgentesPublicosPeer::CD_INGRESSO, $this->cd_ingresso);
			$c->add(AgentesPublicosPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(AgentesPublicosPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(AgentesPublicosPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(AgentesPublicosPeer::CD_VINCULO, $this->cd_vinculo);
			$c->add(AgentesPublicosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$c->add(AgentesPublicosPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
			$this->aAgentesPublicos = AgentesPublicosPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aAgentesPublicos->addGestoresUgs($this);
			 */
		}
		return $this->aAgentesPublicos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aTiposCargos = null;
			$this->aUnidGestoras = null;
			$this->aAgentesPublicos = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseGestoresUg:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseGestoresUg::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseGestoresUg
