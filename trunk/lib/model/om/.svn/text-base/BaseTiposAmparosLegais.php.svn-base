<?php

/**
 * Base class that represents a row from the 'TIPOS_AMPAROS_LEGAIS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:23:17 2010
 *
 * @package    lib.model.om
 */
abstract class BaseTiposAmparosLegais extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        TiposAmparosLegaisPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_amparo_legal field.
	 * @var        string
	 */
	protected $cd_amparo_legal;

	/**
	 * The value for the de_amparo_legal field.
	 * @var        string
	 */
	protected $de_amparo_legal;

	/**
	 * @var        array AgentesPublicos[] Collection to store aggregation of AgentesPublicos objects.
	 */
	protected $collAgentesPublicoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAgentesPublicoss.
	 */
	private $lastAgentesPublicosCriteria = null;

	/**
	 * @var        array ExtincoesItens[] Collection to store aggregation of ExtincoesItens objects.
	 */
	protected $collExtincoesItenss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collExtincoesItenss.
	 */
	private $lastExtincoesItensCriteria = null;

	/**
	 * @var        array ItensRemuneratorios[] Collection to store aggregation of ItensRemuneratorios objects.
	 */
	protected $collItensRemuneratorioss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collItensRemuneratorioss.
	 */
	private $lastItensRemuneratoriosCriteria = null;

	/**
	 * @var        array Reingressos[] Collection to store aggregation of Reingressos objects.
	 */
	protected $collReingressoss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collReingressoss.
	 */
	private $lastReingressosCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'TiposAmparosLegaisPeer';

	/**
	 * Get the [cd_amparo_legal] column value.
	 * 
	 * @return     string
	 */
	public function getCdAmparoLegal()
	{
		return $this->cd_amparo_legal;
	}

	/**
	 * Get the [de_amparo_legal] column value.
	 * 
	 * @return     string
	 */
	public function getDeAmparoLegal()
	{
		return $this->de_amparo_legal;
	}

	/**
	 * Set the value of [cd_amparo_legal] column.
	 * 
	 * @param      string $v new value
	 * @return     TiposAmparosLegais The current object (for fluent API support)
	 */
	public function setCdAmparoLegal($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_amparo_legal !== $v) {
			$this->cd_amparo_legal = $v;
			$this->modifiedColumns[] = TiposAmparosLegaisPeer::CD_AMPARO_LEGAL;
		}

		return $this;
	} // setCdAmparoLegal()

	/**
	 * Set the value of [de_amparo_legal] column.
	 * 
	 * @param      string $v new value
	 * @return     TiposAmparosLegais The current object (for fluent API support)
	 */
	public function setDeAmparoLegal($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->de_amparo_legal !== $v) {
			$this->de_amparo_legal = $v;
			$this->modifiedColumns[] = TiposAmparosLegaisPeer::DE_AMPARO_LEGAL;
		}

		return $this;
	} // setDeAmparoLegal()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_amparo_legal = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->de_amparo_legal = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 2; // 2 = TiposAmparosLegaisPeer::NUM_COLUMNS - TiposAmparosLegaisPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating TiposAmparosLegais object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposAmparosLegaisPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = TiposAmparosLegaisPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collAgentesPublicoss = null;
			$this->lastAgentesPublicosCriteria = null;

			$this->collExtincoesItenss = null;
			$this->lastExtincoesItensCriteria = null;

			$this->collItensRemuneratorioss = null;
			$this->lastItensRemuneratoriosCriteria = null;

			$this->collReingressoss = null;
			$this->lastReingressosCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposAmparosLegaisPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTiposAmparosLegais:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				TiposAmparosLegaisPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTiposAmparosLegais:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(TiposAmparosLegaisPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseTiposAmparosLegais:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseTiposAmparosLegais:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				TiposAmparosLegaisPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = TiposAmparosLegaisPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += TiposAmparosLegaisPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAgentesPublicoss !== null) {
				foreach ($this->collAgentesPublicoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collExtincoesItenss !== null) {
				foreach ($this->collExtincoesItenss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collItensRemuneratorioss !== null) {
				foreach ($this->collItensRemuneratorioss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collReingressoss !== null) {
				foreach ($this->collReingressoss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = TiposAmparosLegaisPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAgentesPublicoss !== null) {
					foreach ($this->collAgentesPublicoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collExtincoesItenss !== null) {
					foreach ($this->collExtincoesItenss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collItensRemuneratorioss !== null) {
					foreach ($this->collItensRemuneratorioss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collReingressoss !== null) {
					foreach ($this->collReingressoss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TiposAmparosLegaisPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdAmparoLegal();
				break;
			case 1:
				return $this->getDeAmparoLegal();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = TiposAmparosLegaisPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdAmparoLegal(),
			$keys[1] => $this->getDeAmparoLegal(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = TiposAmparosLegaisPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdAmparoLegal($value);
				break;
			case 1:
				$this->setDeAmparoLegal($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = TiposAmparosLegaisPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdAmparoLegal($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDeAmparoLegal($arr[$keys[1]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);

		if ($this->isColumnModified(TiposAmparosLegaisPeer::CD_AMPARO_LEGAL)) $criteria->add(TiposAmparosLegaisPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);
		if ($this->isColumnModified(TiposAmparosLegaisPeer::DE_AMPARO_LEGAL)) $criteria->add(TiposAmparosLegaisPeer::DE_AMPARO_LEGAL, $this->de_amparo_legal);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);

		$criteria->add(TiposAmparosLegaisPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getCdAmparoLegal();
	}

	/**
	 * Generic method to set the primary key (cd_amparo_legal column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setCdAmparoLegal($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of TiposAmparosLegais (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdAmparoLegal($this->cd_amparo_legal);

		$copyObj->setDeAmparoLegal($this->de_amparo_legal);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getAgentesPublicoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAgentesPublicos($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getExtincoesItenss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addExtincoesItens($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getItensRemuneratorioss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addItensRemuneratorios($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getReingressoss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addReingressos($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     TiposAmparosLegais Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     TiposAmparosLegaisPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new TiposAmparosLegaisPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collAgentesPublicoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAgentesPublicoss()
	 */
	public function clearAgentesPublicoss()
	{
		$this->collAgentesPublicoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAgentesPublicoss collection (array).
	 *
	 * By default this just sets the collAgentesPublicoss collection to an empty array (like clearcollAgentesPublicoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAgentesPublicoss()
	{
		$this->collAgentesPublicoss = array();
	}

	/**
	 * Gets an array of AgentesPublicos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais has previously been saved, it will retrieve
	 * related AgentesPublicoss from storage. If this TiposAmparosLegais is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array AgentesPublicos[]
	 * @throws     PropelException
	 */
	public function getAgentesPublicoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
			   $this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				AgentesPublicosPeer::addSelectColumns($criteria);
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				AgentesPublicosPeer::addSelectColumns($criteria);
				if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
					$this->collAgentesPublicoss = AgentesPublicosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;
		return $this->collAgentesPublicoss;
	}

	/**
	 * Returns the number of related AgentesPublicos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related AgentesPublicos objects.
	 * @throws     PropelException
	 */
	public function countAgentesPublicoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$count = AgentesPublicosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
					$count = AgentesPublicosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAgentesPublicoss);
				}
			} else {
				$count = count($this->collAgentesPublicoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a AgentesPublicos object to this object
	 * through the AgentesPublicos foreign key attribute.
	 *
	 * @param      AgentesPublicos $l AgentesPublicos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAgentesPublicos(AgentesPublicos $l)
	{
		if ($this->collAgentesPublicoss === null) {
			$this->initAgentesPublicoss();
		}
		if (!in_array($l, $this->collAgentesPublicoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAgentesPublicoss, $l);
			$l->setTiposAmparosLegais($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinTiposIngressos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposIngressos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposIngressos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinVinculos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinVinculos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinVinculos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinTiposExpedientes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposExpedientes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposExpedientes($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinSitFuncionais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinSitFuncionais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinSitFuncionais($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinRegimesJuridicos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinRegimesJuridicos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinRegimesJuridicos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinCboMunicipais($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinCboMunicipais($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinCboMunicipais($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinTiposCargos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposCargos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinTiposCargos($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related AgentesPublicoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getAgentesPublicossJoinServidores($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAgentesPublicoss === null) {
			if ($this->isNew()) {
				$this->collAgentesPublicoss = array();
			} else {

				$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinServidores($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(AgentesPublicosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastAgentesPublicosCriteria) || !$this->lastAgentesPublicosCriteria->equals($criteria)) {
				$this->collAgentesPublicoss = AgentesPublicosPeer::doSelectJoinServidores($criteria, $con, $join_behavior);
			}
		}
		$this->lastAgentesPublicosCriteria = $criteria;

		return $this->collAgentesPublicoss;
	}

	/**
	 * Clears out the collExtincoesItenss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addExtincoesItenss()
	 */
	public function clearExtincoesItenss()
	{
		$this->collExtincoesItenss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collExtincoesItenss collection (array).
	 *
	 * By default this just sets the collExtincoesItenss collection to an empty array (like clearcollExtincoesItenss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initExtincoesItenss()
	{
		$this->collExtincoesItenss = array();
	}

	/**
	 * Gets an array of ExtincoesItens objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais has previously been saved, it will retrieve
	 * related ExtincoesItenss from storage. If this TiposAmparosLegais is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ExtincoesItens[]
	 * @throws     PropelException
	 */
	public function getExtincoesItenss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExtincoesItenss === null) {
			if ($this->isNew()) {
			   $this->collExtincoesItenss = array();
			} else {

				$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ExtincoesItensPeer::addSelectColumns($criteria);
				$this->collExtincoesItenss = ExtincoesItensPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ExtincoesItensPeer::addSelectColumns($criteria);
				if (!isset($this->lastExtincoesItensCriteria) || !$this->lastExtincoesItensCriteria->equals($criteria)) {
					$this->collExtincoesItenss = ExtincoesItensPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastExtincoesItensCriteria = $criteria;
		return $this->collExtincoesItenss;
	}

	/**
	 * Returns the number of related ExtincoesItens objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ExtincoesItens objects.
	 * @throws     PropelException
	 */
	public function countExtincoesItenss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collExtincoesItenss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$count = ExtincoesItensPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				if (!isset($this->lastExtincoesItensCriteria) || !$this->lastExtincoesItensCriteria->equals($criteria)) {
					$count = ExtincoesItensPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collExtincoesItenss);
				}
			} else {
				$count = count($this->collExtincoesItenss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ExtincoesItens object to this object
	 * through the ExtincoesItens foreign key attribute.
	 *
	 * @param      ExtincoesItens $l ExtincoesItens
	 * @return     void
	 * @throws     PropelException
	 */
	public function addExtincoesItens(ExtincoesItens $l)
	{
		if ($this->collExtincoesItenss === null) {
			$this->initExtincoesItenss();
		}
		if (!in_array($l, $this->collExtincoesItenss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collExtincoesItenss, $l);
			$l->setTiposAmparosLegais($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related ExtincoesItenss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getExtincoesItenssJoinItensRemuneratorios($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExtincoesItenss === null) {
			if ($this->isNew()) {
				$this->collExtincoesItenss = array();
			} else {

				$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collExtincoesItenss = ExtincoesItensPeer::doSelectJoinItensRemuneratorios($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastExtincoesItensCriteria) || !$this->lastExtincoesItensCriteria->equals($criteria)) {
				$this->collExtincoesItenss = ExtincoesItensPeer::doSelectJoinItensRemuneratorios($criteria, $con, $join_behavior);
			}
		}
		$this->lastExtincoesItensCriteria = $criteria;

		return $this->collExtincoesItenss;
	}

	/**
	 * Clears out the collItensRemuneratorioss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addItensRemuneratorioss()
	 */
	public function clearItensRemuneratorioss()
	{
		$this->collItensRemuneratorioss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collItensRemuneratorioss collection (array).
	 *
	 * By default this just sets the collItensRemuneratorioss collection to an empty array (like clearcollItensRemuneratorioss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initItensRemuneratorioss()
	{
		$this->collItensRemuneratorioss = array();
	}

	/**
	 * Gets an array of ItensRemuneratorios objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais has previously been saved, it will retrieve
	 * related ItensRemuneratorioss from storage. If this TiposAmparosLegais is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ItensRemuneratorios[]
	 * @throws     PropelException
	 */
	public function getItensRemuneratorioss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collItensRemuneratorioss === null) {
			if ($this->isNew()) {
			   $this->collItensRemuneratorioss = array();
			} else {

				$criteria->add(ItensRemuneratoriosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ItensRemuneratoriosPeer::addSelectColumns($criteria);
				$this->collItensRemuneratorioss = ItensRemuneratoriosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ItensRemuneratoriosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ItensRemuneratoriosPeer::addSelectColumns($criteria);
				if (!isset($this->lastItensRemuneratoriosCriteria) || !$this->lastItensRemuneratoriosCriteria->equals($criteria)) {
					$this->collItensRemuneratorioss = ItensRemuneratoriosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastItensRemuneratoriosCriteria = $criteria;
		return $this->collItensRemuneratorioss;
	}

	/**
	 * Returns the number of related ItensRemuneratorios objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ItensRemuneratorios objects.
	 * @throws     PropelException
	 */
	public function countItensRemuneratorioss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collItensRemuneratorioss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ItensRemuneratoriosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$count = ItensRemuneratoriosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ItensRemuneratoriosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				if (!isset($this->lastItensRemuneratoriosCriteria) || !$this->lastItensRemuneratoriosCriteria->equals($criteria)) {
					$count = ItensRemuneratoriosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collItensRemuneratorioss);
				}
			} else {
				$count = count($this->collItensRemuneratorioss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ItensRemuneratorios object to this object
	 * through the ItensRemuneratorios foreign key attribute.
	 *
	 * @param      ItensRemuneratorios $l ItensRemuneratorios
	 * @return     void
	 * @throws     PropelException
	 */
	public function addItensRemuneratorios(ItensRemuneratorios $l)
	{
		if ($this->collItensRemuneratorioss === null) {
			$this->initItensRemuneratorioss();
		}
		if (!in_array($l, $this->collItensRemuneratorioss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collItensRemuneratorioss, $l);
			$l->setTiposAmparosLegais($this);
		}
	}

	/**
	 * Clears out the collReingressoss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addReingressoss()
	 */
	public function clearReingressoss()
	{
		$this->collReingressoss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collReingressoss collection (array).
	 *
	 * By default this just sets the collReingressoss collection to an empty array (like clearcollReingressoss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initReingressoss()
	{
		$this->collReingressoss = array();
	}

	/**
	 * Gets an array of Reingressos objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais has previously been saved, it will retrieve
	 * related Reingressoss from storage. If this TiposAmparosLegais is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Reingressos[]
	 * @throws     PropelException
	 */
	public function getReingressoss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
			   $this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ReingressosPeer::addSelectColumns($criteria);
				$this->collReingressoss = ReingressosPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				ReingressosPeer::addSelectColumns($criteria);
				if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
					$this->collReingressoss = ReingressosPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastReingressosCriteria = $criteria;
		return $this->collReingressoss;
	}

	/**
	 * Returns the number of related Reingressos objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Reingressos objects.
	 * @throws     PropelException
	 */
	public function countReingressoss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$count = ReingressosPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
					$count = ReingressosPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collReingressoss);
				}
			} else {
				$count = count($this->collReingressoss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Reingressos object to this object
	 * through the Reingressos foreign key attribute.
	 *
	 * @param      Reingressos $l Reingressos
	 * @return     void
	 * @throws     PropelException
	 */
	public function addReingressos(Reingressos $l)
	{
		if ($this->collReingressoss === null) {
			$this->initReingressoss();
		}
		if (!in_array($l, $this->collReingressoss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collReingressoss, $l);
			$l->setTiposAmparosLegais($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getReingressossJoinTiposReingressos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposReingressos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposReingressos($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getReingressossJoinTiposExpedientes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposExpedientes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinTiposExpedientes($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this TiposAmparosLegais is new, it will return
	 * an empty collection; or if this TiposAmparosLegais has previously
	 * been saved, it will retrieve related Reingressoss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in TiposAmparosLegais.
	 */
	public function getReingressossJoinDesligamentos($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(TiposAmparosLegaisPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collReingressoss === null) {
			if ($this->isNew()) {
				$this->collReingressoss = array();
			} else {

				$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

				$this->collReingressoss = ReingressosPeer::doSelectJoinDesligamentos($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ReingressosPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);

			if (!isset($this->lastReingressosCriteria) || !$this->lastReingressosCriteria->equals($criteria)) {
				$this->collReingressoss = ReingressosPeer::doSelectJoinDesligamentos($criteria, $con, $join_behavior);
			}
		}
		$this->lastReingressosCriteria = $criteria;

		return $this->collReingressoss;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collAgentesPublicoss) {
				foreach ((array) $this->collAgentesPublicoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collExtincoesItenss) {
				foreach ((array) $this->collExtincoesItenss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collItensRemuneratorioss) {
				foreach ((array) $this->collItensRemuneratorioss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collReingressoss) {
				foreach ((array) $this->collReingressoss as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collAgentesPublicoss = null;
		$this->collExtincoesItenss = null;
		$this->collItensRemuneratorioss = null;
		$this->collReingressoss = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseTiposAmparosLegais:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseTiposAmparosLegais::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseTiposAmparosLegais
