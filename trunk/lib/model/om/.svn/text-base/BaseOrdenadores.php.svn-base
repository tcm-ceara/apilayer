<?php

/**
 * Base class that represents a row from the 'ORDENADORES' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:23:07 2010
 *
 * @package    lib.model.om
 */
abstract class BaseOrdenadores extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        OrdenadoresPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the dt_versao_orc field.
	 * @var        int
	 */
	protected $dt_versao_orc;

	/**
	 * The value for the cd_unid_gest_ug field.
	 * @var        int
	 */
	protected $cd_unid_gest_ug;

	/**
	 * The value for the cd_orgao field.
	 * @var        string
	 */
	protected $cd_orgao;

	/**
	 * The value for the cd_unid_orc field.
	 * @var        string
	 */
	protected $cd_unid_orc;

	/**
	 * The value for the dt_inclusao_uog field.
	 * @var        string
	 */
	protected $dt_inclusao_uog;

	/**
	 * The value for the cd_cpf_serv_muni field.
	 * @var        string
	 */
	protected $cd_cpf_serv_muni;

	/**
	 * The value for the cd_ingresso field.
	 * @var        string
	 */
	protected $cd_ingresso;

	/**
	 * The value for the cd_vinculo field.
	 * @var        string
	 */
	protected $cd_vinculo;

	/**
	 * The value for the nu_expediente_no field.
	 * @var        string
	 */
	protected $nu_expediente_no;

	/**
	 * The value for the dt_ini_gestao_ord field.
	 * @var        string
	 */
	protected $dt_ini_gestao_ord;

	/**
	 * The value for the dt_ref_ord field.
	 * @var        int
	 */
	protected $dt_ref_ord;

	/**
	 * The value for the nm_ordenador_ord field.
	 * @var        string
	 */
	protected $nm_ordenador_ord;

	/**
	 * The value for the dt_fim_gestao_ord field.
	 * @var        string
	 */
	protected $dt_fim_gestao_ord;

	/**
	 * The value for the cd_tipo_cargo field.
	 * @var        string
	 */
	protected $cd_tipo_cargo;

	/**
	 * @var        UnidOrcUg
	 */
	protected $aUnidOrcUg;

	/**
	 * @var        AgentesPublicos
	 */
	protected $aAgentesPublicos;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'OrdenadoresPeer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [dt_versao_orc] column value.
	 * 
	 * @return     int
	 */
	public function getDtVersaoOrc()
	{
		return $this->dt_versao_orc;
	}

	/**
	 * Get the [cd_unid_gest_ug] column value.
	 * 
	 * @return     int
	 */
	public function getCdUnidGestUg()
	{
		return $this->cd_unid_gest_ug;
	}

	/**
	 * Get the [cd_orgao] column value.
	 * 
	 * @return     string
	 */
	public function getCdOrgao()
	{
		return $this->cd_orgao;
	}

	/**
	 * Get the [cd_unid_orc] column value.
	 * 
	 * @return     string
	 */
	public function getCdUnidOrc()
	{
		return $this->cd_unid_orc;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_inclusao_uog] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtInclusaoUog($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_inclusao_uog === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_inclusao_uog);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_inclusao_uog, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [cd_cpf_serv_muni] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfServMuni()
	{
		return $this->cd_cpf_serv_muni;
	}

	/**
	 * Get the [cd_ingresso] column value.
	 * 
	 * @return     string
	 */
	public function getCdIngresso()
	{
		return $this->cd_ingresso;
	}

	/**
	 * Get the [cd_vinculo] column value.
	 * 
	 * @return     string
	 */
	public function getCdVinculo()
	{
		return $this->cd_vinculo;
	}

	/**
	 * Get the [nu_expediente_no] column value.
	 * 
	 * @return     string
	 */
	public function getNuExpedienteNo()
	{
		return $this->nu_expediente_no;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_ini_gestao_ord] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtIniGestaoOrd($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_ini_gestao_ord === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_ini_gestao_ord);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_ini_gestao_ord, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [dt_ref_ord] column value.
	 * 
	 * @return     int
	 */
	public function getDtRefOrd()
	{
		return $this->dt_ref_ord;
	}

	/**
	 * Get the [nm_ordenador_ord] column value.
	 * 
	 * @return     string
	 */
	public function getNmOrdenadorOrd()
	{
		return $this->nm_ordenador_ord;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_fim_gestao_ord] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtFimGestaoOrd($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_fim_gestao_ord === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_fim_gestao_ord);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_fim_gestao_ord, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [cd_tipo_cargo] column value.
	 * 
	 * @return     string
	 */
	public function getCdTipoCargo()
	{
		return $this->cd_tipo_cargo;
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_MUNICIPIO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdMunicipio() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [dt_versao_orc] column.
	 * 
	 * @param      int $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setDtVersaoOrc($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_versao_orc !== $v) {
			$this->dt_versao_orc = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::DT_VERSAO_ORC;
		}

		if ($this->aUnidOrcUg !== null && $this->aUnidOrcUg->getDtVersaoOrc() !== $v) {
			$this->aUnidOrcUg = null;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getDtVersaoOrc() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setDtVersaoOrc()

	/**
	 * Set the value of [cd_unid_gest_ug] column.
	 * 
	 * @param      int $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdUnidGestUg($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->cd_unid_gest_ug !== $v) {
			$this->cd_unid_gest_ug = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_UNID_GEST_UG;
		}

		if ($this->aUnidOrcUg !== null && $this->aUnidOrcUg->getCdUnidGestUg() !== $v) {
			$this->aUnidOrcUg = null;
		}

		return $this;
	} // setCdUnidGestUg()

	/**
	 * Set the value of [cd_orgao] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdOrgao($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_orgao !== $v) {
			$this->cd_orgao = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_ORGAO;
		}

		if ($this->aUnidOrcUg !== null && $this->aUnidOrcUg->getCdOrgao() !== $v) {
			$this->aUnidOrcUg = null;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdOrgao() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdOrgao()

	/**
	 * Set the value of [cd_unid_orc] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdUnidOrc($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_unid_orc !== $v) {
			$this->cd_unid_orc = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_UNID_ORC;
		}

		if ($this->aUnidOrcUg !== null && $this->aUnidOrcUg->getCdUnidOrc() !== $v) {
			$this->aUnidOrcUg = null;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdUnidOrc() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdUnidOrc()

	/**
	 * Sets the value of [dt_inclusao_uog] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setDtInclusaoUog($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_inclusao_uog !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_inclusao_uog !== null && $tmpDt = new MssqlDateTime($this->dt_inclusao_uog)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_inclusao_uog = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = OrdenadoresPeer::DT_INCLUSAO_UOG;
			}
		} // if either are not null

		if ($this->aUnidOrcUg !== null && $this->aUnidOrcUg->getDtInclusaoUog() !== $v) {
			$this->aUnidOrcUg = null;
		}

		return $this;
	} // setDtInclusaoUog()

	/**
	 * Set the value of [cd_cpf_serv_muni] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdCpfServMuni($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_serv_muni !== $v) {
			$this->cd_cpf_serv_muni = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_CPF_SERV_MUNI;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdCpfServMuni() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdCpfServMuni()

	/**
	 * Set the value of [cd_ingresso] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdIngresso($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_ingresso !== $v) {
			$this->cd_ingresso = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_INGRESSO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdIngresso() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdIngresso()

	/**
	 * Set the value of [cd_vinculo] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdVinculo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_vinculo !== $v) {
			$this->cd_vinculo = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_VINCULO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getCdVinculo() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setCdVinculo()

	/**
	 * Set the value of [nu_expediente_no] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setNuExpedienteNo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_expediente_no !== $v) {
			$this->nu_expediente_no = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::NU_EXPEDIENTE_NO;
		}

		if ($this->aAgentesPublicos !== null && $this->aAgentesPublicos->getNuExpedienteNo() !== $v) {
			$this->aAgentesPublicos = null;
		}

		return $this;
	} // setNuExpedienteNo()

	/**
	 * Sets the value of [dt_ini_gestao_ord] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setDtIniGestaoOrd($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_ini_gestao_ord !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_ini_gestao_ord !== null && $tmpDt = new MssqlDateTime($this->dt_ini_gestao_ord)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_ini_gestao_ord = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = OrdenadoresPeer::DT_INI_GESTAO_ORD;
			}
		} // if either are not null

		return $this;
	} // setDtIniGestaoOrd()

	/**
	 * Set the value of [dt_ref_ord] column.
	 * 
	 * @param      int $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setDtRefOrd($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_ref_ord !== $v) {
			$this->dt_ref_ord = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::DT_REF_ORD;
		}

		return $this;
	} // setDtRefOrd()

	/**
	 * Set the value of [nm_ordenador_ord] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setNmOrdenadorOrd($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nm_ordenador_ord !== $v) {
			$this->nm_ordenador_ord = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::NM_ORDENADOR_ORD;
		}

		return $this;
	} // setNmOrdenadorOrd()

	/**
	 * Sets the value of [dt_fim_gestao_ord] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setDtFimGestaoOrd($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_fim_gestao_ord !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_fim_gestao_ord !== null && $tmpDt = new MssqlDateTime($this->dt_fim_gestao_ord)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_fim_gestao_ord = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = OrdenadoresPeer::DT_FIM_GESTAO_ORD;
			}
		} // if either are not null

		return $this;
	} // setDtFimGestaoOrd()

	/**
	 * Set the value of [cd_tipo_cargo] column.
	 * 
	 * @param      string $v new value
	 * @return     Ordenadores The current object (for fluent API support)
	 */
	public function setCdTipoCargo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_tipo_cargo !== $v) {
			$this->cd_tipo_cargo = $v;
			$this->modifiedColumns[] = OrdenadoresPeer::CD_TIPO_CARGO;
		}

		return $this;
	} // setCdTipoCargo()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->dt_versao_orc = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_unid_gest_ug = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->cd_orgao = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->cd_unid_orc = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->dt_inclusao_uog = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->cd_cpf_serv_muni = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->cd_ingresso = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->cd_vinculo = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->nu_expediente_no = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->dt_ini_gestao_ord = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->dt_ref_ord = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->nm_ordenador_ord = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->dt_fim_gestao_ord = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->cd_tipo_cargo = ($row[$startcol + 14] !== null) ? (string) $row[$startcol + 14] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 15; // 15 = OrdenadoresPeer::NUM_COLUMNS - OrdenadoresPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Ordenadores object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aAgentesPublicos !== null && $this->cd_municipio !== $this->aAgentesPublicos->getCdMunicipio()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidOrcUg !== null && $this->dt_versao_orc !== $this->aUnidOrcUg->getDtVersaoOrc()) {
			$this->aUnidOrcUg = null;
		}
		if ($this->aAgentesPublicos !== null && $this->dt_versao_orc !== $this->aAgentesPublicos->getDtVersaoOrc()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidOrcUg !== null && $this->cd_unid_gest_ug !== $this->aUnidOrcUg->getCdUnidGestUg()) {
			$this->aUnidOrcUg = null;
		}
		if ($this->aUnidOrcUg !== null && $this->cd_orgao !== $this->aUnidOrcUg->getCdOrgao()) {
			$this->aUnidOrcUg = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_orgao !== $this->aAgentesPublicos->getCdOrgao()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidOrcUg !== null && $this->cd_unid_orc !== $this->aUnidOrcUg->getCdUnidOrc()) {
			$this->aUnidOrcUg = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_unid_orc !== $this->aAgentesPublicos->getCdUnidOrc()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aUnidOrcUg !== null && $this->dt_inclusao_uog !== $this->aUnidOrcUg->getDtInclusaoUog()) {
			$this->aUnidOrcUg = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_cpf_serv_muni !== $this->aAgentesPublicos->getCdCpfServMuni()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_ingresso !== $this->aAgentesPublicos->getCdIngresso()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->cd_vinculo !== $this->aAgentesPublicos->getCdVinculo()) {
			$this->aAgentesPublicos = null;
		}
		if ($this->aAgentesPublicos !== null && $this->nu_expediente_no !== $this->aAgentesPublicos->getNuExpedienteNo()) {
			$this->aAgentesPublicos = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OrdenadoresPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = OrdenadoresPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aUnidOrcUg = null;
			$this->aAgentesPublicos = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OrdenadoresPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseOrdenadores:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				OrdenadoresPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseOrdenadores:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(OrdenadoresPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseOrdenadores:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseOrdenadores:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				OrdenadoresPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUnidOrcUg !== null) {
				if ($this->aUnidOrcUg->isModified() || $this->aUnidOrcUg->isNew()) {
					$affectedRows += $this->aUnidOrcUg->save($con);
				}
				$this->setUnidOrcUg($this->aUnidOrcUg);
			}

			if ($this->aAgentesPublicos !== null) {
				if ($this->aAgentesPublicos->isModified() || $this->aAgentesPublicos->isNew()) {
					$affectedRows += $this->aAgentesPublicos->save($con);
				}
				$this->setAgentesPublicos($this->aAgentesPublicos);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = OrdenadoresPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += OrdenadoresPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUnidOrcUg !== null) {
				if (!$this->aUnidOrcUg->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUnidOrcUg->getValidationFailures());
				}
			}

			if ($this->aAgentesPublicos !== null) {
				if (!$this->aAgentesPublicos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAgentesPublicos->getValidationFailures());
				}
			}


			if (($retval = OrdenadoresPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = OrdenadoresPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getDtVersaoOrc();
				break;
			case 2:
				return $this->getCdUnidGestUg();
				break;
			case 3:
				return $this->getCdOrgao();
				break;
			case 4:
				return $this->getCdUnidOrc();
				break;
			case 5:
				return $this->getDtInclusaoUog();
				break;
			case 6:
				return $this->getCdCpfServMuni();
				break;
			case 7:
				return $this->getCdIngresso();
				break;
			case 8:
				return $this->getCdVinculo();
				break;
			case 9:
				return $this->getNuExpedienteNo();
				break;
			case 10:
				return $this->getDtIniGestaoOrd();
				break;
			case 11:
				return $this->getDtRefOrd();
				break;
			case 12:
				return $this->getNmOrdenadorOrd();
				break;
			case 13:
				return $this->getDtFimGestaoOrd();
				break;
			case 14:
				return $this->getCdTipoCargo();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = OrdenadoresPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getDtVersaoOrc(),
			$keys[2] => $this->getCdUnidGestUg(),
			$keys[3] => $this->getCdOrgao(),
			$keys[4] => $this->getCdUnidOrc(),
			$keys[5] => $this->getDtInclusaoUog(),
			$keys[6] => $this->getCdCpfServMuni(),
			$keys[7] => $this->getCdIngresso(),
			$keys[8] => $this->getCdVinculo(),
			$keys[9] => $this->getNuExpedienteNo(),
			$keys[10] => $this->getDtIniGestaoOrd(),
			$keys[11] => $this->getDtRefOrd(),
			$keys[12] => $this->getNmOrdenadorOrd(),
			$keys[13] => $this->getDtFimGestaoOrd(),
			$keys[14] => $this->getCdTipoCargo(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = OrdenadoresPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setDtVersaoOrc($value);
				break;
			case 2:
				$this->setCdUnidGestUg($value);
				break;
			case 3:
				$this->setCdOrgao($value);
				break;
			case 4:
				$this->setCdUnidOrc($value);
				break;
			case 5:
				$this->setDtInclusaoUog($value);
				break;
			case 6:
				$this->setCdCpfServMuni($value);
				break;
			case 7:
				$this->setCdIngresso($value);
				break;
			case 8:
				$this->setCdVinculo($value);
				break;
			case 9:
				$this->setNuExpedienteNo($value);
				break;
			case 10:
				$this->setDtIniGestaoOrd($value);
				break;
			case 11:
				$this->setDtRefOrd($value);
				break;
			case 12:
				$this->setNmOrdenadorOrd($value);
				break;
			case 13:
				$this->setDtFimGestaoOrd($value);
				break;
			case 14:
				$this->setCdTipoCargo($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = OrdenadoresPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setDtVersaoOrc($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdUnidGestUg($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCdOrgao($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setCdUnidOrc($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setDtInclusaoUog($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setCdCpfServMuni($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCdIngresso($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCdVinculo($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setNuExpedienteNo($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setDtIniGestaoOrd($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setDtRefOrd($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setNmOrdenadorOrd($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setDtFimGestaoOrd($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setCdTipoCargo($arr[$keys[14]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(OrdenadoresPeer::DATABASE_NAME);

		if ($this->isColumnModified(OrdenadoresPeer::CD_MUNICIPIO)) $criteria->add(OrdenadoresPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(OrdenadoresPeer::DT_VERSAO_ORC)) $criteria->add(OrdenadoresPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		if ($this->isColumnModified(OrdenadoresPeer::CD_UNID_GEST_UG)) $criteria->add(OrdenadoresPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
		if ($this->isColumnModified(OrdenadoresPeer::CD_ORGAO)) $criteria->add(OrdenadoresPeer::CD_ORGAO, $this->cd_orgao);
		if ($this->isColumnModified(OrdenadoresPeer::CD_UNID_ORC)) $criteria->add(OrdenadoresPeer::CD_UNID_ORC, $this->cd_unid_orc);
		if ($this->isColumnModified(OrdenadoresPeer::DT_INCLUSAO_UOG)) $criteria->add(OrdenadoresPeer::DT_INCLUSAO_UOG, $this->dt_inclusao_uog);
		if ($this->isColumnModified(OrdenadoresPeer::CD_CPF_SERV_MUNI)) $criteria->add(OrdenadoresPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		if ($this->isColumnModified(OrdenadoresPeer::CD_INGRESSO)) $criteria->add(OrdenadoresPeer::CD_INGRESSO, $this->cd_ingresso);
		if ($this->isColumnModified(OrdenadoresPeer::CD_VINCULO)) $criteria->add(OrdenadoresPeer::CD_VINCULO, $this->cd_vinculo);
		if ($this->isColumnModified(OrdenadoresPeer::NU_EXPEDIENTE_NO)) $criteria->add(OrdenadoresPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		if ($this->isColumnModified(OrdenadoresPeer::DT_INI_GESTAO_ORD)) $criteria->add(OrdenadoresPeer::DT_INI_GESTAO_ORD, $this->dt_ini_gestao_ord);
		if ($this->isColumnModified(OrdenadoresPeer::DT_REF_ORD)) $criteria->add(OrdenadoresPeer::DT_REF_ORD, $this->dt_ref_ord);
		if ($this->isColumnModified(OrdenadoresPeer::NM_ORDENADOR_ORD)) $criteria->add(OrdenadoresPeer::NM_ORDENADOR_ORD, $this->nm_ordenador_ord);
		if ($this->isColumnModified(OrdenadoresPeer::DT_FIM_GESTAO_ORD)) $criteria->add(OrdenadoresPeer::DT_FIM_GESTAO_ORD, $this->dt_fim_gestao_ord);
		if ($this->isColumnModified(OrdenadoresPeer::CD_TIPO_CARGO)) $criteria->add(OrdenadoresPeer::CD_TIPO_CARGO, $this->cd_tipo_cargo);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(OrdenadoresPeer::DATABASE_NAME);

		$criteria->add(OrdenadoresPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(OrdenadoresPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
		$criteria->add(OrdenadoresPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
		$criteria->add(OrdenadoresPeer::CD_ORGAO, $this->cd_orgao);
		$criteria->add(OrdenadoresPeer::CD_UNID_ORC, $this->cd_unid_orc);
		$criteria->add(OrdenadoresPeer::DT_INCLUSAO_UOG, $this->dt_inclusao_uog);
		$criteria->add(OrdenadoresPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
		$criteria->add(OrdenadoresPeer::CD_INGRESSO, $this->cd_ingresso);
		$criteria->add(OrdenadoresPeer::CD_VINCULO, $this->cd_vinculo);
		$criteria->add(OrdenadoresPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
		$criteria->add(OrdenadoresPeer::DT_INI_GESTAO_ORD, $this->dt_ini_gestao_ord);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getDtVersaoOrc();

		$pks[2] = $this->getCdUnidGestUg();

		$pks[3] = $this->getCdOrgao();

		$pks[4] = $this->getCdUnidOrc();

		$pks[5] = $this->getDtInclusaoUog();

		$pks[6] = $this->getCdCpfServMuni();

		$pks[7] = $this->getCdIngresso();

		$pks[8] = $this->getCdVinculo();

		$pks[9] = $this->getNuExpedienteNo();

		$pks[10] = $this->getDtIniGestaoOrd();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setDtVersaoOrc($keys[1]);

		$this->setCdUnidGestUg($keys[2]);

		$this->setCdOrgao($keys[3]);

		$this->setCdUnidOrc($keys[4]);

		$this->setDtInclusaoUog($keys[5]);

		$this->setCdCpfServMuni($keys[6]);

		$this->setCdIngresso($keys[7]);

		$this->setCdVinculo($keys[8]);

		$this->setNuExpedienteNo($keys[9]);

		$this->setDtIniGestaoOrd($keys[10]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Ordenadores (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setDtVersaoOrc($this->dt_versao_orc);

		$copyObj->setCdUnidGestUg($this->cd_unid_gest_ug);

		$copyObj->setCdOrgao($this->cd_orgao);

		$copyObj->setCdUnidOrc($this->cd_unid_orc);

		$copyObj->setDtInclusaoUog($this->dt_inclusao_uog);

		$copyObj->setCdCpfServMuni($this->cd_cpf_serv_muni);

		$copyObj->setCdIngresso($this->cd_ingresso);

		$copyObj->setCdVinculo($this->cd_vinculo);

		$copyObj->setNuExpedienteNo($this->nu_expediente_no);

		$copyObj->setDtIniGestaoOrd($this->dt_ini_gestao_ord);

		$copyObj->setDtRefOrd($this->dt_ref_ord);

		$copyObj->setNmOrdenadorOrd($this->nm_ordenador_ord);

		$copyObj->setDtFimGestaoOrd($this->dt_fim_gestao_ord);

		$copyObj->setCdTipoCargo($this->cd_tipo_cargo);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Ordenadores Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     OrdenadoresPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new OrdenadoresPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a UnidOrcUg object.
	 *
	 * @param      UnidOrcUg $v
	 * @return     Ordenadores The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUnidOrcUg(UnidOrcUg $v = null)
	{
		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidGestUg(NULL);
		} else {
			$this->setCdUnidGestUg($v->getCdUnidGestUg());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setDtInclusaoUog(NULL);
		} else {
			$this->setDtInclusaoUog($v->getDtInclusaoUog());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		$this->aUnidOrcUg = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the UnidOrcUg object, it will not be re-added.
		if ($v !== null) {
			$v->addOrdenadores($this);
		}

		return $this;
	}


	/**
	 * Get the associated UnidOrcUg object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     UnidOrcUg The associated UnidOrcUg object.
	 * @throws     PropelException
	 */
	public function getUnidOrcUg(PropelPDO $con = null)
	{
		if ($this->aUnidOrcUg === null && (($this->cd_orgao !== "" && $this->cd_orgao !== null) && $this->cd_unid_gest_ug !== null && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && ($this->dt_inclusao_uog !== "" && $this->dt_inclusao_uog !== null) && $this->dt_versao_orc !== null)) {
			$c = new Criteria(UnidOrcUgPeer::DATABASE_NAME);
			$c->add(UnidOrcUgPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(UnidOrcUgPeer::CD_UNID_GEST_UG, $this->cd_unid_gest_ug);
			$c->add(UnidOrcUgPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(UnidOrcUgPeer::DT_INCLUSAO_UOG, $this->dt_inclusao_uog);
			$c->add(UnidOrcUgPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$this->aUnidOrcUg = UnidOrcUgPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUnidOrcUg->addOrdenadoress($this);
			 */
		}
		return $this->aUnidOrcUg;
	}

	/**
	 * Declares an association between this object and a AgentesPublicos object.
	 *
	 * @param      AgentesPublicos $v
	 * @return     Ordenadores The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAgentesPublicos(AgentesPublicos $v = null)
	{
		if ($v === null) {
			$this->setCdCpfServMuni(NULL);
		} else {
			$this->setCdCpfServMuni($v->getCdCpfServMuni());
		}

		if ($v === null) {
			$this->setCdIngresso(NULL);
		} else {
			$this->setCdIngresso($v->getCdIngresso());
		}

		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setCdOrgao(NULL);
		} else {
			$this->setCdOrgao($v->getCdOrgao());
		}

		if ($v === null) {
			$this->setCdUnidOrc(NULL);
		} else {
			$this->setCdUnidOrc($v->getCdUnidOrc());
		}

		if ($v === null) {
			$this->setCdVinculo(NULL);
		} else {
			$this->setCdVinculo($v->getCdVinculo());
		}

		if ($v === null) {
			$this->setDtVersaoOrc(NULL);
		} else {
			$this->setDtVersaoOrc($v->getDtVersaoOrc());
		}

		if ($v === null) {
			$this->setNuExpedienteNo(NULL);
		} else {
			$this->setNuExpedienteNo($v->getNuExpedienteNo());
		}

		$this->aAgentesPublicos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the AgentesPublicos object, it will not be re-added.
		if ($v !== null) {
			$v->addOrdenadores($this);
		}

		return $this;
	}


	/**
	 * Get the associated AgentesPublicos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     AgentesPublicos The associated AgentesPublicos object.
	 * @throws     PropelException
	 */
	public function getAgentesPublicos(PropelPDO $con = null)
	{
		if ($this->aAgentesPublicos === null && (($this->cd_cpf_serv_muni !== "" && $this->cd_cpf_serv_muni !== null) && ($this->cd_ingresso !== "" && $this->cd_ingresso !== null) && ($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->cd_orgao !== "" && $this->cd_orgao !== null) && ($this->cd_unid_orc !== "" && $this->cd_unid_orc !== null) && ($this->cd_vinculo !== "" && $this->cd_vinculo !== null) && $this->dt_versao_orc !== null && ($this->nu_expediente_no !== "" && $this->nu_expediente_no !== null))) {
			$c = new Criteria(AgentesPublicosPeer::DATABASE_NAME);
			$c->add(AgentesPublicosPeer::CD_CPF_SERV_MUNI, $this->cd_cpf_serv_muni);
			$c->add(AgentesPublicosPeer::CD_INGRESSO, $this->cd_ingresso);
			$c->add(AgentesPublicosPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(AgentesPublicosPeer::CD_ORGAO, $this->cd_orgao);
			$c->add(AgentesPublicosPeer::CD_UNID_ORC, $this->cd_unid_orc);
			$c->add(AgentesPublicosPeer::CD_VINCULO, $this->cd_vinculo);
			$c->add(AgentesPublicosPeer::DT_VERSAO_ORC, $this->dt_versao_orc);
			$c->add(AgentesPublicosPeer::NU_EXPEDIENTE_NO, $this->nu_expediente_no);
			$this->aAgentesPublicos = AgentesPublicosPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aAgentesPublicos->addOrdenadoress($this);
			 */
		}
		return $this->aAgentesPublicos;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aUnidOrcUg = null;
			$this->aAgentesPublicos = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseOrdenadores:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseOrdenadores::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseOrdenadores
