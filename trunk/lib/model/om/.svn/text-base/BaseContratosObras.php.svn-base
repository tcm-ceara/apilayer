<?php

/**
 * Base class that represents a row from the 'CONTRATOS_OBRAS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:22:43 2010
 *
 * @package    lib.model.om
 */
abstract class BaseContratosObras extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ContratosObrasPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the cd_cpf_gestor field.
	 * @var        string
	 */
	protected $cd_cpf_gestor;

	/**
	 * The value for the dt_contrato field.
	 * @var        string
	 */
	protected $dt_contrato;

	/**
	 * The value for the nu_contrato field.
	 * @var        string
	 */
	protected $nu_contrato;

	/**
	 * The value for the dt_ini_obra_srv field.
	 * @var        string
	 */
	protected $dt_ini_obra_srv;

	/**
	 * The value for the tp_obra_srv field.
	 * @var        string
	 */
	protected $tp_obra_srv;

	/**
	 * The value for the nu_obra_srv field.
	 * @var        int
	 */
	protected $nu_obra_srv;

	/**
	 * The value for the dt_prazo_fim_obra_srv field.
	 * @var        string
	 */
	protected $dt_prazo_fim_obra_srv;

	/**
	 * @var        Contratos
	 */
	protected $aContratos;

	/**
	 * @var        ObrasServEng
	 */
	protected $aObrasServEng;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'ContratosObrasPeer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [cd_cpf_gestor] column value.
	 * 
	 * @return     string
	 */
	public function getCdCpfGestor()
	{
		return $this->cd_cpf_gestor;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_contrato] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtContrato($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_contrato === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_contrato);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_contrato, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [nu_contrato] column value.
	 * 
	 * @return     string
	 */
	public function getNuContrato()
	{
		return $this->nu_contrato;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_ini_obra_srv] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtIniObraSrv($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_ini_obra_srv === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_ini_obra_srv);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_ini_obra_srv, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [tp_obra_srv] column value.
	 * 
	 * @return     string
	 */
	public function getTpObraSrv()
	{
		return $this->tp_obra_srv;
	}

	/**
	 * Get the [nu_obra_srv] column value.
	 * 
	 * @return     int
	 */
	public function getNuObraSrv()
	{
		return $this->nu_obra_srv;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_prazo_fim_obra_srv] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtPrazoFimObraSrv($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_prazo_fim_obra_srv === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_prazo_fim_obra_srv);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_prazo_fim_obra_srv, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = ContratosObrasPeer::CD_MUNICIPIO;
		}

		if ($this->aContratos !== null && $this->aContratos->getCdMunicipio() !== $v) {
			$this->aContratos = null;
		}

		if ($this->aObrasServEng !== null && $this->aObrasServEng->getCdMunicipio() !== $v) {
			$this->aObrasServEng = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [cd_cpf_gestor] column.
	 * 
	 * @param      string $v new value
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setCdCpfGestor($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_cpf_gestor !== $v) {
			$this->cd_cpf_gestor = $v;
			$this->modifiedColumns[] = ContratosObrasPeer::CD_CPF_GESTOR;
		}

		return $this;
	} // setCdCpfGestor()

	/**
	 * Sets the value of [dt_contrato] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setDtContrato($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_contrato !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_contrato !== null && $tmpDt = new MssqlDateTime($this->dt_contrato)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_contrato = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ContratosObrasPeer::DT_CONTRATO;
			}
		} // if either are not null

		if ($this->aContratos !== null && $this->aContratos->getDtContrato() !== $v) {
			$this->aContratos = null;
		}

		return $this;
	} // setDtContrato()

	/**
	 * Set the value of [nu_contrato] column.
	 * 
	 * @param      string $v new value
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setNuContrato($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_contrato !== $v) {
			$this->nu_contrato = $v;
			$this->modifiedColumns[] = ContratosObrasPeer::NU_CONTRATO;
		}

		if ($this->aContratos !== null && $this->aContratos->getNuContrato() !== $v) {
			$this->aContratos = null;
		}

		return $this;
	} // setNuContrato()

	/**
	 * Sets the value of [dt_ini_obra_srv] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setDtIniObraSrv($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_ini_obra_srv !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_ini_obra_srv !== null && $tmpDt = new MssqlDateTime($this->dt_ini_obra_srv)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_ini_obra_srv = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ContratosObrasPeer::DT_INI_OBRA_SRV;
			}
		} // if either are not null

		if ($this->aObrasServEng !== null && $this->aObrasServEng->getDtIniObraSrv() !== $v) {
			$this->aObrasServEng = null;
		}

		return $this;
	} // setDtIniObraSrv()

	/**
	 * Set the value of [tp_obra_srv] column.
	 * 
	 * @param      string $v new value
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setTpObraSrv($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->tp_obra_srv !== $v) {
			$this->tp_obra_srv = $v;
			$this->modifiedColumns[] = ContratosObrasPeer::TP_OBRA_SRV;
		}

		if ($this->aObrasServEng !== null && $this->aObrasServEng->getTpObraSrv() !== $v) {
			$this->aObrasServEng = null;
		}

		return $this;
	} // setTpObraSrv()

	/**
	 * Set the value of [nu_obra_srv] column.
	 * 
	 * @param      int $v new value
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setNuObraSrv($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->nu_obra_srv !== $v) {
			$this->nu_obra_srv = $v;
			$this->modifiedColumns[] = ContratosObrasPeer::NU_OBRA_SRV;
		}

		if ($this->aObrasServEng !== null && $this->aObrasServEng->getNuObraSrv() !== $v) {
			$this->aObrasServEng = null;
		}

		return $this;
	} // setNuObraSrv()

	/**
	 * Sets the value of [dt_prazo_fim_obra_srv] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ContratosObras The current object (for fluent API support)
	 */
	public function setDtPrazoFimObraSrv($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_prazo_fim_obra_srv !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_prazo_fim_obra_srv !== null && $tmpDt = new MssqlDateTime($this->dt_prazo_fim_obra_srv)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_prazo_fim_obra_srv = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ContratosObrasPeer::DT_PRAZO_FIM_OBRA_SRV;
			}
		} // if either are not null

		return $this;
	} // setDtPrazoFimObraSrv()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->cd_cpf_gestor = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->dt_contrato = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->nu_contrato = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->dt_ini_obra_srv = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->tp_obra_srv = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->nu_obra_srv = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->dt_prazo_fim_obra_srv = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 8; // 8 = ContratosObrasPeer::NUM_COLUMNS - ContratosObrasPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating ContratosObras object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aContratos !== null && $this->cd_municipio !== $this->aContratos->getCdMunicipio()) {
			$this->aContratos = null;
		}
		if ($this->aObrasServEng !== null && $this->cd_municipio !== $this->aObrasServEng->getCdMunicipio()) {
			$this->aObrasServEng = null;
		}
		if ($this->aContratos !== null && $this->dt_contrato !== $this->aContratos->getDtContrato()) {
			$this->aContratos = null;
		}
		if ($this->aContratos !== null && $this->nu_contrato !== $this->aContratos->getNuContrato()) {
			$this->aContratos = null;
		}
		if ($this->aObrasServEng !== null && $this->dt_ini_obra_srv !== $this->aObrasServEng->getDtIniObraSrv()) {
			$this->aObrasServEng = null;
		}
		if ($this->aObrasServEng !== null && $this->tp_obra_srv !== $this->aObrasServEng->getTpObraSrv()) {
			$this->aObrasServEng = null;
		}
		if ($this->aObrasServEng !== null && $this->nu_obra_srv !== $this->aObrasServEng->getNuObraSrv()) {
			$this->aObrasServEng = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ContratosObrasPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ContratosObrasPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aContratos = null;
			$this->aObrasServEng = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ContratosObrasPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseContratosObras:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				ContratosObrasPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseContratosObras:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ContratosObrasPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseContratosObras:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseContratosObras:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				ContratosObrasPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aContratos !== null) {
				if ($this->aContratos->isModified() || $this->aContratos->isNew()) {
					$affectedRows += $this->aContratos->save($con);
				}
				$this->setContratos($this->aContratos);
			}

			if ($this->aObrasServEng !== null) {
				if ($this->aObrasServEng->isModified() || $this->aObrasServEng->isNew()) {
					$affectedRows += $this->aObrasServEng->save($con);
				}
				$this->setObrasServEng($this->aObrasServEng);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ContratosObrasPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += ContratosObrasPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aContratos !== null) {
				if (!$this->aContratos->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aContratos->getValidationFailures());
				}
			}

			if ($this->aObrasServEng !== null) {
				if (!$this->aObrasServEng->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aObrasServEng->getValidationFailures());
				}
			}


			if (($retval = ContratosObrasPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ContratosObrasPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getCdCpfGestor();
				break;
			case 2:
				return $this->getDtContrato();
				break;
			case 3:
				return $this->getNuContrato();
				break;
			case 4:
				return $this->getDtIniObraSrv();
				break;
			case 5:
				return $this->getTpObraSrv();
				break;
			case 6:
				return $this->getNuObraSrv();
				break;
			case 7:
				return $this->getDtPrazoFimObraSrv();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ContratosObrasPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getCdCpfGestor(),
			$keys[2] => $this->getDtContrato(),
			$keys[3] => $this->getNuContrato(),
			$keys[4] => $this->getDtIniObraSrv(),
			$keys[5] => $this->getTpObraSrv(),
			$keys[6] => $this->getNuObraSrv(),
			$keys[7] => $this->getDtPrazoFimObraSrv(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ContratosObrasPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setCdCpfGestor($value);
				break;
			case 2:
				$this->setDtContrato($value);
				break;
			case 3:
				$this->setNuContrato($value);
				break;
			case 4:
				$this->setDtIniObraSrv($value);
				break;
			case 5:
				$this->setTpObraSrv($value);
				break;
			case 6:
				$this->setNuObraSrv($value);
				break;
			case 7:
				$this->setDtPrazoFimObraSrv($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ContratosObrasPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCdCpfGestor($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDtContrato($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setNuContrato($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setDtIniObraSrv($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setTpObraSrv($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setNuObraSrv($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setDtPrazoFimObraSrv($arr[$keys[7]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ContratosObrasPeer::DATABASE_NAME);

		if ($this->isColumnModified(ContratosObrasPeer::CD_MUNICIPIO)) $criteria->add(ContratosObrasPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(ContratosObrasPeer::CD_CPF_GESTOR)) $criteria->add(ContratosObrasPeer::CD_CPF_GESTOR, $this->cd_cpf_gestor);
		if ($this->isColumnModified(ContratosObrasPeer::DT_CONTRATO)) $criteria->add(ContratosObrasPeer::DT_CONTRATO, $this->dt_contrato);
		if ($this->isColumnModified(ContratosObrasPeer::NU_CONTRATO)) $criteria->add(ContratosObrasPeer::NU_CONTRATO, $this->nu_contrato);
		if ($this->isColumnModified(ContratosObrasPeer::DT_INI_OBRA_SRV)) $criteria->add(ContratosObrasPeer::DT_INI_OBRA_SRV, $this->dt_ini_obra_srv);
		if ($this->isColumnModified(ContratosObrasPeer::TP_OBRA_SRV)) $criteria->add(ContratosObrasPeer::TP_OBRA_SRV, $this->tp_obra_srv);
		if ($this->isColumnModified(ContratosObrasPeer::NU_OBRA_SRV)) $criteria->add(ContratosObrasPeer::NU_OBRA_SRV, $this->nu_obra_srv);
		if ($this->isColumnModified(ContratosObrasPeer::DT_PRAZO_FIM_OBRA_SRV)) $criteria->add(ContratosObrasPeer::DT_PRAZO_FIM_OBRA_SRV, $this->dt_prazo_fim_obra_srv);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ContratosObrasPeer::DATABASE_NAME);

		$criteria->add(ContratosObrasPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(ContratosObrasPeer::CD_CPF_GESTOR, $this->cd_cpf_gestor);
		$criteria->add(ContratosObrasPeer::DT_CONTRATO, $this->dt_contrato);
		$criteria->add(ContratosObrasPeer::NU_CONTRATO, $this->nu_contrato);
		$criteria->add(ContratosObrasPeer::DT_INI_OBRA_SRV, $this->dt_ini_obra_srv);
		$criteria->add(ContratosObrasPeer::TP_OBRA_SRV, $this->tp_obra_srv);
		$criteria->add(ContratosObrasPeer::NU_OBRA_SRV, $this->nu_obra_srv);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getCdCpfGestor();

		$pks[2] = $this->getDtContrato();

		$pks[3] = $this->getNuContrato();

		$pks[4] = $this->getDtIniObraSrv();

		$pks[5] = $this->getTpObraSrv();

		$pks[6] = $this->getNuObraSrv();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setCdCpfGestor($keys[1]);

		$this->setDtContrato($keys[2]);

		$this->setNuContrato($keys[3]);

		$this->setDtIniObraSrv($keys[4]);

		$this->setTpObraSrv($keys[5]);

		$this->setNuObraSrv($keys[6]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of ContratosObras (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setCdCpfGestor($this->cd_cpf_gestor);

		$copyObj->setDtContrato($this->dt_contrato);

		$copyObj->setNuContrato($this->nu_contrato);

		$copyObj->setDtIniObraSrv($this->dt_ini_obra_srv);

		$copyObj->setTpObraSrv($this->tp_obra_srv);

		$copyObj->setNuObraSrv($this->nu_obra_srv);

		$copyObj->setDtPrazoFimObraSrv($this->dt_prazo_fim_obra_srv);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     ContratosObras Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ContratosObrasPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ContratosObrasPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Contratos object.
	 *
	 * @param      Contratos $v
	 * @return     ContratosObras The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setContratos(Contratos $v = null)
	{
		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setDtContrato(NULL);
		} else {
			$this->setDtContrato($v->getDtContrato());
		}

		if ($v === null) {
			$this->setNuContrato(NULL);
		} else {
			$this->setNuContrato($v->getNuContrato());
		}

		$this->aContratos = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Contratos object, it will not be re-added.
		if ($v !== null) {
			$v->addContratosObras($this);
		}

		return $this;
	}


	/**
	 * Get the associated Contratos object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Contratos The associated Contratos object.
	 * @throws     PropelException
	 */
	public function getContratos(PropelPDO $con = null)
	{
		if ($this->aContratos === null && (($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->dt_contrato !== "" && $this->dt_contrato !== null) && ($this->nu_contrato !== "" && $this->nu_contrato !== null))) {
			$c = new Criteria(ContratosPeer::DATABASE_NAME);
			$c->add(ContratosPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(ContratosPeer::DT_CONTRATO, $this->dt_contrato);
			$c->add(ContratosPeer::NU_CONTRATO, $this->nu_contrato);
			$this->aContratos = ContratosPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aContratos->addContratosObrass($this);
			 */
		}
		return $this->aContratos;
	}

	/**
	 * Declares an association between this object and a ObrasServEng object.
	 *
	 * @param      ObrasServEng $v
	 * @return     ContratosObras The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setObrasServEng(ObrasServEng $v = null)
	{
		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		if ($v === null) {
			$this->setDtIniObraSrv(NULL);
		} else {
			$this->setDtIniObraSrv($v->getDtIniObraSrv());
		}

		if ($v === null) {
			$this->setNuObraSrv(NULL);
		} else {
			$this->setNuObraSrv($v->getNuObraSrv());
		}

		if ($v === null) {
			$this->setTpObraSrv(NULL);
		} else {
			$this->setTpObraSrv($v->getTpObraSrv());
		}

		$this->aObrasServEng = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the ObrasServEng object, it will not be re-added.
		if ($v !== null) {
			$v->addContratosObras($this);
		}

		return $this;
	}


	/**
	 * Get the associated ObrasServEng object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     ObrasServEng The associated ObrasServEng object.
	 * @throws     PropelException
	 */
	public function getObrasServEng(PropelPDO $con = null)
	{
		if ($this->aObrasServEng === null && (($this->cd_municipio !== "" && $this->cd_municipio !== null) && ($this->dt_ini_obra_srv !== "" && $this->dt_ini_obra_srv !== null) && $this->nu_obra_srv !== null && ($this->tp_obra_srv !== "" && $this->tp_obra_srv !== null))) {
			$c = new Criteria(ObrasServEngPeer::DATABASE_NAME);
			$c->add(ObrasServEngPeer::CD_MUNICIPIO, $this->cd_municipio);
			$c->add(ObrasServEngPeer::DT_INI_OBRA_SRV, $this->dt_ini_obra_srv);
			$c->add(ObrasServEngPeer::NU_OBRA_SRV, $this->nu_obra_srv);
			$c->add(ObrasServEngPeer::TP_OBRA_SRV, $this->tp_obra_srv);
			$this->aObrasServEng = ObrasServEngPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aObrasServEng->addContratosObrass($this);
			 */
		}
		return $this->aObrasServEng;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aContratos = null;
			$this->aObrasServEng = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseContratosObras:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseContratosObras::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseContratosObras
