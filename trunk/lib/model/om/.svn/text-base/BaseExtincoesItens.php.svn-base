<?php

/**
 * Base class that represents a row from the 'EXTINCOES_ITENS' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.1 on:
 *
 * Thu Jul 15 18:22:53 2010
 *
 * @package    lib.model.om
 */
abstract class BaseExtincoesItens extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ExtincoesItensPeer
	 */
	protected static $peer;

	/**
	 * The value for the cd_municipio field.
	 * @var        string
	 */
	protected $cd_municipio;

	/**
	 * The value for the cd_item_ir field.
	 * @var        int
	 */
	protected $cd_item_ir;

	/**
	 * The value for the cd_amparo_legal field.
	 * @var        string
	 */
	protected $cd_amparo_legal;

	/**
	 * The value for the nu_amparo_legal_er field.
	 * @var        string
	 */
	protected $nu_amparo_legal_er;

	/**
	 * The value for the dt_amparo_legal_er field.
	 * @var        string
	 */
	protected $dt_amparo_legal_er;

	/**
	 * The value for the dt_public_er field.
	 * @var        string
	 */
	protected $dt_public_er;

	/**
	 * The value for the nu_decreto_er field.
	 * @var        string
	 */
	protected $nu_decreto_er;

	/**
	 * The value for the dt_decreto_er field.
	 * @var        string
	 */
	protected $dt_decreto_er;

	/**
	 * The value for the dt_public_decreto_er field.
	 * @var        string
	 */
	protected $dt_public_decreto_er;

	/**
	 * The value for the dt_ref_er field.
	 * @var        int
	 */
	protected $dt_ref_er;

	/**
	 * @var        ItensRemuneratorios
	 */
	protected $aItensRemuneratorios;

	/**
	 * @var        TiposAmparosLegais
	 */
	protected $aTiposAmparosLegais;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'ExtincoesItensPeer';

	/**
	 * Get the [cd_municipio] column value.
	 * 
	 * @return     string
	 */
	public function getCdMunicipio()
	{
		return $this->cd_municipio;
	}

	/**
	 * Get the [cd_item_ir] column value.
	 * 
	 * @return     int
	 */
	public function getCdItemIr()
	{
		return $this->cd_item_ir;
	}

	/**
	 * Get the [cd_amparo_legal] column value.
	 * 
	 * @return     string
	 */
	public function getCdAmparoLegal()
	{
		return $this->cd_amparo_legal;
	}

	/**
	 * Get the [nu_amparo_legal_er] column value.
	 * 
	 * @return     string
	 */
	public function getNuAmparoLegalEr()
	{
		return $this->nu_amparo_legal_er;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_amparo_legal_er] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtAmparoLegalEr($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_amparo_legal_er === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_amparo_legal_er);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_amparo_legal_er, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [dt_public_er] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtPublicEr($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_public_er === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_public_er);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_public_er, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [nu_decreto_er] column value.
	 * 
	 * @return     string
	 */
	public function getNuDecretoEr()
	{
		return $this->nu_decreto_er;
	}

	/**
	 * Get the [optionally formatted] temporal [dt_decreto_er] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtDecretoEr($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_decreto_er === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_decreto_er);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_decreto_er, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [dt_public_decreto_er] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or MssqlDateTime object (if format is NULL), NULL if column is NULL
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDtPublicDecretoEr($format = 'Y-m-d H:i:s')
	{
		if ($this->dt_public_decreto_er === null) {
			return null;
		}



		try {
			$dt = new MssqlDateTime($this->dt_public_decreto_er);
		} catch (Exception $x) {
			throw new PropelException("Internally stored date/time/timestamp value could not be converted to MssqlDateTime: " . var_export($this->dt_public_decreto_er, true), $x);
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a MssqlDateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [dt_ref_er] column value.
	 * 
	 * @return     int
	 */
	public function getDtRefEr()
	{
		return $this->dt_ref_er;
	}

	/**
	 * Set the value of [cd_municipio] column.
	 * 
	 * @param      string $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setCdMunicipio($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_municipio !== $v) {
			$this->cd_municipio = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::CD_MUNICIPIO;
		}

		if ($this->aItensRemuneratorios !== null && $this->aItensRemuneratorios->getCdMunicipio() !== $v) {
			$this->aItensRemuneratorios = null;
		}

		return $this;
	} // setCdMunicipio()

	/**
	 * Set the value of [cd_item_ir] column.
	 * 
	 * @param      int $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setCdItemIr($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->cd_item_ir !== $v) {
			$this->cd_item_ir = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::CD_ITEM_IR;
		}

		return $this;
	} // setCdItemIr()

	/**
	 * Set the value of [cd_amparo_legal] column.
	 * 
	 * @param      string $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setCdAmparoLegal($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cd_amparo_legal !== $v) {
			$this->cd_amparo_legal = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::CD_AMPARO_LEGAL;
		}

		if ($this->aTiposAmparosLegais !== null && $this->aTiposAmparosLegais->getCdAmparoLegal() !== $v) {
			$this->aTiposAmparosLegais = null;
		}

		return $this;
	} // setCdAmparoLegal()

	/**
	 * Set the value of [nu_amparo_legal_er] column.
	 * 
	 * @param      string $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setNuAmparoLegalEr($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_amparo_legal_er !== $v) {
			$this->nu_amparo_legal_er = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::NU_AMPARO_LEGAL_ER;
		}

		return $this;
	} // setNuAmparoLegalEr()

	/**
	 * Sets the value of [dt_amparo_legal_er] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setDtAmparoLegalEr($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_amparo_legal_er !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_amparo_legal_er !== null && $tmpDt = new MssqlDateTime($this->dt_amparo_legal_er)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_amparo_legal_er = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ExtincoesItensPeer::DT_AMPARO_LEGAL_ER;
			}
		} // if either are not null

		return $this;
	} // setDtAmparoLegalEr()

	/**
	 * Sets the value of [dt_public_er] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setDtPublicEr($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_public_er !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_public_er !== null && $tmpDt = new MssqlDateTime($this->dt_public_er)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_public_er = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ExtincoesItensPeer::DT_PUBLIC_ER;
			}
		} // if either are not null

		return $this;
	} // setDtPublicEr()

	/**
	 * Set the value of [nu_decreto_er] column.
	 * 
	 * @param      string $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setNuDecretoEr($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nu_decreto_er !== $v) {
			$this->nu_decreto_er = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::NU_DECRETO_ER;
		}

		return $this;
	} // setNuDecretoEr()

	/**
	 * Sets the value of [dt_decreto_er] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setDtDecretoEr($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_decreto_er !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_decreto_er !== null && $tmpDt = new MssqlDateTime($this->dt_decreto_er)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_decreto_er = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ExtincoesItensPeer::DT_DECRETO_ER;
			}
		} // if either are not null

		return $this;
	} // setDtDecretoEr()

	/**
	 * Sets the value of [dt_public_decreto_er] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setDtPublicDecretoEr($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new MssqlDateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new MssqlDateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->dt_public_decreto_er !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->dt_public_decreto_er !== null && $tmpDt = new MssqlDateTime($this->dt_public_decreto_er)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->dt_public_decreto_er = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = ExtincoesItensPeer::DT_PUBLIC_DECRETO_ER;
			}
		} // if either are not null

		return $this;
	} // setDtPublicDecretoEr()

	/**
	 * Set the value of [dt_ref_er] column.
	 * 
	 * @param      int $v new value
	 * @return     ExtincoesItens The current object (for fluent API support)
	 */
	public function setDtRefEr($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->dt_ref_er !== $v) {
			$this->dt_ref_er = $v;
			$this->modifiedColumns[] = ExtincoesItensPeer::DT_REF_ER;
		}

		return $this;
	} // setDtRefEr()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->cd_municipio = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->cd_item_ir = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->cd_amparo_legal = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->nu_amparo_legal_er = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->dt_amparo_legal_er = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->dt_public_er = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->nu_decreto_er = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->dt_decreto_er = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->dt_public_decreto_er = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->dt_ref_er = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = ExtincoesItensPeer::NUM_COLUMNS - ExtincoesItensPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating ExtincoesItens object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aItensRemuneratorios !== null && $this->cd_municipio !== $this->aItensRemuneratorios->getCdMunicipio()) {
			$this->aItensRemuneratorios = null;
		}
		if ($this->aTiposAmparosLegais !== null && $this->cd_amparo_legal !== $this->aTiposAmparosLegais->getCdAmparoLegal()) {
			$this->aTiposAmparosLegais = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExtincoesItensPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ExtincoesItensPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aItensRemuneratorios = null;
			$this->aTiposAmparosLegais = null;
		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExtincoesItensPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseExtincoesItens:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				ExtincoesItensPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseExtincoesItens:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExtincoesItensPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseExtincoesItens:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseExtincoesItens:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				ExtincoesItensPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aItensRemuneratorios !== null) {
				if ($this->aItensRemuneratorios->isModified() || $this->aItensRemuneratorios->isNew()) {
					$affectedRows += $this->aItensRemuneratorios->save($con);
				}
				$this->setItensRemuneratorios($this->aItensRemuneratorios);
			}

			if ($this->aTiposAmparosLegais !== null) {
				if ($this->aTiposAmparosLegais->isModified() || $this->aTiposAmparosLegais->isNew()) {
					$affectedRows += $this->aTiposAmparosLegais->save($con);
				}
				$this->setTiposAmparosLegais($this->aTiposAmparosLegais);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ExtincoesItensPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += ExtincoesItensPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aItensRemuneratorios !== null) {
				if (!$this->aItensRemuneratorios->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aItensRemuneratorios->getValidationFailures());
				}
			}

			if ($this->aTiposAmparosLegais !== null) {
				if (!$this->aTiposAmparosLegais->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTiposAmparosLegais->getValidationFailures());
				}
			}


			if (($retval = ExtincoesItensPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}



			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ExtincoesItensPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getCdMunicipio();
				break;
			case 1:
				return $this->getCdItemIr();
				break;
			case 2:
				return $this->getCdAmparoLegal();
				break;
			case 3:
				return $this->getNuAmparoLegalEr();
				break;
			case 4:
				return $this->getDtAmparoLegalEr();
				break;
			case 5:
				return $this->getDtPublicEr();
				break;
			case 6:
				return $this->getNuDecretoEr();
				break;
			case 7:
				return $this->getDtDecretoEr();
				break;
			case 8:
				return $this->getDtPublicDecretoEr();
				break;
			case 9:
				return $this->getDtRefEr();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ExtincoesItensPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getCdMunicipio(),
			$keys[1] => $this->getCdItemIr(),
			$keys[2] => $this->getCdAmparoLegal(),
			$keys[3] => $this->getNuAmparoLegalEr(),
			$keys[4] => $this->getDtAmparoLegalEr(),
			$keys[5] => $this->getDtPublicEr(),
			$keys[6] => $this->getNuDecretoEr(),
			$keys[7] => $this->getDtDecretoEr(),
			$keys[8] => $this->getDtPublicDecretoEr(),
			$keys[9] => $this->getDtRefEr(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ExtincoesItensPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setCdMunicipio($value);
				break;
			case 1:
				$this->setCdItemIr($value);
				break;
			case 2:
				$this->setCdAmparoLegal($value);
				break;
			case 3:
				$this->setNuAmparoLegalEr($value);
				break;
			case 4:
				$this->setDtAmparoLegalEr($value);
				break;
			case 5:
				$this->setDtPublicEr($value);
				break;
			case 6:
				$this->setNuDecretoEr($value);
				break;
			case 7:
				$this->setDtDecretoEr($value);
				break;
			case 8:
				$this->setDtPublicDecretoEr($value);
				break;
			case 9:
				$this->setDtRefEr($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ExtincoesItensPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setCdMunicipio($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCdItemIr($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCdAmparoLegal($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setNuAmparoLegalEr($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setDtAmparoLegalEr($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setDtPublicEr($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setNuDecretoEr($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setDtDecretoEr($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setDtPublicDecretoEr($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setDtRefEr($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ExtincoesItensPeer::DATABASE_NAME);

		if ($this->isColumnModified(ExtincoesItensPeer::CD_MUNICIPIO)) $criteria->add(ExtincoesItensPeer::CD_MUNICIPIO, $this->cd_municipio);
		if ($this->isColumnModified(ExtincoesItensPeer::CD_ITEM_IR)) $criteria->add(ExtincoesItensPeer::CD_ITEM_IR, $this->cd_item_ir);
		if ($this->isColumnModified(ExtincoesItensPeer::CD_AMPARO_LEGAL)) $criteria->add(ExtincoesItensPeer::CD_AMPARO_LEGAL, $this->cd_amparo_legal);
		if ($this->isColumnModified(ExtincoesItensPeer::NU_AMPARO_LEGAL_ER)) $criteria->add(ExtincoesItensPeer::NU_AMPARO_LEGAL_ER, $this->nu_amparo_legal_er);
		if ($this->isColumnModified(ExtincoesItensPeer::DT_AMPARO_LEGAL_ER)) $criteria->add(ExtincoesItensPeer::DT_AMPARO_LEGAL_ER, $this->dt_amparo_legal_er);
		if ($this->isColumnModified(ExtincoesItensPeer::DT_PUBLIC_ER)) $criteria->add(ExtincoesItensPeer::DT_PUBLIC_ER, $this->dt_public_er);
		if ($this->isColumnModified(ExtincoesItensPeer::NU_DECRETO_ER)) $criteria->add(ExtincoesItensPeer::NU_DECRETO_ER, $this->nu_decreto_er);
		if ($this->isColumnModified(ExtincoesItensPeer::DT_DECRETO_ER)) $criteria->add(ExtincoesItensPeer::DT_DECRETO_ER, $this->dt_decreto_er);
		if ($this->isColumnModified(ExtincoesItensPeer::DT_PUBLIC_DECRETO_ER)) $criteria->add(ExtincoesItensPeer::DT_PUBLIC_DECRETO_ER, $this->dt_public_decreto_er);
		if ($this->isColumnModified(ExtincoesItensPeer::DT_REF_ER)) $criteria->add(ExtincoesItensPeer::DT_REF_ER, $this->dt_ref_er);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ExtincoesItensPeer::DATABASE_NAME);

		$criteria->add(ExtincoesItensPeer::CD_MUNICIPIO, $this->cd_municipio);
		$criteria->add(ExtincoesItensPeer::CD_ITEM_IR, $this->cd_item_ir);

		return $criteria;
	}

	/**
	 * Returns the composite primary key for this object.
	 * The array elements will be in same order as specified in XML.
	 * @return     array
	 */
	public function getPrimaryKey()
	{
		$pks = array();

		$pks[0] = $this->getCdMunicipio();

		$pks[1] = $this->getCdItemIr();

		return $pks;
	}

	/**
	 * Set the [composite] primary key.
	 *
	 * @param      array $keys The elements of the composite key (order must match the order in XML file).
	 * @return     void
	 */
	public function setPrimaryKey($keys)
	{

		$this->setCdMunicipio($keys[0]);

		$this->setCdItemIr($keys[1]);

	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of ExtincoesItens (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCdMunicipio($this->cd_municipio);

		$copyObj->setCdItemIr($this->cd_item_ir);

		$copyObj->setCdAmparoLegal($this->cd_amparo_legal);

		$copyObj->setNuAmparoLegalEr($this->nu_amparo_legal_er);

		$copyObj->setDtAmparoLegalEr($this->dt_amparo_legal_er);

		$copyObj->setDtPublicEr($this->dt_public_er);

		$copyObj->setNuDecretoEr($this->nu_decreto_er);

		$copyObj->setDtDecretoEr($this->dt_decreto_er);

		$copyObj->setDtPublicDecretoEr($this->dt_public_decreto_er);

		$copyObj->setDtRefEr($this->dt_ref_er);


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     ExtincoesItens Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ExtincoesItensPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ExtincoesItensPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a ItensRemuneratorios object.
	 *
	 * @param      ItensRemuneratorios $v
	 * @return     ExtincoesItens The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setItensRemuneratorios(ItensRemuneratorios $v = null)
	{
		if ($v === null) {
			$this->setCdMunicipio(NULL);
		} else {
			$this->setCdMunicipio($v->getCdMunicipio());
		}

		$this->aItensRemuneratorios = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the ItensRemuneratorios object, it will not be re-added.
		if ($v !== null) {
			$v->addExtincoesItens($this);
		}

		return $this;
	}


	/**
	 * Get the associated ItensRemuneratorios object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     ItensRemuneratorios The associated ItensRemuneratorios object.
	 * @throws     PropelException
	 */
	public function getItensRemuneratorios(PropelPDO $con = null)
	{
		if ($this->aItensRemuneratorios === null && (($this->cd_municipio !== "" && $this->cd_municipio !== null))) {
			$this->aItensRemuneratorios = ItensRemuneratoriosPeer::retrieveByPk($this->cd_municipio);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aItensRemuneratorios->addExtincoesItenss($this);
			 */
		}
		return $this->aItensRemuneratorios;
	}

	/**
	 * Declares an association between this object and a TiposAmparosLegais object.
	 *
	 * @param      TiposAmparosLegais $v
	 * @return     ExtincoesItens The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTiposAmparosLegais(TiposAmparosLegais $v = null)
	{
		if ($v === null) {
			$this->setCdAmparoLegal(NULL);
		} else {
			$this->setCdAmparoLegal($v->getCdAmparoLegal());
		}

		$this->aTiposAmparosLegais = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the TiposAmparosLegais object, it will not be re-added.
		if ($v !== null) {
			$v->addExtincoesItens($this);
		}

		return $this;
	}


	/**
	 * Get the associated TiposAmparosLegais object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     TiposAmparosLegais The associated TiposAmparosLegais object.
	 * @throws     PropelException
	 */
	public function getTiposAmparosLegais(PropelPDO $con = null)
	{
		if ($this->aTiposAmparosLegais === null && (($this->cd_amparo_legal !== "" && $this->cd_amparo_legal !== null))) {
			$this->aTiposAmparosLegais = TiposAmparosLegaisPeer::retrieveByPk($this->cd_amparo_legal);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aTiposAmparosLegais->addExtincoesItenss($this);
			 */
		}
		return $this->aTiposAmparosLegais;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
		} // if ($deep)

			$this->aItensRemuneratorios = null;
			$this->aTiposAmparosLegais = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseExtincoesItens:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseExtincoesItens::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseExtincoesItens
